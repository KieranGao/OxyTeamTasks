# OxyTeamTask 客户端开发日志

---

## 2026-05-20 | 第一阶段：客户端页面框架搭建

### 开发成果

完成 Vue 3 + Vite 客户端项目初始化与页面框架搭建，建立了完整的项目骨架。

**技术栈选型：**
- Vue 3 (Composition API + `<script setup>`) — 前端框架
- Vite 5 — 构建工具
- Vue Router 4 (Hash 模式) — 路由管理，为后续 Electron 集成做兼容
- Pinia 2 — 状态管理，Token/用户信息持久化至 localStorage
- Axios — HTTP 请求客户端，已配置拦截器自动注入 Auth Token
- Element Plus — 桌面端 UI 组件库，中文友好、Vue 3 生态成熟

**项目结构（`Client/` 目录）：**

```
Client/
├── index.html                     # 入口 HTML
├── package.json                   # 依赖与脚本配置
├── vite.config.js                 # Vite 构建配置 (base: './' 兼容 Electron)
└── src/
    ├── main.js                    # 应用入口，挂载 Pinia/Router/ElementPlus
    ├── App.vue                    # 根组件
    ├── config/index.js            # 服务器地址与 API 配置
    ├── router/index.js            # 12 条路由 + 全局导航守卫
    ├── stores/
    │   ├── user.js                # 用户状态 (Token/UID/角色)，登录/注册/登出
    │   └── app.js                 # 全局 UI 状态 (主题/侧边栏/页面标题)
    ├── api/
    │   ├── request.js             # Axios 实例 (BaseURL/拦截器/401 处理)
    │   └── user.js                # 用户相关 API (验证码/注册/登录/重置密码)
    ├── layout/
    │   ├── MainLayout.vue         # 主布局：侧边栏 + 顶栏 + 内容区
    │   └── AuthLayout.vue         # 登录/注册布局：居中卡片 + 渐变背景
    ├── views/
    │   ├── login/LoginView.vue    # 登录页 (已完整实现)
    │   ├── register/RegisterView.vue  # 注册页 (已完整实现，含60s验证码倒计时)
    │   ├── dashboard/DashboardView.vue # 工作台 (占位)
    │   ├── taskboard/TaskBoardView.vue # 任务看板 (占位)
    │   ├── todolist/TodoListView.vue   # TODO List (占位)
    │   ├── checkin/CheckinView.vue     # 每日打卡 (占位)
    │   ├── messages/MessagesView.vue   # 消息中心 (占位)
    │   ├── profile/ProfileView.vue     # 个人中心 (已含基本信息+主题设置)
    │   └── manage/
    │       ├── TaskManageView.vue      # 任务管理 (占位, captain/coach)
    │       ├── TeamProgressView.vue    # 全队进度 (占位, captain/coach)
    │       ├── UserManageView.vue      # 权限管理 (占位, coach only)
    │       └── MonitorView.vue         # 系统监控 (占位, coach only)
    └── styles/
        ├── variables.css          # CSS 变量：亮色/暗色双主题色彩体系
        └── global.css             # 全局重置、滚动条、过渡动画
```

**核心功能实现：**

1. **登录/注册流程** — 已完整对接后端 GateServer 的 4 个 API 接口：
   - `POST /get_verify_code` — 发送邮箱验证码
   - `POST /user_register` — 用户注册
   - `POST /user_resetpass` — 密码重置
   - `POST /user_login` — 用户登录（获取 Token + PushServer 节点信息）

2. **暗色/亮色主题切换** — 通过 CSS 自定义属性实现全局双主题，切换 `<html>` class 即可无闪烁切换，主题偏好持久化至 localStorage，并跟随系统偏好设置

3. **路由权限守卫** — 三层控制：
   - `guest: true` — 游客页面（登录/注册），已登录用户自动跳转工作台
   - `requiresAuth: true` — 需登录页面，未登录自动跳转登录页
   - `roles: ['captain', 'coach']` — 角色权限控制，无权限用户被重定向

4. **Axios 拦截器** — 请求自动注入 `Authorization: Bearer <token>`，响应 401 自动清除登录态并跳转登录页，网络超时/断连友好提示

5. **主布局（MainLayout）** — 可折叠侧边栏 + 顶栏（面包屑/主题开关/消息铃铛/用户下拉菜单），菜单项根据用户角色动态显示（队员/队长/教练）

### 验证记录

- `npm install` 成功安装 82 个依赖包
- `vite build` 生产构建成功，无编译错误
- `vite --host` 开发服务器在 223ms 启动，HTTP 响应正常
- 多页面路由页面切换正常，主题切换工作正常

### 下一步开发规划

**第二阶段建议功能（按优先级排序）：**

1. **TCP PushServer 连接模块** — 登录成功后通过 TCP 连接 PushServer，实现实时消息接收能力。这是后续所有实时功能的基础：
   - 封装 WebSocket/TCP 连接管理类
   - 实现 TCP 二进制协议解析（4 字节头：2B msg_id + 2B msg_len）
   - 实现 MSG_CHAT_LOGIN (1005) 登录流程
   - 心跳机制保持长连接
   - 将收到的消息写入 Pinia store

2. **任务看板页面** — 对接 TaskServer 的 RPC 接口（需等后端 TaskServer 开发完成后）：
   - 任务卡片列表（进行中/已截止分栏）
   - 按难度、状态、发布时间筛选
   - 任务详情弹窗

3. **消息中心页面** — 基于 PushServer 连接模块：
   - 实时消息列表
   - 消息已读/未读标记
   - 离线消息补发展示
   - 消息通知角标

4. **每日打卡页面** — 提交打卡与打卡状态查看

5. **Electron 容器集成** — 将 Vue 应用嵌入 Electron 窗口，实现真正的桌面客户端

---

## 2026-05-21 | 第二阶段：密码加密、重置密码与注册登录完善

### 开发成果

完成客户端密码加密机制、密码重置页面开发，完善注册/登录流程的安全性。

**参考源：** Qt 客户端 (`Ori-Client/`) 的实现模式：
- 所有密码在发送前经客户端本地加密
- 注册/登录/重置密码均通过邮箱验证码进行身份验证
- 后端 API 接口：`POST /get_verify_code`, `/user_register`, `/user_resetpass`, `/user_login`
- 数据库 `OxyTasks.user` 表字段：`uid`, `name`, `email`, `password`

**1. 密码加密方案 — SHA-256 替换 XOR**

Qt 客户端使用简单 XOR 混淆（`password[i] ^ (len % 255)`），安全强度极低，任何拦截请求的人都能轻易还原原始密码。

Vue 客户端升级为 **SHA-256 加密哈希**：
- 使用浏览器内置 Web Crypto API (`crypto.subtle.digest('SHA-256', ...)` )，无外部依赖
- 密码在离开客户端前被哈希为 64 位十六进制字符串
- 后端直接比较哈希值（对后端的 `WHERE password = ?` 查询模式完全兼容）
- 同一密码总是产生相同的哈希（确定性），因此登录/注册/重置可正常匹配
- **安全提升**：SHA-256 是单向哈希函数，即使 HTTP 请求被拦截，也无法还原原始密码

新增文件：[Client/src/utils/crypto.js](Client/src/utils/crypto.js)

**2. 注册页 — 密码哈希集成**

更新 [Client/src/views/register/RegisterView.vue](Client/src/views/register/RegisterView.vue)：
- 表单提交前调用 `await sha256(form.password)` 和 `await sha256(form.confirm)` 对密码和确认密码分别哈希
- 密码一致性校验仍在原始明文上进行（`validateConfirm` 在哈希之前运行）
- 发送到后端的数据中 `password` 和 `confirm` 字段均包含 SHA-256 哈希值

**3. 登录页 — 密码哈希 + 忘记密码入口**

更新 [Client/src/views/login/LoginView.vue](Client/src/views/login/LoginView.vue)：
- 登录前调用 `await sha256(form.password)` 对密码哈希
- 新增「忘记密码？」链接，导航至 `/reset` 路由
- 底部链接改为：忘记密码？| 还没有账号？立即注册

**4. 密码重置页（全新）— 三步向导流程**

新增 [Client/src/views/reset/ResetPasswordView.vue](Client/src/views/reset/ResetPasswordView.vue)，实现完整的密码重置向导：

- **第一步 - 验证身份**：输入用户名 + 注册邮箱 + 邮箱验证码（60s 倒计时按钮调用 `/get_verify_code`）
- **第二步 - 重置密码**：输入新密码 + 确认密码，提交前经 `sha256()` 哈希，调用 `POST /user_resetpass` 传入 `{ user, email, password, confirm, verify_code }`
- **第三步 - 完成**：显示成功结果，提供「返回登录」按钮

UI 使用 `el-steps` 步骤条展示进度，每步均可返回上一步修改。

**5. 路由与状态管理更新**

- [Client/src/router/index.js](Client/src/router/index.js)：新增 `/reset` 路由，挂载至 `AuthLayout`，标记 `guest: true`
- [Client/src/stores/user.js](Client/src/stores/user.js)：新增 `resetPassword(data)` action，调用 `POST /user_resetpass` API

### 与 Qt 客户端对照

| 功能 | Qt 客户端 (Ori-Client) | Vue 客户端 (Client) |
|---|---|---|
| 密码加密算法 | XOR (`pwd[i] ^ (len%255)`) | SHA-256 (Web Crypto API) |
| 注册页 | 单页表单 | 单页表单 + 60s 验证码倒计时 |
| 登录页 | 邮箱+密码+头像图标 | 邮箱+密码 + 忘记密码/注册链接 |
| 密码重置 | 单页表单（用户名+邮箱+验证码+新密码） | 三步向导（验证身份 → 重置密码 → 完成） |
| 验证码发送 | 10 秒冷却 | 60 秒冷却 |
| API 端点 | 完全相同 | 完全相同 |

### 验证记录

- `vite build` 生产构建成功，1677 模块，零编译错误
- 路由 `/login`, `/register`, `/reset` 均可正常导航
- 密码 SHA-256 哈希在客户端完成，后端接收到的 `password` 字段为 64 位十六进制字符串

### 已知事项

- 由于后端使用 `WHERE password = ?` 直接比较，密码哈希是确定性的（SHA-256 不加盐）。未来后端升级为服务端哈希（bcrypt/scrypt）后，客户端可直接切换为发送明文密码由服务端处理
- Qt 客户端已注册用户的密码使用 XOR 存储，Vue 客户端新注册的用户使用 SHA-256 存储——两者不兼容。如系统已有测试数据，需清空 `user` 表后重新注册

### 下一步开发规划

**第三阶段建议功能：**

1. **TCP PushServer 连接模块（高优先级）** — 登录成功后通过 TCP 连接 PushServer：
   - 实现 TCP 二进制协议解析（4 字节头：2B msg_id + 2B msg_len）
   - 登录后自动连接，发送 `{"uid": ..., "token": ...}` 完成 TCP 认证
   - 心跳保活机制
   - 接收消息写入 Pinia store，供消息中心使用
   - 参考 Qt 客户端的 `TCPManager` 实现

2. **消息中心页面** — 基于 PushServer 连接：
   - 实时消息列表展示
   - 消息已读/未读状态标记
   - 离线消息恢复提示

3. **个人中心完善** — 修改密码功能（已登录用户直接修改密码，无需验证码找回流程）

4. **任务看板页面** — 需等待后端 TaskServer 接口就绪

---

## 2026-05-21 | Debug：修复 CORS 跨域问题与配置读取

### 问题诊断

客户端启动后无法完成注册，HTTP 请求无法到达 GateServer。

**经排查发现两个根因：**

**1. CORS 跨域阻塞（主因）**

- 浏览器运行在 `localhost:5173`（Vite dev server），发出 POST 请求到 `localhost:8080`（GateServer），属于跨源请求
- `Content-Type: application/json` 触发浏览器 CORS 预检（OPTIONS 请求）
- GateServer 的 `HttpConnection::handleRequest_()` 仅处理 GET 和 POST 两个 verb，**OPTIONS 请求被静默丢弃**（无任何响应）
- 浏览器等待预检响应超时，实际 POST 请求永远不会发出
- GateServer 响应中也没有 `Access-Control-Allow-Origin` 等 CORS 头，即使绕过预检也无法被 JavaScript 读取

**请求链路分析：**
```
浏览器 (localhost:5173) ──OPTIONS──> GateServer (localhost:8080) → 被忽略，无响应 → 浏览器报 CORS 错误
浏览器 (localhost:5173) ──POST──X   实际请求从未发出
```

**2. config.json 未被读取**

- 用户将服务器信息写入 `Client/config.json`，但代码 `src/config/index.js` 使用硬编码值
- 两处配置虽然端口一致（8080），但架构上 config.json 应该是单一配置来源

### 修复方案

**1. Vite 开发代理 — 彻底绕过 CORS**

修改 [Client/vite.config.js](Client/vite.config.js)：
- 从 `config.json` 读取 GateServer host/port
- 配置 proxy：`/api/*` → `http://{host}:{port}/*`（路径重写，去掉 `/api` 前缀）
- `changeOrigin: true` 确保 Host 头正确

**请求链路（修复后）：**
```
浏览器 (localhost:5173) ──POST /api/get_verify_code──> Vite Dev Server (同源，无 CORS)
                                                           │
                                                           └── 代理转发 ──> GateServer (localhost:8080)
                                                                            /get_verify_code
```
浏览器到 Vite 是同源请求，不触发 CORS；Vite 到 GateServer 是服务器间通信，同样不受 CORS 限制。

**2. 配置模块读取 config.json**

修改 [Client/src/config/index.js](Client/src/config/index.js)：
- `import serverConfig from '../../config.json'` — Vite 原生支持 JSON 导入
- 开发模式 (`import.meta.env.DEV`) 使用 `/api` 走代理
- 生产模式使用直连 URL（Electron 环境无 CORS 限制）

### 验证记录

```
# GateServer 直接测试 — 正常
$ curl -X POST http://127.0.0.1:8080/get_verify_code \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com"}'
→ {"email":"test@example.com","error":0} ✓

$ curl -X POST http://127.0.0.1:8080/user_register \
  -H "Content-Type: application/json" \
  -d '{"user":"test","email":"t@t.com","password":"abc","confirm":"abc","verify_code":"x"}'
→ {"error":1003} (VERIFY_CODE_EXPIRED, 预期行为 — 验证码不存在) ✓

# Vite 代理测试 — 正常
$ curl -X POST http://localhost:5175/api/get_verify_code \
  -H "Content-Type: application/json" \
  -d '{"email":"proxytest@example.com"}'
→ {"email":"proxytest@example.com","error":0} ✓ (代理转发成功)

- vite build 生产构建成功，1678 模块，零编译错误

### 客户端注册流程（端到端）

完整注册数据流：
1. 用户在注册页填写用户名、邮箱、密码、确认密码
2. 点击「发送验证码」→ `POST /api/get_verify_code {"email":"..."}` → Vite 代理 → GateServer → gRPC 调用 MailerServer → 邮件发送
3. 用户输入收到的验证码，点击「注册」
4. 密码在客户端经 `sha256(password)` 和 `sha256(confirm)` 哈希
5. `POST /api/user_register {"user":"...","email":"...","password":"<sha256>","confirm":"<sha256>","verify_code":"..."}`
6. GateServer 验证 Redis 中的验证码 → 调用 MySQL 存储过程 `reg_user` → 返回 `{"error":0,"uid":...}`
7. 注册成功，跳转登录页

### 下一步开发规划

**第三阶段建议功能：**

1. **TCP PushServer 连接模块（高优先级）** — 登录成功后通过 TCP 连接 PushServer：
   - 实现 TCP 二进制协议解析（4 字节头：2B msg_id + 2B msg_len）
   - 登录后自动连接，发送 `{"uid": ..., "token": ...}` 完成 TCP 认证
   - 心跳保活机制
   - 接收消息写入 Pinia store，供消息中心使用
   - 参考 Qt 客户端的 `TCPManager` 实现

2. **消息中心页面** — 基于 PushServer 连接：
   - 实时消息列表展示
   - 消息已读/未读状态标记
   - 离线消息恢复提示

3. **个人中心完善** — 修改密码功能（已登录用户直接修改密码，无需验证码找回流程）

4. **任务看板页面** — 需等待后端 TaskServer 接口就绪

---

## 2026-05-21 | 第三阶段：gRPC 重命名 + 注册直通 + 账户修复

### 1. gRPC 服务重命名: GetChatServer → GetPushServer

将所有后端服务中的 `GetChatServer` gRPC 服务统一重命名为 `GetPushServer`。

**Proto 文件修改（3 个）：**
- [GateServer/message.proto](GateServer/message.proto) — rpc 定义 + message 类型同步
- [StatusServer/message.proto](StatusServer/message.proto) — 全面重命名
- [PushServer/message.proto](PushServer/message.proto) — 同步重命名

**Protobuf 重新生成：** 三个 Server 及各自 build/ 目录均运行 protoc 重新生成 `.pb.h`, `.pb.cc`, `.grpc.pb.h`, `.grpc.pb.cc`

**C++ 源码重命名（12 文件）：**
- GateServer: LogicSystem.cpp, StatusGrpcClient.h/.cpp, RPCConnectPool.h
- StatusServer: StatusServiceImpl.h/.cpp, StatusGrpcClient.h/.cpp, RPCConnectPool.h
- PushServer: StatusGrpcClient.h/.cpp, RPCConnectPool.h

重命名映射：
```
GetChatServerReq  → GetPushServerReq
GetChatServerRsp  → GetPushServerRsp
getChatServer()   → getPushServer()
GetChatServer()   → GetPushServer()
class ChatServer  → class PushServer
```

### 2. 修复：注册后直接可登录

MySQL 存储过程 `reg_user` 的 `status` 默认值从 `0`（待审核）改为 `1`（正常），新注册用户无需管理员审核即可直接登录。

### 3. 修复：现有账户密码兼容

数据库用户 `OxyTheCrack`（uid=1, role=2 教练）密码从明文 `123456` 更新为 SHA-256 哈希值：
```
8d969eef6ecad3c29a3a629280e686cf0c3f5d5a86aff3ca12020c923adc6c92
```
使 Vue 客户端（发送 SHA-256 哈希密码）可正常匹配登录。

### 数据库现有数据

| uid | username | email | role | status |
|-----|----------|-------|------|--------|
| 1 | OxyTheCrack | gazy0906@outlook.com | 2 (教练) | 1 (正常) |

### 注意事项

- 注册流程只需 GateServer + MailerServer + Redis + MySQL
- 完整登录还需 StatusServer 运行（分配 PushServer 节点）
- 建议启动顺序：MailerServer → StatusServer → GateServer

### 下一步开发规划

1. **TCP PushServer 连接模块** — 登录后通过 TCP 连接 PushServer，接收实时消息
2. **消息中心页面** — 基于 PushServer 连接展示实时/离线消息
3. **启动 StatusServer** — 完成登录全流程联调
4. **个人中心完善** — 已登录用户修改密码

---

## 2026-05-21 | Debug：修复重置密码模块验证码发送失败

### 问题诊断

重置密码页面点击「发送验证码」后提示"验证码已发送"，但实际 MailerServer 未被调用，Redis 无缓存。

**根因有两个：**

**1. GateServer 配置文件节名不匹配（主因）**

`VerifyGrpcClient.cpp` 读取配置：
```cpp
std::string host = config["VerifyServer"]["host"];  // 查找 [VerifyServer] 节
std::string port = config["VerifyServer"]["port"];
```

但 `config.ini` 中实际节名为 `[MailerServer]`。ConfigManager 找不到 `[VerifyServer]` 节，返回空字符串，gRPC 通道创建到 `":50051"`（空 host），连接静默失败。

GateServer 的 `/get_verify_code` handler 仍返回 HTTP 200：
```json
{"error": 1002, "email": "..."}   // RPC_ERROR
```

**修复：** 读取的服务器名从 `[VerifyServer]` 改为 `[MailerServer]`



**2. 客户端未检查响应中的 error 字段（次因）**

`RegisterView.vue` 和 `ResetPasswordView.vue` 的 `sendVerifyCode` 函数直接 `await getVerifyCode(email)` 后显示成功，不检查 `res.error`。即使服务器返回 `RPC_ERROR`，前端也显示"验证码已发送"。

**修复：** 两个页面均添加 `if (res.error !== 0)` 检查，失败时显示错误提示。

**受影响的文件：**
| 文件 | 修改 |
|------|------|
| [Client/src/views/register/RegisterView.vue](Client/src/views/register/RegisterView.vue) | sendVerifyCode 添加 res.error 检查 |
| [Client/src/views/reset/ResetPasswordView.vue](Client/src/views/reset/ResetPasswordView.vue) | sendVerifyCode 添加 res.error 检查 |

### 验证

- `vite build` 成功，1678 模块，零错误
- **重启 GateServer 后**，验证码发送应恢复正常流程：
  1. 客户端 POST `/api/get_verify_code` → Vite 代理 → GateServer
  2. GateServer VerifyGrpcClient 读取 `[MailerServer]` → 正确连接 `127.0.0.1:50051`
  3. gRPC 调用 MailerServer → 生成验证码 → 存入 Redis → 发送邮件
  4. 返回 `{ error: 0 }` → 客户端检查通过 → 显示成功提示

### 注意事项

- GateServer 需重启以加载新的配置节名（无需重新编译）
- 客户端 `vite build` 已通过，`npx vite --host` 即可启动

---

## 2026-05-21 | 第四阶段：角色显示修复 + 所属队伍 + 后端架构规划

### 1. Bug 修复：角色显示错误

**问题：** 用户 role=2（教练），但个人信息页显示灰色「队员」标签。

**根因链：**
1. `MySQLDao::checkLogin()` SQL 查询 `SELECT uid, username` — 未包含 `role`
2. `UserInfo` 结构体无 `role` 字段
3. GateServer 登录响应 JSON 不含 `role`
4. Vue `userStore.login()` 执行 `res.role || 'member'` — 永远回退到 `'member'`

**修复 — GateServer（4 个文件）：**

| 文件 | 修改 |
|------|------|
| [Global.h](GateServer/Global.h) | `UserInfo` 新增 `int role` 和 `int belong_captain_id` |
| [MySQLDao.cpp](GateServer/MySQLDao.cpp) | SQL 改为 `SELECT uid, username, role, belong_captain_id`，读取新字段 |
| [LogicSystem.cpp](GateServer/LogicSystem.cpp) | 登录响应新增 `"role"` 和 `"belong_captain_id"` |
| [MySQLDao.cpp/MySQLManager.cpp](GateServer/MySQLDao.cpp) | 新增 `updateTeamInfo(uid, belong_captain_id)` 方法 |

**修复 — Client（3 个文件）：**

| 文件 | 修改 |
|------|------|
| [user.js](Client/src/stores/user.js) | role 改为数值存储（0/1/2），新增 `roleLabel`、`roleKey`、`belongCaptainId` getters |
| [router/index.js](Client/src/router/index.js) | 路由守卫适配数值 role → 字符串 roleKey 映射 |
| [ProfileView.vue](Client/src/views/profile/ProfileView.vue) | 角色 tag 颜色修正 + 新增所属队伍模块 |

### 2. 角色 Tag 颜色方案

| 角色 | 数值 | Element Plus Type | 颜色 |
|------|------|------------------|------|
| 队员 | 0 | `info` | 灰色 |
| 队长 | 1 | `success` | 绿色 |
| 教练 | 2 | `""` (default/primary) | 蓝色 |

### 3. 新增：所属队伍信息

- 个人信息页新增「所属队伍」行，显示 `belong_captain_id` 或「未分配」
- 教练角色可见「修改」按钮，可编辑队伍归属
- 后端新增 `POST /user_update_team` 端点：`{ uid, belong_captain_id }`

### 4. 客户端功能 → 后端微服务映射

基于当前已实现的客户端功能，以下是各功能应在哪个微服务实现：

#### 通用功能（所有角色）

| 客户端功能 | 目标微服务 | 说明 |
|-----------|-----------|------|
| 登录 `/user_login` | **UMSServer** | 当前临时在 GateServer 实现，UMSServer 就绪后迁移 |
| 注册 `/user_register` | **UMSServer** | 同上，验证码通过 MailerServer 生成 |
| 密码重置 `/user_resetpass` | **UMSServer** | 同上 |
| 邮箱验证码 `/get_verify_code` | **MailerServer** (via GateServer) | 已实现，GateServer 通过 gRPC 转发 |
| 个人中心（查看信息） | **UMSServer** | 查询用户 role、belong_captain_id 等 |
| 修改所属队伍 | **UMSServer** | 教练权限，当前临时在 GateServer |
| 任务看板 | **TaskServer** | 任务列表查询、按状态/难度筛选 |
| TODO List | **TaskServer** | 个人待办 CRUD、优先级、截止时间 |
| 每日打卡 | **TaskServer** | 打卡提交、去重、记录查询 |
| 消息中心（实时+历史） | **PushServer** | TCP 实时推送 + 离线消息缓存 |
| 离线消息恢复 | **PushServer** | 上线后从缓存队列补推 |
| 深色/浅色模式 | 客户端本地 | localStorage，无后端依赖 |

#### 队长专属

| 客户端功能 | 目标微服务 | 说明 |
|-----------|-----------|------|
| 任务管理（发布/编辑/删除） | **TaskServer** | 训练任务 CRUD |
| 全队进度查看 | **TaskServer** | 聚合查询队员打卡和完成率 |
| 向队员推送通知 | **PushServer** | 通过 PushServer 广播/单播消息 |

#### 教练专属

| 客户端功能 | 目标微服务 | 说明 |
|-----------|-----------|------|
| 任务管理 | **TaskServer** | 同队长 |
| 全队进度 | **TaskServer** | 同队长 |
| 权限管理（审核/角色变更） | **UMSServer** | 用户审批、role 修改 |
| 全局监控（在线状态） | **StatusServer** | 队员在线数、PushServer 节点状态 |
| 系统日志查看 | **GateServer** | 操作日志聚合查询 |

### 5. 后端开发优先级建议

**Phase 1 — UMSServer（最高优先级）**
当前用户认证功能临时实现在 GateServer 中，需要迁移到 UMSServer：
- 注册/登录/密码重置（从 GateServer 迁移）
- Token 验证（供其他微服务调用的 RPC）
- 用户信息查询与修改
- 角色/权限管理（教练审核用户、修改角色）
- 所属队伍修改

**Phase 2 — TaskServer（核心业务）**
- 训练任务 CRUD（队长/教练发布、编辑、删除）
- 打卡管理（队员提交、每日去重）
- 进度统计（全队/个人完成率）
- TODO List 管理
- 数据库表设计：`task`、`checkin`、`todo`

**Phase 3 — PushServer（实时通信）**
- 消息路由逻辑（单播/广播/按角色推送）
- 离线消息队列
- 与其他微服务的消息推送接口
- 当前已有 TCP 连接基础设施，需补充业务逻辑层

### 验证记录

- GateServer `cmake + make` 编译成功，零错误
- Client `vite build` 生产构建成功，1678 模块
- role 从 DB → GateServer → JSON → Pinia → UI 全链路打通
- 路由守卫适配数值 role 映射

### 建议启动顺序
```
MailerServer (gRPC :50051) → StatusServer (gRPC :50052) → GateServer (HTTP :8080)
```

---

## 2026-05-21 | 第五阶段：业务逻辑解耦 — GateServer / UMSServer / StatusServer 拆分

### 架构变更

将原先集中在 GateServer 的用户业务逻辑拆分到各微服务，GateServer 变为纯 HTTP → gRPC 网关。

**新架构数据流：**

```
Client ──HTTP──> GateServer ──gRPC──> UMSServer ──gRPC──> MailerServer
                     │                      │
                     │                      ├── MySQL (user表)
                     │                      └── Redis (验证码)
                     │
                     └──gRPC──> StatusServer ── Redis (token)
                                    │
                                    └── 权值线段树 (PushServer负载均衡)
```

**Proto 重新定义（message.proto）：**

| Service | RPC | 实现方 |
|---------|-----|--------|
| `UserService.Register` | 注册 | UMSServer |
| `UserService.Login` | 登录验证 | UMSServer |
| `UserService.ResetPass` | 密码重置 | UMSServer |
| `UserService.GetVerifyCode` | 获取验证码 | UMSServer |
| `MailerService.SendMail` | 发送邮件 | MailerServer |
| `StatusService.AllocatePushServer` | 分配推送节点 | StatusServer |
| `StatusService.ReportLogin` | TCP登录上报 | StatusServer |

### 各服务修改清单

**MailerServer（Node.js）:**
- 服务名：`VerifyService.GetMailCode` → `MailerService.SendMail`
- 职责简化：仅发送邮件，不再生成验证码/操作 Redis

**StatusServer:**
- `GetPushServer` → `AllocatePushServer`
- `Login` → `ReportLogin`
- `StatusGrpcClient` 同步更新

**UMSServer（全新 gRPC 服务，端口 :50053）:**
- 从 GateServer 副本改造而来，移除所有 HTTP 相关文件
- 新增：`UMSGrpcServiceImpl` 实现 4 个 UserService RPC
- 新增：`MailerGrpcClient` 调用 MailerServer 发送邮件
- 复用：MySQL 连接池、Redis 连接池、ConfigManager

**GateServer（纯 HTTP 网关）:**
- 移除 `VerifyGrpcClient`（替换为 `UserGrpcClient`）
- `StatusGrpcClient` 方法名同步更新
- `LogicSystem.cpp` 全部 handler 改为 gRPC 调用
- 保留 MySQLManager（仅用于 `/user_update_team` 和 `getUserInfo`）
- Config.ini 新增 `[UMSServer]` 节

### 构建验证

| 服务 | 状态 |
|------|------|
| GateServer | ✓ 编译成功 |
| UMSServer | ✓ 编译成功 |
| StatusServer | ✓ 编译成功 |
| MailerServer | ✓ 代码就绪 |

### 启动顺序

```
1. MailerServer  (gRPC :50051)
2. UMSServer     (gRPC :50053) — 依赖 MailerServer + Redis + MySQL
3. StatusServer  (gRPC :50052) — 依赖 Redis
4. GateServer    (HTTP :8080)  — 依赖 UMSServer + StatusServer + MySQL
```

### 下一步

- 启动 UMSServer + StatusServer 测试全链路登录/注册
- PushServer 同步适配新 proto（ReportLogin）
- TaskServer 业务逻辑开发
