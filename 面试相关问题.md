# OxyTeamTasks 面试相关问题

> 按面试被问到的可能性从高到低排序，答案基于项目实际实现，口语化风格。

---

## 一、几乎必问（简历上写了就一定会被追）

### 1. 你为什么选择微服务架构，而不是单体？

因为这个项目的服务之间职责差异很大。网关只做协议翻译，用户服务管注册登录，推送服务要维持大量 WebSocket 长连接，它们对资源的需求完全不同——推送服务吃内存和连接数，任务服务吃 CPU 和数据库。拆开之后每个服务可以独立扩缩容，比如推送连接数多了就多加几个 PushServer 实例，不用把整个应用都扩容，而且各服务间代码可以高效复用，也不用每次都编译整个项目。

当然微服务也有代价——部署运维复杂度上来了，服务间通信有开销，调试也比单体麻烦。但对于这个场景，收益是大于成本的。

---

### 2. gRPC 和 RESTful API 有什么区别？为什么选 gRPC？

最大的区别是序列化方式和通信协议。REST 用 JSON + HTTP/1.1，gRPC 用 Protobuf + HTTP/2。Protobuf 是二进制的，体积小、序列化反序列化快，对我们这种频繁的服务间调用来说性能差距很明显。

选 gRPC 主要是因为：第一，我们的服务间通信很密集，一个前端请求经过网关要调两三个下游服务，Protobuf 的性能优势能体现出来；第二，Protobuf 有强类型的 IDL，定义好 `.proto` 文件之后可以直接生成 C++ 和 Node.js 的代码，接口变更时编译就能发现不兼容的问题；第三，gRPC 原生支持流式通信和 deadline，虽然我们目前主要用 unary call（一元调用，一问一答的形式），但扩展性也会好。

缺点是前端不能直接调 gRPC，所以需要一个 HTTP 网关做协议翻译。

---

### 3. 分布式锁怎么实现的？为什么要用 Lua 脚本？

用 Redis 实现的，核心逻辑是 SET key NX（不存在才设置）加 TTL 防死锁，再加上一个唯一标识（UUID）作为 owner，释放锁的时候要先比对 owner 再删除。

为什么要用 Lua 脚本呢，因为"比对 owner + 删除"这个操作必须是原子的。如果你用两条 Redis 命令，先 GET 再 DEL，在高并发下可能出现这种情况：你 GET 完发现是自己的锁，但在你 DEL 之前，锁刚好过期被别人抢走了，你 DEL 的就是别人的锁。Lua 脚本把这三步打包成一个原子操作发给 Redis 执行，就不会有这个问题。

我还加了重试机制 `acquireLockWithRetry`，获取失败会短暂等待后重试几次，避免瞬间竞争导致全部失败。

---

### 4. 连接池是怎么设计的？

项目里有三类连接池：MySQL 连接池、Redis 连接池、gRPC 连接池。设计思路是一样的——启动时预创建一批连接放队列里，用的时候从队列取，用完还回去。

MySQL 连接池我用 RAII 的方式封装了一个 `ConnectionGuard`，构造函数从池里取连接，析构函数自动归还。这样不管函数怎么 return，连接都不会泄漏。gRPC 连接池存的是 Stub 而不是裸连接，原理一样。

队列用 mutex + condition_variable 保护，取的时候如果池空了就等待，归还的时候通知等待的线程。池大小是可配置的，一般设成比 CPU 核数稍大一点。

---

### 5. WebSocket 跨节点消息推送怎么实现的？

这是推送架构的核心问题。PushServer 是一个集群，用户可能连在不同的节点上。当任务服务要给某个用户推消息时，流程是这样的：

第一步，查 Redis 里的 `pushnode:{uid}`，这个 key 记录了用户连接在哪个 PushServer 节点上（host:ws_port:grpc_port）。

第二步，判断目标节点是不是本机。如果是，直接通过本地 Session 对象 WebSocket 推出去。

第三步，如果不是本机，通过 gRPC 调用目标节点的 `PushToUser` 接口，让目标节点推给用户。

第四步，如果用户不在线（没有任何节点有他的 Session），消息就写入 Redis List 缓存（`msgs:{uid}`），用户下次登录时自动拉取。

这样做的好处是 PushServer 可以水平扩展，加一个新节点只需要在 StatusServer 注册一下就行。

---

## 二、高频问题（深入问技术细节）

### 6. 权值线段树做负载均衡是怎么回事？

PushServer 是集群部署的，用户登录时需要分配一个节点给他。最简单的方式是遍历所有节点，找连接数最少的那个，复杂度 O(n)。

我用权值线段树优化了这个过程。每个叶子节点存一个 PushServer 的连接数，线段树维护区间最小值和对应的下标。分配的时候直接查询全局最小值，O(log n) 就能拿到结果。用户断开连接时，在对应叶子节点减一，向上更新，也是 O(log n)。

代码里其实保留了两种实现——"Brute"暴力遍历和"SegmentTree"线段树，通过配置切换。这样可以对比验证正确性，也方便面试时讲清楚区别。

百级集群规模下，log₂(100) ≈ 7，相比遍历 100 个节点，查询次数降了一个数量级。

---

### 7. Redis 在项目中具体用在哪些场景？

用得挺多的，主要有这几类：

**Token 和会话管理**：用户登录后生成 UUID 作为 Token 存到 Redis，TTL 24 小时。下次请求带 Token 过来，我直接 Redis GET 验证，不用每次都查 MySQL。

**分布式锁**：用 Lua 脚本实现的，保护登录踢人、任务状态流转、每日打卡这些并发场景。

**消息缓存**：用户不在线时，消息 LPUSH 到 Redis List 里，最多保留 50 条，上线后自动拉取。未读计数用一个 INCR/DECR 维护的数字。

**节点映射**：`pushnode:{uid}` 记录用户连接在哪个 PushServer 节点上，用于跨节点消息路由。

**踢人标记**：同账号新登录时，StatusServer 设置 `kick:{uid}` 标记旧 Token，PushServer 检测到后踢掉旧连接。

---

### 8. 多端并发下任务状态流转怎么保证一致性？

这是一个典型的分布式并发问题。我们的任务支持多执行人，每个人有独立的状态。如果两个人同时操作同一个任务，可能出现状态覆盖。

我的方案是两层保护：第一层是 Redis 分布式锁，操作前先获取 `lock:task:{task_id}`，同一时刻只有一个请求能进入临界区。第二层是数据库兜底——`task_assignments` 表有 `task_id + assignee_uid` 的唯一联合索引，即使分布式锁失效（比如 Redis 短暂不可用），数据库的唯一约束也能防止重复数据写入。

每日打卡也是同样的思路，`checkins` 表有 `uid + checkin_date` 的唯一索引，配合 Redis 锁保证每人每天只能打一次卡。

---

### 9. Defer 机制是什么？为什么要用它？

Defer 是我模仿 Go 语言的 `defer` 设计的一个 RAII 工具类。它在构造函数里存一个回调函数，在析构函数里执行。因为 C++ 的局部变量在离开作用域时一定会析构，所以不管函数从哪个 return 路径退出，这个回调都会被执行。

我在 gRPC 服务处理函数里大量使用它。比如一个 RPC handler 的最后需要发响应给客户端，如果每个 return 前都写一遍发送逻辑，很容易漏。用 Defer 把发送逻辑包起来，函数开头创建一个 Defer 对象，不管中间怎么 return，最后都会统一发送响应。

```cpp
Defer defer([&resp, session]() {
    session->send(resp.toStyledString());
    if (resp["error"].asInt() != 0) {
        session->close();
    }
});
```

---

### 10. 踢人机制怎么实现的？

用户在 A 设备登录，然后又在 B 设备登录同一个账号，需要把 A 踢下线。流程是这样的：

B 登录时，StatusServer 的 `AllocatePushServer` 先获取 `lock:login:{uid}` 分布式锁。在锁内检查 Redis 是否存在旧 Token，如果有且和新 Token 不同，就写入一个 `kick:{uid}` 标记，值是旧 Token，TTL 60 秒。

B 的 PushServer 收到 WebSocket 登录请求后，用 Lua 脚本原子执行 GET+DEL 检查 `kick:{uid}`。如果拿到了踢人标记，就找到该 uid 的旧 Session（通过 `getSessionByUid`），发送一条 `kicked` 类型的消息，然后关闭连接。

这里为什么要用 Lua 脚本原子操作呢？因为"检查标记 + 删除标记"必须是原子的，否则可能两个请求同时检测到标记，重复踢人。

---

## 三、中频问题（看你简历怎么写的）

### 11. HTTP 网关的设计思路是什么？

GateServer 的核心职责是协议翻译——把前端的 HTTP JSON 请求转成 gRPC Protobuf 调用。前端不直接调 gRPC，因为浏览器不原生支持 Protobuf。

网关做三件事：一是路由分发，根据 URL path（比如 `/task_create`、`/user_login`）找到对应的 gRPC Client 方法；二是鉴权，从请求头取 Token 和 uid，查询验证；三是错误码映射，把 gRPC 的 Status 翻译成前端能理解的 JSON 错误码。

好处是鉴权逻辑集中在网关，下游服务不用重复校验；前端只需要知道一个 HTTP 地址，不用关心后面有几个微服务。

---

### 12. 消息的可靠性怎么保障？会不会丢消息？

我做了几层保障：

第一层，消息先落库再推送。`pushMessageToUser` 先写 MySQL 持久化，再写 Redis 缓存，最后才尝试 WebSocket 推送。即使推送失败，消息已经存下来了。

第二层，离线消息缓存。用户不在线时，消息存到 Redis List（`msgs:{uid}`），用户重新连接后在登录响应里一次性返回缓存消息和未读计数。

第三层，WebSocket 层面的断线重连。前端 `pushClient.js` 有 `onclose` 事件处理，断线后自动尝试重连。

但说实话，目前没有做到 exactly-once 语义。比如网络抖动时，推送成功了但 ACK 丢了，可能会重推。对于通知类消息，at-least-onse 是可以接受的。如果是更严格的场景，需要加消息去重的 message ID 机制。

---

### 13. 心跳检测怎么做的？

PushServer 启动时会开一个独立线程，每隔一段时间（比如 10 秒）向 StatusServer 发一次 gRPC 心跳请求（`ServerHeartbeat`），携带自己的 host、port 和服务名。

StatusServer 收到后记录 `last_heartbeat` 时间戳。查询服务状态时（`QueryServerStatus`），用当前时间和 last_heartbeat 做差，超过阈值（比如 30 秒）就标记为 offline。

这个心跳的作用是让 StatusServer 知道哪些 PushServer 节点还活着。分配节点时不会把用户分到已经挂掉的节点上，且数据也会同步到教练端方便调试

---

### 14. 角色权限控制是怎么做的？

三级角色：队员（0）、队长（1）、教练（2）。

前端通过 Vue Router 的 `beforeEach` 导航守卫做第一层控制。路由配置里有 `meta.roles` 数组，守卫里检查当前用户的角色是否在允许列表里，不在就跳到无权限页面。

后端在 gRPC 服务层做第二层控制。每个 RPC handler 开头从请求里取 uid，查数据库拿到角色，角色不够就直接返回错误码。

两层都要做是因为前端的控制可以被绕过（直接调接口），后端才是真正的权限校验。

---

## 四、低频但可能追问的

### 15. IOContextPool 的线程模型是什么样的？

是主从 Reactor 模型。`MainServer` 的 `acceptor_` 绑定在主 `io_context` 上，专门负责监听和接受新连接（主 Reactor）。同时启动时创建 N 个 `boost::asio::io_context` 放进 `IOContextPool`，每个跑在一个独立线程上（从 Reactor）。新连接 accept 之后，从池里轮询取一个 io_context，把这个连接的 socket 注册到上面。这样主 Reactor 只处理 accept 事件，从 Reactor 各自处理自己管理的连接的 I/O 事件，互不阻塞。、

---

### 16. 单例模式怎么实现的？为什么用 CRTP？

用的 Meyers Singleton（局部静态变量）+ CRTP（Curiously Recurring Template Pattern）。基类模板里有一个 `static T& getInstance()` 方法，返回局部静态变量的引用。C++11 保证局部静态变量的初始化是线程安全的，所以不用手动加锁。

用 CRTP 是因为每个 Manager 类（MySQLManager、RedisManager、ConfigManager 等）都需要单例，但不想在每个类里重复写一样的 getInstance 代码。CRTP 让基类模板能拿到派生类的类型，一份模板代码所有子类复用。

---

### 17. 如果要继续优化这个项目，你会做什么？

几个方向：

一是消息的 exactly-once 投递。现在是 at-least-once，可以加一个基于 message ID 的幂等去重表。

二是服务发现。现在 PushServer 节点信息是写死在 StatusServer 配置里的，可以改成注册中心模式，节点启动时自动注册，下线时自动注销。

三是 Proto 文件管理。现在每个服务目录下都有一份相同的 `message.proto`，改一个要同步改六份。可以抽到公共目录，构建时自动拷贝。

四是可观测性。现在日志是各服务自己写文件再上报到 StatusServer，可以接入 OpenTelemetry 做链路追踪，排查跨服务调用问题更方便。

---

### 18. 你在这个项目中遇到的最大挑战是什么？

一是负载均衡的高效处理，动态寻找最小连接数的在线的Pushs

跨节点消息推送那块。一开始只考虑了单个 PushServer 的情况，后来要支持集群才发现问题：用户 A 连在 PushServer1，用户 B 连在 PushServer2，A 给 B 发消息怎么推过去？

一开始想的是所有 PushServer 之间互相建 gRPC 连接，但这样 N 个节点就要 N×N 条连接，扩展性很差。后来改成通过 StatusServer 做中转——查 `pushnode` 映射拿到目标节点地址，点对点 gRPC 转发。这样连接数是 O(N) 而不是 O(N²)。

另一个坑是踢人逻辑的竞态条件。两个设备同时登录，两个请求同时到达 StatusServer，一开始没有分布式锁，两个都拿到了旧 Token 并设置踢人标记，导致踢人消息发了两次。加上 `lock:login:{uid}` 分布式锁之后才解决。
