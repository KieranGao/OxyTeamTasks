# OxyTeamTasks 面试相关问题

> 按面试被问到的可能性从高到低排序，答案基于项目实际实现，口语化风格。

---

## 一、几乎必问（简历上写了就一定会被追）

### 1. 你为什么选择微服务架构，而不是单体？

因为这个项目的服务之间职责差异很大。网关只做协议翻译，用户服务管注册登录，推送服务要维持大量 WebSocket 长连接，它们对资源的需求完全不同——推送服务吃内存和连接数，任务服务吃 CPU 和数据库。拆开之后每个服务可以独立扩缩容，比如推送连接数多了就多加几个 PushServer 实例，不用把整个应用都扩容，而且各服务间代码可以高效复用，也不用每次都编译整个项目。

当然微服务也有代价——部署运维复杂度上来了，服务间通信有开销，调试也比单体麻烦。但对于这个场景，收益是大于成本的。

---

### 2. gRPC 和 RESTful API 有什么区别？为什么选 gRPC？

最大的区别是序列化方式和通信协议。REST 用 JSON + HTTP/1.1，gRPC 用 Protobuf + HTTP/2。Protobuf 是二进制的，体积小、序列化反序列化快，对我们这种频繁的服务间调用来说性能差距很明显。

选 gRPC 主要是因为：第一，我们的服务间通信很密集，一个前端请求经过网关要调两三个下游服务，Protobuf 的性能优势能体现出来；第二，Protobuf 有强类型的 IDL，定义好 `.proto` 文件之后可以直接生成 C++ 和 Node.js 的代码，接口变更时编译就能发现不兼容的问题；第三，gRPC 原生支持流式通信和 deadline，虽然我们目前主要用 unary call（一元调用，一问一答的形式），但扩展性也会好。

缺点是前端不能直接调 gRPC，所以需要一个 HTTP 网关做协议翻译。

---

### 3. 分布式锁怎么实现的？为什么要用 Lua 脚本？

用 Redis 实现的，核心逻辑是 SET key NX（不存在才设置）加 TTL 防死锁，再加上一个唯一标识（UUID）作为 owner，释放锁的时候要先比对 owner 再删除。

为什么要用 Lua 脚本呢，因为"比对 owner + 删除"这个操作必须是原子的。如果你用两条 Redis 命令，先 GET 再 DEL，在高并发下可能出现这种情况：你 GET 完发现是自己的锁，但在你 DEL 之前，锁刚好过期被别人抢走了，你 DEL 的就是别人的锁。Lua 脚本把这三步打包成一个原子操作发给 Redis 执行，就不会有这个问题。

我还加了重试机制 `acquireLockWithRetry`，获取失败会短暂等待后重试几次，避免瞬间竞争导致全部失败。

---

### 4. 连接池是怎么设计的？

项目里有三类连接池：MySQL 连接池、Redis 连接池、gRPC 连接池。设计思路是一样的——启动时预创建一批连接放队列里，用的时候从队列取，用完还回去。

MySQL 连接池我用 RAII 的方式封装了一个 `ConnectionGuard`，构造函数从池里取连接，析构函数自动归还。这样不管函数怎么 return，连接都不会泄漏。gRPC 连接池存的是 Stub 而不是裸连接，原理一样。

队列用 mutex + condition_variable 保护，取的时候如果池空了就等待，归还的时候通知等待的线程。池大小是可配置的，一般设成比 CPU 核数稍大一点。

---

### 5. WebSocket 跨节点消息推送怎么实现的？

这是推送架构的核心问题。PushServer 是一个集群，用户可能连在不同的节点上。当任务服务要给某个用户推消息时，流程是这样的：

第一步，查 Redis 里的 `pushnode:{uid}`，这个 key 记录了用户连接在哪个 PushServer 节点上（host:ws_port:grpc_port）。

第二步，判断目标节点是不是本机。如果是，直接通过本地 Session 对象 WebSocket 推出去。

第三步，如果不是本机，通过 gRPC 调用目标节点的 `PushToUser` 接口，让目标节点推给用户。

第四步，如果用户不在线（没有任何节点有他的 Session），消息就写入 Redis List 缓存（`msgs:{uid}`），用户下次登录时自动拉取。

这样做的好处是 PushServer 可以水平扩展，加一个新节点只需要在 StatusServer 注册一下就行。

---

## 二、高频问题（深入问技术细节）

### 6. 权值线段树做负载均衡是怎么回事？

PushServer 是集群部署的，用户登录时需要分配一个节点给他。最简单的方式是遍历所有节点，找连接数最少的那个，复杂度 O(n)。

我用权值线段树优化了这个过程。每个叶子节点存一个 PushServer 的连接数，线段树维护区间最小值和对应的下标。分配的时候直接查询全局最小值，O(log n) 就能拿到结果。用户断开连接时，在对应叶子节点减一，向上更新，也是 O(log n)。

代码里其实保留了两种实现——"Brute"暴力遍历和"SegmentTree"线段树，通过配置切换。这样可以对比验证正确性，也方便面试时讲清楚区别。

百级集群规模下，log₂(100) ≈ 7，相比遍历 100 个节点，查询次数降了一个数量级。

---

### 7. Redis 在项目中具体用在哪些场景？

用得挺多的，主要有这几类：

**Token 和会话管理**：用户登录后生成 UUID 作为 Token 存到 Redis，TTL 24 小时。下次请求带 Token 过来，我直接 Redis GET 验证，不用每次都查 MySQL。

**分布式锁**：用 Lua 脚本实现的，保护登录踢人、任务状态流转、每日打卡这些并发场景。

**消息缓存**：用户不在线时，消息 LPUSH 到 Redis List 里，最多保留 50 条，上线后自动拉取。未读计数用一个 INCR/DECR 维护的数字。

**节点映射**：`pushnode:{uid}` 记录用户连接在哪个 PushServer 节点上，用于跨节点消息路由。

**踢人标记**：同账号新登录时，StatusServer 设置 `kick:{uid}` 标记旧 Token，PushServer 检测到后踢掉旧连接。

---

### 8. 多端并发下任务状态流转怎么保证一致性？

这是一个典型的分布式并发问题。我们的任务支持多执行人，每个人有独立的状态。如果两个人同时操作同一个任务，可能出现状态覆盖。

我的方案是两层保护：第一层是 Redis 分布式锁，操作前先获取 `lock:task:{task_id}`，同一时刻只有一个请求能进入临界区。第二层是数据库兜底——`task_assignments` 表有 `task_id + assignee_uid` 的唯一联合索引，即使分布式锁失效（比如 Redis 短暂不可用），数据库的唯一约束也能防止重复数据写入。

每日打卡也是同样的思路，`checkins` 表有 `uid + checkin_date` 的唯一索引，配合 Redis 锁保证每人每天只能打一次卡。

---

### 9. Defer 机制是什么？为什么要用它？

Defer 是我模仿 Go 语言的 `defer` 设计的一个 RAII 工具类。它在构造函数里存一个回调函数，在析构函数里执行。因为 C++ 的局部变量在离开作用域时一定会析构，所以不管函数从哪个 return 路径退出，这个回调都会被执行。

我在 gRPC 服务处理函数里大量使用它。比如一个 RPC handler 的最后需要发响应给客户端，如果每个 return 前都写一遍发送逻辑，很容易漏。用 Defer 把发送逻辑包起来，函数开头创建一个 Defer 对象，不管中间怎么 return，最后都会统一发送响应。

```cpp
Defer defer([&resp, session]() {
    session->send(resp.toStyledString());
    if (resp["error"].asInt() != 0) {
        session->close();
    }
});
```

---

### 10. 踢人机制怎么实现的？

用户在 A 设备登录，然后又在 B 设备登录同一个账号，需要把 A 踢下线。流程是这样的：

B 登录时，StatusServer 的 `AllocatePushServer` 先获取 `lock:login:{uid}` 分布式锁。在锁内检查 Redis 是否存在旧 Token，如果有且和新 Token 不同，就写入一个 `kick:{uid}` 标记，值是旧 Token，TTL 60 秒。

B 的 PushServer 收到 WebSocket 登录请求后，用 Lua 脚本原子执行 GET+DEL 检查 `kick:{uid}`。如果拿到了踢人标记，就找到该 uid 的旧 Session（通过 `getSessionByUid`），发送一条 `kicked` 类型的消息，然后关闭连接。

这里为什么要用 Lua 脚本原子操作呢？因为"检查标记 + 删除标记"必须是原子的，否则可能两个请求同时检测到标记，重复踢人。

---

## 三、中频问题（看你简历怎么写的）

### 11. HTTP 网关的设计思路是什么？

GateServer 的核心职责是协议翻译——把前端的 HTTP JSON 请求转成 gRPC Protobuf 调用。前端不直接调 gRPC，因为浏览器不原生支持 Protobuf。

网关做三件事：一是路由分发，根据 URL path（比如 `/task_create`、`/user_login`）找到对应的 gRPC Client 方法；二是鉴权，从请求头取 Token 和 uid，查询验证；三是错误码映射，把 gRPC 的 Status 翻译成前端能理解的 JSON 错误码。

好处是鉴权逻辑集中在网关，下游服务不用重复校验；前端只需要知道一个 HTTP 地址，不用关心后面有几个微服务。

---

### 12. 消息的可靠性怎么保障？会不会丢消息？

我做了几层保障：

第一层，消息先落库再推送。`pushMessageToUser` 先写 MySQL 持久化，再写 Redis 缓存，最后才尝试 WebSocket 推送。即使推送失败，消息已经存下来了。

第二层，离线消息缓存。用户不在线时，消息存到 Redis List（`msgs:{uid}`），用户重新连接后在登录响应里一次性返回缓存消息和未读计数。

第三层，WebSocket 层面的断线重连。前端 `pushClient.js` 有 `onclose` 事件处理，断线后自动尝试重连。

但说实话，目前没有做到 exactly-once 语义。比如网络抖动时，推送成功了但 ACK 丢了，可能会重推。对于通知类消息，at-least-onse 是可以接受的。如果是更严格的场景，需要加消息去重的 message ID 机制。

---

### 13. 心跳检测怎么做的？

PushServer 启动时会开一个独立线程，每隔一段时间（比如 10 秒）向 StatusServer 发一次 gRPC 心跳请求（`ServerHeartbeat`），携带自己的 host、port 和服务名。

StatusServer 收到后记录 `last_heartbeat` 时间戳。查询服务状态时（`QueryServerStatus`），用当前时间和 last_heartbeat 做差，超过阈值（比如 30 秒）就标记为 offline。

这个心跳的作用是让 StatusServer 知道哪些 PushServer 节点还活着。分配节点时不会把用户分到已经挂掉的节点上，且数据也会同步到教练端方便调试

---

### 14. 角色权限控制是怎么做的？

三级角色：队员（0）、队长（1）、教练（2）。

前端通过 Vue Router 的 `beforeEach` 导航守卫做第一层控制。路由配置里有 `meta.roles` 数组，守卫里检查当前用户的角色是否在允许列表里，不在就跳到无权限页面。

后端在 gRPC 服务层做第二层控制。每个 RPC handler 开头从请求里取 uid，查数据库拿到角色，角色不够就直接返回错误码。

两层都要做是因为前端的控制可以被绕过（直接调接口），后端才是真正的权限校验。

---

## 四、低频但可能追问的

### 15. IOContextPool 的线程模型是什么样的？

是主从 Reactor 模型。`MainServer` 的 `acceptor_` 绑定在主 `io_context` 上，专门负责监听和接受新连接（主 Reactor）。同时启动时创建 N 个 `boost::asio::io_context` 放进 `IOContextPool`，每个跑在一个独立线程上（从 Reactor）。新连接 accept 之后，从池里轮询取一个 io_context，把这个连接的 socket 注册到上面。这样主 Reactor 只处理 accept 事件，从 Reactor 各自处理自己管理的连接的 I/O 事件，互不阻塞。、

---

### 16. 单例模式怎么实现的？为什么用 CRTP？

用的 Meyers Singleton（局部静态变量）+ CRTP（Curiously Recurring Template Pattern）。基类模板里有一个 `static T& getInstance()` 方法，返回局部静态变量的引用。C++11 保证局部静态变量的初始化是线程安全的，所以不用手动加锁。

用 CRTP 是因为每个 Manager 类（MySQLManager、RedisManager、ConfigManager 等）都需要单例，但不想在每个类里重复写一样的 getInstance 代码。CRTP 让基类模板能拿到派生类的类型，一份模板代码所有子类复用。

---

### 17. 如果要继续优化这个项目，你会做什么？

几个方向：

一是消息的 exactly-once 投递。现在是 at-least-once，可以加一个基于 message ID 的幂等去重表。

二是服务发现。现在 PushServer 节点信息是写死在 StatusServer 配置里的，可以改成注册中心模式，节点启动时自动注册，下线时自动注销。

三是 Proto 文件管理。现在每个服务目录下都有一份相同的 `message.proto`，改一个要同步改六份。可以抽到公共目录，构建时自动拷贝。

四是可观测性。现在日志是各服务自己写文件再上报到 StatusServer，可以接入 OpenTelemetry 做链路追踪，排查跨服务调用问题更方便。

---

### 18. 你在这个项目中遇到的最大挑战是什么？

一是负载均衡的高效处理，动态寻找最小连接数的在线的Pushs

跨节点消息推送那块。一开始只考虑了单个 PushServer 的情况，后来要支持集群才发现问题：用户 A 连在 PushServer1，用户 B 连在 PushServer2，A 给 B 发消息怎么推过去？

一开始想的是所有 PushServer 之间互相建 gRPC 连接，但这样 N 个节点就要 N×N 条连接，扩展性很差。后来改成通过 StatusServer 做中转——查 `pushnode` 映射拿到目标节点地址，点对点 gRPC 转发。这样连接数是 O(N) 而不是 O(N²)。

另一个坑是踢人逻辑的竞态条件。两个设备同时登录，两个请求同时到达 StatusServer，一开始没有分布式锁，两个都拿到了旧 Token 并设置踢人标记，导致踢人消息发了两次。加上 `lock:login:{uid}` 分布式锁之后才解决。

---

### 19. 项目中延时任务是怎么实现的？

用的是最朴素的方案——独立线程 + 轮询。TaskServer 启动时开一个 `std::thread`，每 60 秒醒一次，检查当前小时数：

- 早上 8 点：查数据库找今天截止日期到期且未完成的任务，给每个执行人推一条 `task_remind` 提醒
- 晚上 8 点：查数据库找今天还没打卡的活跃用户，每人推一条 `checkin_remind` 提醒

用一个 `std::set<std::string> triggered_dates` 做去重，key 是日期加类型（比如 `"2026-06-06:deadline"`），保证每天每种提醒只触发一次。

选择这个方案而不是定时器或消息队列，是因为需求很简单——只需要固定时间点的全局提醒，不需要任意时间的单用户延时任务。轮询方案零依赖、好调试、不容易出 bug。代价是最多有 60 秒的延迟误差，但对提醒场景来说完全可以接受。

如果未来需要支持用户自定义提醒时间（比如"3 小时后提醒我"），现在的方案就不够了。可以改成 Redis Sorted Set，score 是触发时间戳，后台线程每秒 `ZRANGEBYSCORE` 取到期任务执行。这样延时精度到秒级，也不需要轮询数据库。

---

### 20. GateServer 的连接超时是怎么做的？

每个 HTTP 连接有一个 `boost::asio::steady_timer`，默认 60 秒。连接建立时启动 `async_wait`，如果定时器先到期就强制关闭 socket（说明客户端太久没发完请求）。如果响应先写完就 `cancel()` 取消定时器。这是 Boost.Beast 的标准超时模式，防止慢连接占用资源。

---

### 21. 为什么引入 Kafka？

原来的日志上报是各服务器通过 gRPC 同步调 StatusServer 的 `ReportLog`，问题有三个：

一是 StatusServer 挂了或网络抖动，日志直接丢失，没有缓冲。二是各服务器要等 StatusServer 响应，虽然 Logger 回调是 fire-and-forget 语义，但 gRPC 调用本身有建立连接和序列化的开销。三是扩展性差——以后想接 监控告警，得在 StatusServer 里加逻辑，耦合度高。

引入 Kafka 后，各服务器作为 producer 把日志写入 Kafka topic，StatusServer 作为 consumer 拉取。Kafka 自带持久化缓冲，StatusServer 短暂不可用时日志不丢。多消费者天然支持——以后加个 consumer group 直接接，不用改生产端。

---

### 22. Kafka 在这个项目中承担什么角色？

日志上报的主通道。各服务器的 Logger 每 5 秒 flush 一次，回调函数把 batch 序列化成 JSON 写入 Kafka topic `logs`。StatusServer 跑一个 consumer 线程拉取消息，解析后写 Redis。

gRPC `ReportLog` 保留为兜底通道——proto 定义和 handler 都没删，但生产端不再主动调用。两个通道共用同一个 `storeLogEntries()` 函数，逻辑不重复。

---

### 23. 介绍一下 Kafka 的核心概念？

Kafka 是分布式消息队列，本质是持久化的发布-订阅系统。

核心模型：Producer 写消息到 Topic，Topic 按 Partition 分片，Consumer Group 内的消费者分摊 partition。一条消息只被组内一个消费者处理，不同组独立消费。

关键特性：消息持久化到磁盘，可按时间配置保留策略；partition 内消息有序；consumer 通过 offset 管理消费位置，支持回放；吞吐量极高，百万级/秒。

---

### 24. 项目中分区策略是怎么选的？

按 service name 做 key，同一服务的日志进同一 partition。比如 PushServer 的日志全在 partition 0，GateServer 的全在 partition 1。

好处是同一服务的日志在 partition 内严格有序，排查问题时按时间顺序看就行。坏处是如果某个服务日志量特别大，对应的 partition 会成为热点。但对日志场景来说，各服务日志量差不多，这个方案够用。

---

### 25. Consumer Group 是怎么用的？

StatusServer 用独立的 consumer group `status-server-log-group`。组内只有一个消费者，独占所有 partition。以后如果要接 ELK，新建一个 group `elk-log-group`，两个组独立消费同一批消息，互不影响。

这就是 Kafka 的发布-订阅语义——不同 consumer group 各自维护 offset，一条消息可以被多个组消费，但组内只被一个消费者处理。

---

### 26. Kafka Producer 的实现细节？

用 librdkafka 的 C API 封装成单例 `KafkaProducer`。`produce()` 方法调 `rd_kafka_produce()`，`RD_KAFKA_MSG_F_COPY` 标志让 rdkafka 内部复制消息，调用方不用管 buffer 生命周期。`produceLogBatch()` 把 Logger 的 `vector<LogEntry>` 手动拼成 JSON 字符串（不用 jsoncpp，避免给不需要 JSON 的服务器加依赖）。

析构时 `rd_kafka_flush(rk_, 5000)` 等最多 5 秒发完剩余消息，保证不丢。

---

### 27. Kafka Consumer 的实现细节？

`KafkaConsumer` 封装了 librdkafka consumer，独立线程跑 `consumeLoop()`。每次 `rd_kafka_consumer_poll(rk_, 1000)` 拉一条消息，1 秒超时。收到消息后反序列化 JSON，调 `storeLogEntries()` 写 Redis。

offset 自动提交（`enable.auto.commit=true`，5 秒间隔）。StatusServer 重启后从上次 offset 继续消费，不丢消息。

错误处理：poll 超时跳过；消息处理异常 catch 后 `LOG_ERROR` 继续消费，不阻塞。

---

### 28. Kafka 和 gRPC 做日志上报的对比？

| 维度 | gRPC | Kafka |
|---|---|---|
| 可靠性 | StatusServer 挂了日志丢失 | Kafka 持久化缓冲，不丢 |
| 耦合度 | 生产端直接依赖 StatusServer | 生产端只依赖 Kafka broker |
| 扩展性 | 加消费者要改 StatusServer | 加 consumer group 就行 |
| 延迟 | 同步调用，网络延迟 | 异步写入，毫秒级 |
| 复杂度 | 零额外依赖 | 需要部署 Kafka |

本项目选 Kafka 做主通道、gRPC 做兜底，兼顾可靠性和兼容性。

---

### 29. 项目中各服务进程是怎么同步的？

不是进程级同步（信号量、共享内存），而是服务级协调，核心是 StatusServer 作为注册中心。

启动顺序同步：`start_all.sh` 按固定顺序启动——StatusServer 先起，其他服务依次启动，每个间隔 `sleep 1`。StatusServer 必须先启动，因为其他服务启动后要向它注册心跳。

心跳保活：每个服务启动一个心跳线程，每 10 秒向 StatusServer 发一次 `ServerHeartbeat` gRPC 调用，带上 host:port。StatusServer 记录时间戳，查询时如果某服务超过 30 秒没心跳就标记为 offline。

PushServer 注册：PushServer 启动时发 `ReportLogin` 注册到 StatusServer 的负载均衡表，下线时发 `ReportDisconnect` 注销。StatusServer 始终维护可用 PushServer 列表。

分布式锁：登录踢人、在线状态等跨服务共享操作，用 Redis `SET NX EX` + Lua 脚本做原子锁，防止竞态条件。

总结：StatusServer 做注册中心和健康检查，Redis 做分布式锁，gRPC 做同步通信，Kafka 做异步解耦。没有用 ZooKeeper 或 etcd 这类专门的协调服务，用现有组件组合实现够用的协调能力。

---

### 30. 项目中缓存和数据库的一致性是怎么保证的？

项目对不同数据类型用了不同的策略，不是一刀切。

第一类是 Redis-only，不做持久化。登录 Token（`utoken:<uid>`，24 小时 TTL）、踢人标记（`kick:<uid>`，60 秒 TTL）、验证码（`code:<email>`，3 分钟 TTL）、PushServer 路由表（`pushnode:<uid>`，24 小时 TTL）。这些数据本身就是临时的，丢了最多让用户重新登录或重新分配节点，不会造成数据不一致。

第二类是 Write-through，先写 MySQL 再写 Redis。消息是典型例子——`pushMessageToUser()` 先 `INSERT INTO messages`，再 `LPUSH` 到 Redis 缓存（最近 50 条，7 天 TTL）。未读计数 `unread:<uid>` 存在 Redis 里做快速增减，但消息本身的持久化在 MySQL。标记已读时也是双写——MySQL `UPDATE is_read=1` 加 Redis `DECRBY`，两边都用分布式锁保护。

第三类是 MySQL-only，不用 Redis 缓存。任务、待办、打卡、用户信息、角色权限这些数据直接读写 MySQL，不做缓存层。原因是这些数据的读写频率不算高，MySQL 连接池扛得住，加缓存反而引入一致性问题。

已知的不一致点有两个：一是删除消息只删 MySQL，Redis 缓存靠 TTL 自然过期，最多有 7 天的脏数据窗口。二是注册通知教练时 `INCR unread` 没加分布式锁，其他消息操作都加了锁。

总结：临时数据放 Redis 靠 TTL 兜底，核心业务数据走 Write-through 双写加锁保护，低频数据直接走 MySQL 不缓存。没有用 Canal、binlog 这类中间件做最终一致性，因为业务规模不需要那么复杂的方案。

---

## 通用后端面试题（2026-06-08 模拟面试）

### 31. TCP 三次握手的过程是怎样的？为什么不是两次或者四次？

三次握手：
1. 客户端发 SYN（seq=x）→ 服务端
2. 服务端回 SYN+ACK（seq=y, ack=x+1）→ 客户端
3. 客户端发 ACK（seq=x+1, ack=y+1）→ 服务端，连接建立

为什么不是两次：防止**历史失效的 SYN 报文**被服务端误接受。客户端发了一个 SYN 但网络延迟很久才到服务端，服务端回 SYN+ACK 后以为连接建好了，但客户端根本没发过这个请求。三次握手让客户端在第三步拒绝失效请求。

为什么不是四次：三次已经能双向确认对方的收发能力，四次是冗余的。

加分项：半连接队列（SYN Queue）、SYN Flood 攻击及防御（SYN Cookie）。

---

### 32. MySQL 索引底层用的是什么数据结构？为什么？

InnoDB 用 **B+ 树**。

为什么是 B+ 树：
1. 非叶子节点只存 key 不存数据 → 单页放更多 key → 树高更低 → 磁盘 IO 更少（两千万数据约 3 层）
2. 所有数据都在叶子节点 → 查询效率稳定（O(log N)）
3. 叶子节点有双向链表 → 支持范围查询和排序

为什么不用其他结构：
- Hash 表：等值查询 O(1) 但不支持范围查询，Memory 引擎用
- 二叉搜索树：树高 O(log N)，磁盘 IO 太多
- B 树：非叶子节点也存数据，每个页能放的 key 更少，树高更高

加分项：聚簇索引 vs 非聚簇索引、回表查询。

---

### 33. Redis 常用的数据类型有哪些？分别适合什么场景？

| 类型 | 底层实现 | 典型场景 |
|------|---------|---------|
| String | SDS | 缓存、计数器、分布式锁 |
| Hash | listpack / hashtable | 存对象（比 String 存 JSON 节省空间） |
| List | quicklist | 消息队列、最新动态 |
| Set | listpack / hashtable | 去重、集合运算（共同好友） |
| Sorted Set | listpack / skiplist + hashtable | 排行榜、延时队列 |

加分项：Stream（消息队列增强版）、Bitmap（签到统计）、HyperLogLog（UV 统计）。

---

### 34. 线程和进程有什么区别？什么情况下用多线程，什么情况下用多进程？

| 维度 | 进程 | 线程 |
|------|------|------|
| 本质 | 资源分配单位 | 调度单位 |
| 地址空间 | 独立 | 共享进程地址空间 |
| 共享资源 | 不共享 | 堆、全局变量、FD |
| 独立资源 | 全部 | 栈、PC、寄存器 |
| 切换开销 | 大（刷页表） | 小 |

- CPU 密集型 → 多进程（真并行）
- IO 密集型 → 多线程（等待时不阻塞）

加分项：协程（用户态调度）、GIL。

---

### 35. Redis 分布式锁的实现原理是什么？为什么要用 Lua 脚本释放锁？

加锁：`SET key owner_uuid NX EX 30`（互斥 + 过期 + owner 标识）

释放锁（Lua 脚本）：`GET key == owner_uuid → DEL`，原子性验证 + 删除。

为什么用 Lua：GET 和 DEL 之间如果没有原子性，可能误删别人的锁（锁过期后别人的锁被自己删了）。

为什么 Lua 能原子执行：Redis 单线程，Lua 脚本执行期间不会被其他命令打断。

加分项：锁续期（Watchdog）、Redlock（多节点方案）、可重入锁。

---

### 36. HTTP 的 GET 和 POST 有什么区别？

| 维度 | GET | POST |
|------|-----|------|
| 语义 | 获取资源（幂等） | 提交数据（非幂等） |
| 参数位置 | URL query string | Request body |
| URL 长度 | 有限制（2-8KB） | 无限制 |
| 编码 | URL 编码 | URL / multipart（文件上传） |
| 缓存 | 可缓存 | 默认不缓存 |
| 日志暴露 | 参数在 URL 中 | body 不在日志中 |

协议层面：报文格式完全一样，方法名只是语义约定。

---

### 37. 连接池的作用是什么？连接池大小怎么设置？

作用：
1. 连接复用（减少创建开销）
2. 限流（保护下游服务）
3. 健康检查（剔除失效连接）
4. 预热（避免冷启动延迟）

大小设置：
- Little's Law：连接数 = QPS × 平均持有时间
- 经验公式：CPU 核心数 × 2 + 磁盘数（HikariCP 推荐）
- 太小：线程等待，吞吐量下降；太大：占用下游连接上限

---

### 38. MySQL 的事务隔离级别有哪些？InnoDB 默认用的是哪个？

四个级别：

| 隔离级别 | 脏读 | 不可重复读 | 幻读 |
|---------|------|-----------|------|
| RU | ✅ | ✅ | ✅ |
| RC | ❌ | ✅ | ✅ |
| RR | ❌ | ❌ | ⚠️ 基本解决 |
| Serializable | ❌ | ❌ | ❌ |

InnoDB 默认 RR。通过 **MVCC（undo log 版本链 + Read View）+ 间隙锁（Next-Key Lock）** 基本解决幻读。

RC 和 RR 的 MVCC 区别：RC 每次读创建新 Read View，RR 只在第一次 SELECT 时创建。

加分项：当前读（SELECT FOR UPDATE）走锁机制而非 MVCC。

---

### 39. 如何设计一个简单的消息队列？

核心组件：
1. 存储：append-only log 写磁盘（顺序写入性能高）
2. 并发：互斥锁 + 条件变量（或无锁队列）
3. 分类：topic + partition，分区内有序

消费语义：pull 模型 + offset 记录 + ACK 确认

可靠性：持久化 + ACK + 重试 + 死信队列 + 消息 TTL

性能：批量读写、零拷贝、页缓存

加分项：消费者组、rebalance、消息回溯。

---

### 40. 后端日常实习面试高频考点速查

| 领域 | 高频考点 |
|------|---------|
| 网络 | TCP 三次握手/四次挥手、HTTP vs HTTPS、GET vs POST、Cookie vs Session |
| 数据库 | MySQL 索引（B+ 树）、事务隔离级别、MVCC、锁机制、SQL 优化 |
| 缓存 | Redis 数据类型、缓存穿透/击穿/雪崩、分布式锁、持久化 |
| 操作系统 | 进程 vs 线程、协程、死锁、内存管理、零拷贝 |
| 并发 | 线程同步、锁（互斥锁/读写锁/自旋锁）、CAS、线程池 |
| 系统设计 | 连接池、消息队列、负载均衡、限流、熔断 |
| 语言基础 | C++（RAII/智能指针/STL）、Go（goroutine/channel/interface） |

---

## 通用后端面试题·第二轮（2026-06-08 模拟面试）

### 41. C++ 智能指针有哪几种？RAII 是什么？

三种智能指针，核心思想是 **RAII（资源获取即初始化）**——将资源生命周期绑定到对象生命周期。

- **unique_ptr**：独占所有权，零开销，不可拷贝只可移动。**默认首选**
- **shared_ptr**：共享所有权，引用计数（控制块），有原子操作开销
- **weak_ptr**：不增加引用计数，解决循环引用，lock() 提升为 shared_ptr

优先用 make_unique/make_shared（异常安全）。默认用 unique_ptr，需要共享时才用 shared_ptr。

加分：自定义删除器、shared_ptr 线程安全（引用计数原子但对象本身不安全）。

---

### 42. select、poll、epoll 的区别？

| 维度 | select | poll | epoll |
|------|--------|------|-------|
| 数据结构 | fd_set 位图 | 链表 | 红黑树 + 就绪链表 |
| 最大连接数 | 1024 | 无限制 | 无限制 |
| 就绪通知 | O(N) 遍历 | O(N) 遍历 | 回调机制，只返回就绪 fd |

epoll 性能好的根本原因：epoll_wait 只返回就绪 fd，不需要遍历全部。

LT（水平触发，默认）：持续通知。ET（边缘触发）：只通知一次，需非阻塞 IO。

加分：EPOLLONESHOT、epoll + 线程池的 Reactor 模型。

---

### 43. CAS 是什么？和互斥锁的区别？

CAS（Compare-And-Swap）：CPU 原子指令，乐观锁（先操作，最后检查）。

互斥锁：悲观锁（先加锁，再操作），涉及内核态切换。

选择标准：操作简单 + 竞争低 → CAS；操作复杂 + 竞争高 → 互斥锁。

CAS 问题：ABA（值 A→B→A 检测不到，用版本号解决）、自旋消耗 CPU、只能操作单个变量。

---

### 44. 栈和堆的区别？

| 维度 | 栈 | 堆 |
|------|-----|-----|
| 管理 | 系统自动 | 手动 new/delete |
| 速度 | 极快（移动指针） | 慢（查找空闲块） |
| 大小 | 8MB | 虚拟内存 |
| 碎片 | 无 | 有 |

堆分配：brk（≤128KB）和 mmap（>128KB）。

内存泄漏：new 了没 delete，指针丢失。悬垂指针：delete 后指针未置 nullptr。野指针：未初始化指针。

排查：Valgrind、AddressSanitizer（-fsanitize=address）。

---

### 45. Go goroutine 和线程的区别？channel 怎么实现的？

goroutine：用户态协程，初始栈 2KB，可动态增长，百万级并发。

GMP 调度模型：G（goroutine）在 P（逻辑处理器）本地队列执行，M（OS 线程）绑定 P，work stealing 负载均衡。

channel：hchan 结构体，mutex + 环形队列（ringbuf）+ recvq/sendq 等待队列。所有操作都加锁。

无缓冲 channel：直接 handoff，发送方阻塞到接收方就绪。有缓冲 channel：满时发送阻塞，空时接收阻塞。

加分：select 多路复用、channel 方向、goroutine 泄漏。

---

### 46. 限流算法有哪些？令牌桶和漏桶的区别？

| 算法 | 原理 | 突发流量 |
|------|------|---------|
| 令牌桶 | 固定速率放令牌，请求取令牌 | ✅ 允许 |
| 漏桶 | 固定速率处理，不管输入速度 | ❌ 不允许 |
| 固定窗口 | 固定时间窗口计数 | - |
| 滑动窗口 | 滑动时间窗口计数 | - |

关键区别：令牌桶允许突发（桶里攒的令牌一次用完），漏桶输出恒定。

分布式限流：Redis INCR + EXPIRE 或 Lua 脚本。

---

### 47. 缓存穿透、击穿、雪崩分别是什么？

| 问题 | 定义 | 解决 |
|------|------|------|
| 穿透 | 查不存在的 key | 缓存空值 + 布隆过滤器 |
| 击穿 | 热点 key 过期 | singleflight + 逻辑过期 |
| 雪崩 | 大量 key 同时过期 | 随机 TTL + 多级缓存 + 熔断 |

布隆过滤器：多个哈希到位图，0 则一定不存在，有误判率，不支持删除。

---

### 48. 慢 SQL 怎么排查和优化？

四步法：
1. 发现：慢查询日志（`slow_query_log=ON`）
2. 分析：EXPLAIN 看 type（ALL→const）、key、rows、Extra
3. 优化：索引（最左前缀、覆盖索引、索引下推）、SQL（避免 SELECT *、小表驱动大表）
4. 验证：再 EXPLAIN 对比

索引失效：函数、隐式转换、OR、LIKE '%xx'、!= / NOT IN。

---

### 49. 分布式全局唯一 ID 怎么生成？

雪花算法（64 位）：1 符号 + 41 时间戳 + 10 机器 ID + 12 序列号。

优点：趋势递增（索引友好）、高性能、不依赖外部。

时钟回拨：短回拨等待，长回拨熔断 + 告警。

其他方案：UUID（无序）、数据库自增（单点瓶颈）、Redis INCR（依赖 Redis）。

加分：美团 Leaf、BIGINT 存储、NTP 时钟同步。

---

### 50. 第二轮面试高频考点速查

| 领域 | 高频考点 |
|------|---------|
| C++ 进阶 | 智能指针、RAII、移动语义、模板、虚函数表 |
| Go 进阶 | GMP 模型、channel、context、interface、defer |
| IO 模型 | select/poll/epoll、LT/ET、Reactor/Proactor |
| 并发进阶 | CAS、ABA 问题、无锁数据结构、线程池 |
| 内存管理 | 栈 vs 堆、内存泄漏、智能指针、内存对齐 |
| 缓存进阶 | 穿透/击穿/雪崩、布隆过滤器、singleflight、多级缓存 |
| SQL 进阶 | EXPLAIN、索引优化、慢查询排查、分库分表 |
| 系统设计 | 限流（令牌桶/漏桶）、负载均衡、分布式 ID、消息队列 |
