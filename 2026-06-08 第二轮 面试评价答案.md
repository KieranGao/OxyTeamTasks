# 第二轮模拟面试标准答案

**面试时间：** 2026-06-08 21:00
**候选人：** 高梓源
**目标岗位：** 后端开发实习生
**面试轮次：** 第二轮（技术基础 + 项目深挖）
**面试题数：** 10 题

---

## Q1. C++ 智能指针有哪几种？分别解决什么问题？

**标准答案：**

C++ 有三种智能指针，核心思想是 **RAII（资源获取即初始化）**——将资源生命周期绑定到对象生命周期，构造函数获取资源，析构函数释放资源，利用栈对象自动析构的特性保证资源不泄漏。

**unique_ptr（独占指针）：**
- 独占所有权，不可拷贝，只可移动
- 零开销（和裸指针一样大），**默认首选**
- `auto p = std::make_unique<T>(args);`

**shared_ptr（共享指针）：**
- 共享所有权，内部有控制块（引用计数 + 弱引用计数）
- 引用计数归零时自动释放对象
- 有原子操作开销（多线程安全），比 unique_ptr 慢
- `auto p = std::make_shared<T>(args);`

**weak_ptr（弱引用指针）：**
- 不增加引用计数，解决 shared_ptr 的**循环引用**问题
- 不能直接访问对象，需要 `lock()` 提升为 shared_ptr 后才能使用
- 典型场景：观察者模式、缓存

**优先级：** 默认用 unique_ptr，需要共享所有权时才用 shared_ptr。优先用 make_unique/make_shared（异常安全、减少内存分配次数）。

**加分项：**
- 自定义删除器：`shared_ptr<FILE>(fopen(...), fclose)` 管理非内存资源
- shared_ptr 线程安全：引用计数是原子的，但对象本身不是线程安全的

---

## Q2. select、poll、epoll 的区别？epoll 为什么性能更好？

**标准答案：**

| 维度 | select | poll | epoll |
|------|--------|------|-------|
| 数据结构 | fd_set 位图 | 链表 | 红黑树 + 就绪链表 |
| 最大连接数 | FD_SETSIZE=1024 | 无限制 | 无限制 |
| fd 传递 | 每次调用都拷贝到内核态 | 每次拷贝 | epoll_ctl 时拷贝一次 |
| 就绪通知 | 遍历全部 fd，O(N) | 遍历全部 fd，O(N) | 回调机制，只返回就绪 fd |
| 触发模式 | LT | LT | LT + ET |

**epoll 三个核心 API：**
1. `epoll_create`：创建 epoll 实例
2. `epoll_ctl`：增删改监听的 fd（红黑树操作，O(log N)）
3. `epoll_wait`：等待事件，只返回就绪的 fd（就绪链表，O(1)）

**epoll 性能好的根本原因：**
- select/poll 每次调用都要把整个 fd 集合从用户态拷贝到内核态，然后遍历检查哪些就绪
- epoll 只在 epoll_ctl 时拷贝一次，之后通过回调直接把就绪 fd 放到链表，epoll_wait 只取这个链表

**LT vs ET：**
- **LT（水平触发，默认）**：只要 fd 可读/可写就持续通知，编程简单但可能重复通知
- **ET（边缘触发）**：只在状态变化时通知一次，必须一次读完（配合非阻塞 IO），性能更好但编程复杂

**加分项：** epoll 惊群问题（EPOLLONESHOT）、epoll + 线程池的 Reactor 模型

---

## Q3. CAS 是什么？和互斥锁有什么区别？

**标准答案：**

**CAS（Compare-And-Swap）：**
- 一条 **CPU 原子指令**（x86 的 `CMPXCHG`），硬件层面保证原子性
- 三个操作数：内存地址 V、期望值 A、新值 B。如果 V == A，则把 V 改成 B，否则不做任何操作
- **乐观锁**：先操作，最后检查是否被其他线程干扰，不符合预期就重试

**互斥锁：**
- **悲观锁**：先加锁，再操作，操作完解锁
- 涉及内核态切换（用户态 → 内核态 → 用户态），开销比 CAS 大

**选择标准：**
- **操作简单（单变量读写）+ 竞争低** → CAS（无锁，性能好）
- **操作复杂（多变量、临界区大）+ 竞争高** → 互斥锁（CAS 重试开销大）

**CAS 的问题：**
1. **ABA 问题**：值从 A 改成 B 再改回 A，CAS 检测不到变化。解决：版本号（`AtomicStampedReference`）
2. **自旋消耗 CPU**：重试时一直在循环，临界区太长时浪费严重
3. **只能操作单个变量**：不能保护一段代码

**加分项：** CAS 在项目中的应用（原子计数器、无锁队列、ConcurrentHashMap 的分段锁）

---

## Q4. 栈和堆的区别？内存泄漏怎么排查？

**标准答案：**

| 维度 | 栈（Stack） | 堆（Heap） |
|------|-----------|-----------|
| 管理方式 | 系统自动管理（函数返回自动释放） | 程序员手动管理（new/delete） |
| 分配速度 | 极快（移动栈指针，一条指令） | 慢（查找空闲块，可能触发系统调用） |
| 大小限制 | 有限（通常 8MB，递归太深会栈溢出） | 受限于虚拟内存 |
| 碎片问题 | 无（连续分配） | 有（分配释放不规则导致碎片） |
| 存放内容 | 局部变量、函数参数、返回地址 | 动态分配的对象 |

**堆分配底层：**
- `brk`：≤128KB，移动堆顶指针
- `mmap`：>128KB，直接映射物理页（不连续，产生碎片）

**三种指针问题：**
- **内存泄漏**：new 了对象但指针丢失（指针被覆盖、函数返回但没 delete），内存永远无法回收
- **悬垂指针**：对象已 delete，但指针还指向那块内存，访问是未定义行为
- **野指针**：未初始化的指针，指向随机地址

**预防：** RAII + 智能指针（unique_ptr / shared_ptr）

**排查工具：**
- **Valgrind**：全面检测内存泄漏、越界访问，但性能开销大
- **AddressSanitizer（ASan）**：编译时加 `-fsanitize=address`，性能开销比 Valgrind 小很多

---

## Q5. Go goroutine 和线程的区别？channel 怎么实现的？

**标准答案：**

**goroutine vs 线程：**

| 维度 | goroutine | 线程 |
|------|-----------|------|
| 调度方式 | 用户态（Go 调度器） | 内核态（OS 调度器） |
| 栈大小 | 初始 2KB，可动态增长到 GB | 通常 8MB |
| 创建成本 | 几 KB（用户态分配） | 系统调用，开销大 |
| 切换成本 | 只切换栈帧和寄存器（几纳秒） | 切换页表 + TLB（几十纳秒） |
| 并发数量 | 百万级 | 千级 |

**GMP 调度模型：**
- **G（Goroutine）**：协程
- **M（Machine）**：OS 线程
- **P（Processor）**：逻辑处理器（数量默认等于 CPU 核数）
- **调度策略**：P 绑定 M，G 在 P 的本地队列执行。本地队列空了，从全局队列或其他 P 偷（**work stealing**）

**channel 实现：**
- 底层结构 `hchan`：**mutex + 环形队列（ringbuf）+ recvq/sendq 等待队列**
- **所有操作都需要加锁**（hchan 里的 mutex）
- **无缓冲 channel**：直接 handoff，发送方阻塞到接收方就绪
- **有缓冲 channel**：缓冲区满时发送阻塞，空时接收阻塞

**加分项：** select 多路复用、channel 方向（`chan<-` 只写 / `<-chan` 只读）、goroutine 泄漏（忘记关闭 channel 导致 goroutine 永远阻塞）

---

## Q6. 线段树负载均衡 + 限流器设计

**标准答案：**

**线段树负载均衡：**
- **需求**：最小连接数优先策略，需要动态修改连接数 + 动态获取最小值及其下标
- **为什么选线段树**：
  - 暴力遍历 O(N) → 线段树 O(log N)
  - 单点修改 O(log N)：更新某个服务器的连接数
  - 区间查询 O(log N)：获取全局最小连接数
  - 节点挂载服务器索引：push up 过程中记录最小值对应的服务器
- **优化效果**：百级节点下比暴力遍历提升 90%+

**限流算法对比：**

| 算法 | 原理 | 是否允许突发 | 适用场景 |
|------|------|------------|---------|
| **令牌桶** | 固定速率放令牌，桶有最大容量，请求取令牌 | ✅ 允许（攒的令牌一次用完） | 通用限流，最常用 |
| **漏桶** | 请求进队列，固定速率处理 | ❌ 不允许（输出恒定） | 流量整形 |
| **固定窗口** | 固定时间窗口内计数 | ❌ | 简单限流 |
| **滑动窗口** | 滑动时间窗口内计数 | ❌ | 精确限流 |

**分布式限流：** 多实例部署时，用 Redis INCR + EXPIRE 或 Lua 脚本实现全局限流

**加分项：** 限流与熔断的区别（限流控制流量，熔断保护下游）、Guava RateLimiter（令牌桶实现）

---

## Q7. 缓存穿透、击穿、雪崩分别是什么？怎么解决？

**标准答案：**

| 问题 | 定义 | 解决方案 |
|------|------|---------|
| **穿透** | 查询缓存和 DB 都不存在的 key（恶意攻击 / bug） | 缓存空值（短 TTL）+ 布隆过滤器 |
| **击穿** | 单一热点 key 过期，并发请求同时打到 DB | 互斥锁 / singleflight + 逻辑过期（永不过期 + 后台刷新） |
| **雪崩** | 大量 key 同时过期，DB 被打爆 | 随机 TTL + 多级缓存 + 缓存预热 + 熔断降级 |

**布隆过滤器：**
- 原理：多个哈希函数映射到位图，某位为 0 则**一定不存在**，为 1 则**可能存在**
- 优点：空间效率极高，查询 O(k)
- 缺点：**有误判率**（假阳性），**不支持删除**

**singleflight（Go）：**
- 当缓存失效时，只让**一个请求**去查 DB 并回填缓存，其他请求等待并共享结果
- Go 标准库 `sync/singleflight` 包

**加分项：** 本地缓存（bigcache/freecache）、缓存与 DB 双写一致性、热点 key 探测

---

## Q8. 慢 SQL 排查与优化

**标准答案：**

**排查四步法：**

**第一步：发现**
- 开启慢查询日志：`slow_query_log=ON`、`long_query_time=1`（超过 1 秒记录）
- 或通过监控系统发现慢查询

**第二步：分析（EXPLAIN）**
- **type**：访问类型，从差到好：ALL（全表扫描）→ index → range → ref → eq_ref → const
- **key**：实际使用的索引（NULL 表示没用索引）
- **rows**：预估扫描行数（越小越好）
- **Extra**：Using filesort（文件排序）、Using temporary（临时表）都是性能杀手

**第三步：优化**
- **索引优化**：最左前缀匹配、覆盖索引（避免回表）、索引下推
- **SQL 改写**：避免 SELECT *、小表驱动大表、用 EXISTS 替代 IN
- **表结构**：选择合适的字段类型、适当冗余（反范式）减少 JOIN

**第四步：验证**
- 优化后再 EXPLAIN，对比 rows 和 type 是否改善

**索引失效场景：**
- 索引列上使用函数（`WHERE YEAR(date) = 2026`）
- 隐式类型转换（`WHERE varchar_col = 123`）
- OR 条件中有非索引列
- LIKE '%xx'（前缀通配符）
- != / NOT IN / IS NOT NULL（部分场景）

**加分项：** pt-query-disk（慢查询分析工具）、分库分表、读写分离

---

## Q9. 跨节点消息推送怎么实现？

**标准答案：**

**消息路由四步：**

1. **注册**：用户连接 WebSocket → PushServer 写 `pushnode:{uid} = host:port` 到 Redis（TTL 24h）
2. **路由**：发消息时查 `pushnode:{uid}` 获取目标用户所在节点
3. **推送**：
   - 同节点：直接从本地 Session 池取 session，WebSocket 发送
   - 不同节点：通过 gRPC 转发到目标节点，目标节点本地发送（**最多一次跳转**）
4. **离线消息**：缓存到 Redis List（LPUSH + LTRIM 50 + EXPIRE 7d），上线时拉取补推

**可靠性保障：**
- 消息先落 MySQL 再推 Redis（持久化优先）
- 推送失败时消息已在 Redis 缓存，不会丢
- 分布式锁（`lock:unread:{uid}`）保护未读计数的并发修改

**连接断开清理：**
- 用户断开 WebSocket 时，删除 `pushnode:{uid}`
- 否则消息会转发到已经没有用户的节点

**加分项：** 消息去重（message ID）、推送 ACK 机制、消息顺序性（Redis List 保证有序）

---

## Q10. 分布式全局唯一 ID 生成方案

**标准答案：**

**雪花算法（Snowflake）— 64 位结构：**

```
| 1 bit  |    41 bit     |  10 bit   |  12 bit  |
| 符号位 |   时间戳      |  机器 ID  |  序列号  |
| 固定 0 | 毫秒级，约69年 | 1024节点  | 4096/ms  |
```

**优点：**
- 趋势递增：对 B+ 树索引友好（顺序插入，无页分裂）
- 高性能：纯内存计算，每秒可生成数百万 ID
- 不依赖外部服务（对比数据库自增、Redis INCR）

**缺点：**
- 依赖时钟：时钟回拨可能生成重复 ID
- 机器 ID 需要分配管理（10 bit = 1024 个节点）

**时钟回拨处理：**
- 短回拨（几毫秒）：等待到正确时间
- 长回拨（秒级）：熔断 + 告警，通知人工处理

**其他方案对比：**

| 方案 | 优点 | 缺点 |
|------|------|------|
| UUID | 无网络依赖，简单 | 无序，索引不友好，128 位太长 |
| 数据库自增 | 简单，有序 | 单点瓶颈，可用号段模式优化（美团 Leaf） |
| Redis INCR | 简单，有序 | 依赖 Redis 可用性 |
| 雪花算法 | 趋势递增，高性能 | 依赖时钟，机器 ID 管理 |

**加分项：** 美团 Leaf（号段模式 + 雪花模式）、BIGINT 存储（18 位数字）、NTP 时钟同步

---

## 第二轮面试总结

| 题目 | 得分 | 关键提升点 |
|---|---|---|
| Q1 C++ 智能指针 | ⭐⭐⭐⭐ | RAII 解释纠正、make_unique/make_shared |
| Q2 select/poll/epoll | ⭐⭐⭐ | epoll 核心优势讲透（只返回就绪 fd）、LT/ET |
| Q3 CAS vs mutex | ⭐⭐⭐ | ABA 问题、CPU 原子指令、选择标准 |
| Q4 栈和堆 | ⭐⭐⭐ | 区分内存泄漏和悬垂指针、ASan 工具 |
| Q5 Go goroutine/channel | ⭐⭐⭐ | GMP 调度模型、channel 环形队列 |
| Q6 线段树 + 限流器 | ⭐⭐⭐⭐ | 令牌桶 vs 漏桶区别、分布式限流 |
| Q7 缓存穿透/击穿/雪崩 | ⭐⭐⭐⭐ | 补 singleflight、多级缓存、布隆过滤器局限 |
| Q8 慢 SQL 优化 | ⭐⭐⭐ | EXPLAIN 关键字段、索引失效场景 |
| Q9 跨节点消息推送 | ⭐⭐⭐⭐ | 结构化表达、连接断开清理、消息可靠性 |
| Q10 分布式 ID 生成 | ⭐⭐⭐⭐ | 雪花算法 64 位结构、其他方案对比 |

**重点补强方向：**
1. GMP 调度模型的完整描述（Q5）
2. 雪花算法 64 位结构（Q10）
3. EXPLAIN 关键字段含义（Q8）
4. 表达结构化——用"第一、第二、第三"组织答案，减少"然后、嗯、呃"
