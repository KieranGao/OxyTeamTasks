# 八股面经积累

## C/C++ 

【★★★✩✩】**C++的内存布局**

C++ 程序运行时的内存空间可分为 **6 大核心分区**（从高地址到低地址排序）：

> 代码段（Code Segment/Text Segment）→ 只读数据段（RO Data）→ 已初始化数据段（Data Segment）→ 未初始化数据段（BSS Segment）→ 堆（Heap）→ 栈（Stack）
>
> 还包含内核空间（用户不可访问）、内存映射区（mmap，如动态库加载），但面试核心考察前 6 个分区。
>
> 注意：除了堆区生命周期需要程序员手动维护，其余的所有内存区的生命周期都是程序开始时申请，结束后自动释放

**代码段（Code Segment/Text Segment**）：存储程序编译后的二进制机器指令（函数体、类成员函数、全局函数的执行代码）。

- 权限：**只读 + 可执行**（防止指令被篡改，执行时 CPU 读取指令）；
- 内存共享：**多个进程运行同一程序时，代码段可共享**（节省内存）；

- 面试高频考点
  - 内联函数（`inline`）：代码直接嵌入调用处，减少函数调用开销，但会增加代码段体积；
  - 常量表达式函数（`constexpr`）：编译期确定结果，代码段无冗余指令。

**[文本段]只读数据段（RO Data，常量区）**：存储程序中的只读常量，是数据段的 “只读子集”。

- 存储内容：字符串常量（如`"hello world"`）、`const`修饰的全局变量（`const int g_val = 10;`）、`constexpr`常量；
- 权限：只读，写入会触发 “段错误（Segment Fault）”；

- 面试高频考点
  - 易错点：`char* p = "hello"; p[0] = 'H';`（修改只读常量，程序崩溃）；
  - **局部`const`变量：`const int a = 5;`（局部）存储在栈上（非 RO Data）**

**已初始化数据段（Data Segment，数据区）**：存储已初始化的、非只读的全局变量和静态变量。

- 存储内容：全局变量（`int g_val = 10;`）、静态变量（`static int s_val = 20;`，包括**全局静态和局部静态**）；
- 权限：可读可写；
- 磁盘占用：编译时会将初始化值写入可执行文件，占用磁盘空间；
- 面试高频考点
  - 局部静态变量：`void func() { static int a = 1; }`，`a`存储在 Data 段，仅第一次调用初始化，生命周期至程序结束。

**未初始化数据段（BSS Segment，零值区）**：存储未初始化（或初始化为 0）的全局变量和静态变量，又称 “零初始化段”。

- 存储内容：全局变量（`int g_val;`）、静态变量（`static int s_val;`）；

- 权限：可读可写；

- 磁盘占用：编译时仅记录变量大小，**不占用磁盘空间**（程序运行时由操作系统初始化为 0）；

- 面试高频考点

  （区分 Data 段的关键）：

  - `int a;`（全局）→ BSS 段；`int a = 0;`（全局）→ 编译器优化后也放入 BSS 段；`int a = 1;`（全局）→ Data 段；
  - BSS 段优势：**减少可执行文件体积，运行时统一置 0，效率更高。**

**堆（Heap，动态内存区**）：程序员手动管理的动态内存区域，是 C++ 中动态分配内存的核心区域。

- 分配 / 释放：通过`new/delete`（C++）、`malloc/free`（C）手动操作，需程序员控制生命周期；
- 空间特性：大小受系统虚拟内存限制（远大于栈，GB 级），内存地址从低到高生长，非连续分布；
- 效率：分配 / 释放需查找空闲内存块，速度慢于栈，频繁操作易产生内存碎片。

- 面试高频考点
  - 内存泄漏：堆内存未释放（如循环中`new`对象未`delete`），导致程序运行中内存持续占用；
  - 野指针：释放堆内存后未置空，后续访问导致未定义行为；
  - 智能指针：`unique_ptr/shared_ptr`通过 RAII 机制自动管理堆内存，解决泄漏和野指针问题；
  - `new` vs `malloc`：`new`调用构造函数，`delete`调用析构函数；`malloc`仅分配内存，不涉及对象生命周期。

**栈（Stack，栈区）**：编译器自动管理的临时内存区域，用于存储函数执行上下文。

- 分配 / 释放：函数调用时自动为局部变量、函数参数、返回值分配空间，函数执行结束后自动释放；
- 空间特性：大小固定（通常 1-8MB），内存地址从高到低生长，连续分布；
- 效率：仅通过移动栈指针完成分配 / 释放，速度极快。

- 面试高频考点
  - 栈溢出：递归过深、定义超大局部数组（`int arr[1024*1024*10];`）触发`Stack Overflow`；
  - 易错点：返回局部变量的指针 / 引用（`int* func() { int a = 5; return &a; }`），栈空间释放后指针变为野指针。

C++ 内存分为哪些分区：

> “C++ 程序运行时的内存主要分为 6 个核心分区，从高地址到低地址依次是：代码段、只读数据段、已初始化数据段、未初始化数据段、堆、栈，核心区别如下：
>
> 1. 代码段：存储编译后的机器指令，只读可执行，多进程可共享；
> 2. 只读数据段：存储字符串常量、const 全局变量，只读不可写，修改会崩溃；
> 3. 已初始化数据段：存储已初始化的全局 / 静态变量，可读可写，占用磁盘空间；
> 4. 未初始化数据段（BSS）：存储未初始化的全局 / 静态变量，运行时置 0，不占磁盘空间；
> 5. 堆：手动 new/delete 管理，空间大、效率低，易出现内存泄漏，需用智能指针优化；
> 6. 栈：编译器自动管理，存储局部变量 / 函数参数，空间小、效率高，递归过深会栈溢出。

**追问 1：堆和栈的核心区别？**

> “核心区别有 4 点：
>
> 
>
> 1. 管理方式：栈由编译器自动管理，堆需程序员手动管理；
>
> 2. 空间大小：栈小（MB 级），堆大（GB 级）；
>
> 3. 效率：栈分配快（仅移动指针），堆分配慢（查找空闲块）；
>
> 4. 内存碎片：栈无碎片，堆频繁分配释放会产生碎片。”
>
>    | 内存区域 | 外部碎片 | 内部碎片 |                 核心原因                  |
>    | :------: | :------: | :------: | :---------------------------------------: |
>    |    堆    | 大量产生 | 必然产生 |      分配 / 释放随机，按对齐规则分配      |
>    |    栈    | 完全没有 | 几乎没有 | 先进后出的顺序分配 / 释放，栈帧一次性回收 |

**追问 2：static 变量存在哪个分区？**

> “全局 static 和局部 static 均存储在数据段（已初始化→Data 段，未初始化→BSS 段），生命周期至程序结束，局部 static 仅第一次调用初始化。”

**追问 3：const 变量一定存在只读数据段吗？**

> “不一定：全局 const 变量存在只读数据段；局部 const 变量存储在栈上，仅编译期限制修改，可通过const_cast强制修改。”

【★★✩✩✩】**C++的内存对齐规则**

【★★★✩✩】**C++程序编译到运行经历的步骤**
C++程序从编译到运行主要分为5个核心步骤：

- **预处理**：处理#include、#define等指令，展开头文件、替换宏，生成.i文件；
- **编译**：将预处理后的代码转换为汇编语言，完成语法/语义分析，生成.s文件；
- **汇编**：将汇编代码转换为机器码，生成.o目标文件；
- **链接**：将目标文件与库文件合并，解决符号引用，生成可执行文件；
- **运行**：操作系统加载可执行文件，分配内存，从main()开始执行指令。

其中编译和链接是核心，编译负责将高级语言转汇编，链接负责合并多个文件和库，最终生成可运行的二进制文件。”

**延伸问题**

**问**：预处理和编译的区别？
**答**：预处理是文本层面的替换（无语法检查），编译是语法/语义分析并转换为汇编代码（核心逻辑校验）。
**问**：静态链接和动态链接的优缺点？
**答**：静态链接可独立运行，无需依赖库，但体积大、更新库需重新编译；动态链接体积小、库更新无需重编译，但运行时需依赖库文件，可能出现“库缺失”错误。
**问**：“undefined reference to xxx”错误出现在哪个阶段？
**答**：链接阶段，原因是符号（函数/变量）在目标文件中未找到定义（如未链接对应库、函数声明未实现）。

## Golang 

【★★★★★】**GMP 调度模型？**

MP 是 Go 语言 runtime 层面实现的**用户态、M:N 协程调度模型**，是 Go 高并发的核心底层支撑。目的是解决操作系统线程调度开销大、内核态切换重、并发上限低的问题，通过用户态精细化调度，实现轻量级 G 协程的高效复用。

GMP调度模型 面试完整版标准答案（可直接背诵）

GMP 是 Go 语言 runtime 层面实现的**用户态、M:N 协程调度模型**，是 Go 高并发的核心底层支撑。目的是解决操作系统线程调度开销大、内核态切换重、并发上限低的问题，通过用户态精细化调度，实现轻量级 G 协程的高效复用。

**一、GMP 三大核心实体定义**

1. **G（Goroutine，协程）**

Go 语言的最小执行单元，是用户态轻量级任务。

- **结构**：包含执行栈、程序计数器、状态、任务函数、所属 M/P 等信息。
- **状态**：就绪、运行、阻塞、死亡等。

2. **M（Machine，内核线程）**

绑定操作系统内核线程，是真正占用 CPU 时间片的执行载体。

- **特点**：M 不直接执行 G，必须绑定 P 才能运行 G。
- **职责**：负责抢占 CPU、执行 G 的任务、处理系统调用。
- **数量**：不固定，由 runtime 按需扩容，最大不超过 10000。

3. **P（Processor，逻辑处理器）**

GMP 调度的核心中间层，**负责调度逻辑、持有 G 队列（默认大小256，无锁）、限制并发数量**。

- **数量固定**：默认等于 CPU 核心数（GOMAXPROCS），保证 CPU 利用率最大化，避免线程频繁切换。
- **核心资源**：每个 P 拥有一个**本地运行队列**（优先本地消费，减少锁竞争），同时全局共享一个**全局 G 队列（有锁）**。
- **绑定关系**：同一时刻一个 P 只能绑定一个 M，一个 M 同一时刻只跑一个 G。

**二、GMP 整体调度关系（M:N 调度）**

传统 Linux 是 1:1 调度（一个线程对应一个内核线程），Go 是 **M:N 调度**：将成千上万个 G，调度到有限的 M 上执行，通过 P 做资源隔离与任务分配。

核心绑定公式：**P + M = 可执行 CPU 资源，驱动 G 运行**

简单理解：**G 是任务，P 是调度器，M 是干活的工人**。

**三、完整调度执行流程**

1. 程序启动后，runtime 根据 CPU 核心数初始化对应数量的 P，同时初始化初始 M。
2. 用户创建 G 协程后，优先放入**当前 P 的本地就绪队列**，**本地队列满则放入全局队列。每个 M 自带一个 G0 调度栈，负责执行调度逻辑，切换不同的 G**。
3. M 绑定 P 后，优先从**P 本地队列取 G 执行**，**无锁操作，效率极高。**
4. 本地队列空时，会从**全局队列取任务**，**全局队列需要加锁。**
5. **如果全局队列也空**，会触发**工作窃取机制**：从其他繁忙 P 的本地队列**偷一半任务执行（有锁）**。
6. **G 时间片用完、主动挂起、发生阻塞后，P 会解绑当前 G，调度下一个就绪 G 继续执行，最大化利用 CPU。**
7. G 唤醒后**会优先复用旧 P**，是 Go 调度的**缓存亲和性优化**，目的是最大化利用 CPU 高速缓存，在不破坏负载均衡的前提下提升调度吞吐。

**四、GMP 四大核心调度机制**

1. **工作窃取机制（核心亮点）**

空闲的 P 会主动从繁忙 P 的本地队列窃取一半 G 任务，平衡全局负载。优势：充分利用多核 CPU，避免部分 P 空闲、部分 P 过载，实现负载均衡。

2. **抢占式调度（Go 1.14+）**

早期 Go 为协作式调度，只有 G 主动让出 CPU 才会切换；Go1.14 实现**异步抢占**：

- 每个 G 最大占用 CPU 时间片为 10ms；
- 超时后 runtime 强制抢占，暂停当前 G，调度新 G 执行；
- 彻底解决“单个死循环 G 卡死整个 P，其他协程饿死”的问题。

3. **系统调用阻塞处理（GMP 关键容错）**

当 M 执行 G 触发阻塞型系统调用（读写文件、网络阻塞）时：

- M 和 G 一起进入阻塞状态，**P 会立即解绑**；
- **runtime 唤醒一个空闲 M 或新建 M，接管该 P 继续调度其他 G；**
- 系统调用返回后，原 M 重新尝试抢占 P，无 P 则将 G 放入就绪队列，M 休眠。

核心价值：**阻塞不卡 CPU，彻底避免线程阻塞导致的 CPU 浪费**。

4. 队列分层（本地队列+全局队列）

- 本地队列：无锁、速度快，存放当前 P 常规任务，容量有限；
- 全局队列：加锁、兜底存储，存放溢出任务、特殊任务（定时器、GC 任务）。

五、GMP 相比传统线程调度的核心优势

1. **并发能力极强**：G 极轻量，支持百万级并发，远超操作系统线程上限；
2. **切换开销极低**：用户态调度，无需陷入内核，无系统调用开销；
3. **CPU 利用率高**：工作窃取、阻塞解绑机制，避免 CPU 空转与浪费；
4. **多核适配优秀**：P 数量绑定 CPU 核心，减少上下文切换与缓存失效。





![image-20260610005426620](C:\Users\a2556\AppData\Roaming\Typora\typora-user-images\image-20260610005426620.png)

【★★★★✩】**Channel的底层？**

![image-20260610005540930](C:\Users\a2556\AppData\Roaming\Typora\typora-user-images\image-20260610005540930.png)

底层hchan的定义：

<img src="https://cdn.xiaolincoding.com//picgo/13-Channel%E9%9D%A2%E8%AF%95%E9%A2%98-image-cartoon-be1d1b96.png" alt="img" style="zoom: 33%;" />

【★★★★✩】**向Channel发送数据和读取的过程？**

<img src="C:\Users\a2556\AppData\Roaming\Typora\typora-user-images\image-20260610005700558.png" alt="image-20260610005700558" style="zoom: 80%;" />

<img src="C:\Users\a2556\AppData\Roaming\Typora\typora-user-images\image-20260610005814813.png" alt="image-20260610005814813" style="zoom:80%;" />

<img src="C:\Users\a2556\AppData\Roaming\Typora\typora-user-images\image-20260610005852807.png" alt="image-20260610005852807" style="zoom:80%;" />



## 操作系统

【★★★★✩】**虚拟内存和物理内存？**

**物理内存**

就是计算机真实的内存条（RAM），硬件层面的存储空间，所有程序最终数据都要加载到物理内存才能被 CPU 执行，空间有限。

**虚拟内存**

操作系统为**每个进程**独立分配的**逻辑地址空间**，是一套抽象的地址模型，**并非真实硬件内存**。

Linux/Windows 都会通过 MMU（内存管理单元）+ 页表，完成**虚拟地址 → 物理地址**的映射。

**虚拟内存设计目的**

1. **隔离进程地址空间**

   每个进程都认为自己独占整块内存，**进程之间互不干扰。一个进程崩溃不会篡改其他进程内存，提升系统稳定性。**

2. **解决内存空间不足**

   物理内存不够时，OS 会把暂时不用的内存数据换出到**（非内存）磁盘交换分区（Swap）**，需要时再换回物理内存，实现小物理内存跑大程序。

3. **内存权限控制**

   虚拟内存按页划分，可对页设置读 / 写 / 执行权限，**实现内存保护，防止越权访问。**

4. **简化内存管理**

   进程只需操作统一的虚拟地址，不用关心物理内存实际分布；OS 统一调度物理内存，支持内存复用与共享库加载。同时借助分页机制，让连续虚拟地址映射离散物理页，**有效缓解内存碎片问题**。

**物理内存作用**

作为 CPU 直接寻址的运行载体，速度远快于磁盘，负责存放当前活跃的代码、数据、栈、堆。

**核心映射机制**

1. 进程运行时使用**虚拟地址**，CPU 不识别虚拟地址；

2. 硬件 **MMU** 结合**页表**，将虚拟地址翻译成物理地址；

3. 翻译失败触发

   缺页异常：

   - 数据在磁盘 Swap / 可执行文件中 → OS 加载数据到物理内存，更新页表，重新执行指令；
   - 非法地址访问 → 触发段错误（Segment Fault），进程终止。

4. 什么是 Swap 交换分区？

磁盘上划分的一块空间，作为**物理内存的临时扩容**。物理内存紧张时，OS 将长时间未访问的内存页换入 Swap；访问时再换回物理内存。**频繁 Swap 会严重拖慢系统性能**。

2. 虚拟内存和进程堆 / 栈的关系？

进程的栈、堆、代码段、全局变量，**全部都在虚拟地址空间中划分**，由 OS 统一映射到物理内存。

3. 为什么 64 位系统还需要虚拟内存？

不只是扩容，核心是**进程隔离、内存保护、地址统一管理**，这是多进程操作系统必备能力，和位数无关。

**总结：**

物理内存是硬件内存条，是程序实际运行的载体；虚拟内存是操作系统给每个进程抽象出的独立逻辑地址空间，通过页表和 MMU 完成地址映射。它主要用来隔离进程、弥补物理内存不足、做内存权限保护，当内存页不在物理内存时会触发缺页中断，数据在内存与磁盘 Swap 之间交换。



【★★★★★】**进程间通信方式IPC有哪些？**

进程是操作系统资源分配的基本单位，每个进程拥有独立的虚拟地址空间，进程间无法直接访问彼此的内存数据。进程间通信（IPC，InterProcess Communication）是操作系统提供的核心机制：内核在系统空间开辟专用缓冲区，允许进程将数据从用户空间拷贝到内核缓冲区，再由其他进程从内核缓冲区读取，以此实现进程间的数据交换、同步或通知。

**管道/匿名管道(pipe)**

匿名管道是Linux最基础的IPC方式，本质是内核维护的**单向字节流缓冲区**，仅适用于**有亲缘关系的进程**（父子/兄弟进程）通信。

管道的核心特性：

- 半双工通信：数据仅能沿一个方向流动，双向通信需创建两个独立管道；
- 内存级文件：管道不属于文件系统，仅存在于内核内存中，随进程退出自动释放；
- FIFO规则：数据按“先进先出”顺序读写，读取后数据从缓冲区删除，且无法随机定位（如lseek操作）；
- 阻塞特性：读端无数据时阻塞，写端缓冲区满时阻塞；若写端关闭，读端读取到EOF后返回；若读端关闭，写端会触发SIGPIPE信号并退出。

管道的局限：

- 仅支持亲缘进程通信，无跨进程访问能力；
- 无消息边界，需自定义数据格式（如固定长度、分隔符）；
- 缓冲区大小固定（通常几KB），无法传输大量数据。



**有名管道(FIFO)**

有名管道（FIFO）是匿名管道的扩展，核心区别是**通过文件系统中的特殊文件标识**，因此支持**任意无亲缘关系的进程**通信。

有名管道的核心特性：

- 文件标识：FIFO文件存储在文件系统中（可通过`ls -l`查看，类型为p），但数据仍存于内核内存；
- 跨进程访问：任意进程只要有权限访问FIFO文件路径，即可通过该管道通信；
- 兼容FIFO规则：与匿名管道一致，数据按先进先出读写，不支持随机定位；
- 阻塞/非阻塞模式：
  - 默认阻塞：打开FIFO时，读端等待写端打开，写端等待读端打开；
  - 非阻塞模式：通过`O_NONBLOCK`标志打开，无对应进程时直接返回错误，不阻塞；
  - 读写模式（O_RDWR）：同一进程可同时读写，不会阻塞。



 **信号(Signal)**
信号是Linux最轻量化的IPC机制，本质是**异步通知**，用于告知进程发生特定事件（而非传输数据），是软件层面对硬件中断的模拟。

信号的核心特性：

- 异步性：信号可随时发送给进程，无需等待进程执行状态；若进程阻塞该信号，内核会暂存至解除阻塞后传递；
- 有限数据量：仅传递“信号类型”（编号），无附加数据（部分信号可携带少量数据，如SIGQUEUE）；
- 预定义行为：每个信号有默认处理方式（终止、忽略、暂停等），也可通过`signal()`/`sigaction()`自定义处理函数；
- 不可靠性：早期System V信号（如SIGINT）不保证有序送达，POSIX实时信号（SIGRTMIN~SIGRTMAX）支持排队，可靠性更高。



**消息队列(Message Queue)**
消息队列是内核中的**结构化消息链表**，解决了管道无格式、信号数据量少的问题，支持按“消息类型”读写数据。

消息队列的核心特性：

- 结构化存储：每个消息包含“类型标识”和“数据内容”，进程可按类型读取指定消息（无需FIFO）；
- 独立于进程：消息队列存于内核，进程退出后消息仍保留，需手动删除或内核重启时释放；
- 异步通信：发送方写入消息后无需等待接收方，接收方可在任意时间读取；
- 容量限制：单条消息大小、队列总容量受内核参数限制（如`msgmax`、`msgmnb`）。

Linux主流消息队列类型：

- System V消息队列：通过`msgget()`/`msgsnd()`/`msgrcv()`实现，标识符为key值，使用最广泛；
- POSIX消息队列：通过`mq_open()`/`mq_send()`/`mq_receive()`实现，基于文件路径标识，支持超时读取。

消息队列的优势：

- 克服管道“无格式字节流”的缺点，可按业务定义消息结构；
- 支持多发送方/多接收方，且可按优先级读取消息（按类型筛选）；
- 无需进程同步等待，降低耦合度。



**共享内存(Shared Memory)**
共享内存是**效率最高的IPC方式**，本质是内核开辟一块物理内存，让多个进程将其映射到自身虚拟地址空间，进程可直接读写该内存，无需数据拷贝。

共享内存的核心特性：

- 零拷贝：数据直接在进程虚拟地址空间与共享内存间交互，无内核缓冲区中转（管道/消息队列需2次拷贝：用户→内核→用户）；
- 映射机制：通过`mmap()`（POSIX）或`shmat()`（System V）将共享内存映射到进程地址空间，解除映射用`munmap()`/`shmdt()`；
- **无内置同步：多个进程同时读写会导致数据竞争，需配合信号量、互斥锁等同步机制；**
- 生命周期：POSIX共享内存需`shm_unlink()`删除，System V共享内存需`shmctl(IPC_RMID)`删除，否则内核重启前一直存在。

共享内存的实现方式：

- POSIX共享内存：推荐使用，通过`shm_open()`创建内存对象，`ftruncate()`设置大小，接口更简洁；
- System V共享内存：通过`shmget()`创建，基于key值标识，需手动管理标识符。



**信号量(Semaphore)**
信号量是内核中的**计数器**，核心作用是实现进程间的**同步与互斥**，控制多个进程对共享资源的访问。

信号量的核心特性：

- 原子操作：信号量的增减操作（P/V原语）是原子的，避免多进程竞争；
  - P操作（wait）：信号量值减1，若结果≤0则进程阻塞；
  - V操作（post）：信号量值加1，唤醒阻塞的进程；
- 类型分类：
  - 二值信号量：值为0/1，用于互斥（如控制共享内存的独占访问）；
  - 计数信号量：值为非负整数，用于控制资源数量（如限制同时访问的进程数）；
- 实现方式：
  - System V信号量：基于key值，通过`semget()`/`semop()`/`semctl()`实现；
  - POSIX信号量：有名信号量（文件路径标识）、基于内存的信号量（共享内存中），接口更易用。

信号量与互斥量的区别：

| 特性     | 信号量              | 互斥量              |
| -------- | ------------------- | ------------------- |
| 取值范围 | 非负整数            | 仅0/1（锁状态）     |
| 操作主体 | 不同进程/线程可操作 | 加锁/解锁需同一线程 |
| 核心用途 | 同步+互斥（多资源） | 仅互斥（单资源）    |
| 阻塞唤醒 | 可唤醒任意等待进程  | 仅唤醒一个等待线程  |



**套接字(Socket)**
套接字是跨网络/跨主机的IPC机制，不仅支持同一主机的进程通信，还能实现不同主机间的进程通信，是网络编程的核心基础。

套接字的核心特性：

- 通信范围：本地进程（AF_UNIX域）或网络进程（AF_INET/AF_INET6域）；
- 标识属性：由“域+端口号+协议类型”唯一确定；
  - 域：AF_UNIX（本地通信，基于文件系统）、AF_INET（IPv4网络）；
  - 端口号：0-65535，1024以下为系统保留端口；
  - 协议类型：
    - SOCK_STREAM（流套接字）：基于TCP，可靠、有序、面向连接；
    - SOCK_DGRAM（数据报套接字）：基于UDP，无连接、不可靠、速度快；
    - SOCK_RAW（原始套接字）：直接访问网络层（IP），用于自定义协议/网络抓包；
- 通信模型：
  - 客户端/服务器（C/S）：服务器绑定端口监听，客户端主动连接；
  - 无连接模型（UDP）：无需建立连接，直接发送数据报。

**总结**

1. **性能优先级**：共享内存（最快，零拷贝）> 消息队列 > 管道/FIFO > 套接字 > 信号（仅通知）；
2. **适用场景**：
   - 亲缘进程简单通信：匿名管道；
   - 无亲缘进程本地通信：有名管道/FIFO；
   - 跨网络通信：套接字；
   - 高并发数据传输：共享内存+信号量（同步）；
   - 异步通知/简单控制：信号；
   - 结构化数据异步通信：消息队列；
3. **注意**：共享内存、消息队列等需手动释放资源，否则会造成内核资源泄漏；多进程并发访问共享资源时，必须配合信号量/互斥锁保证同步。



【★★★★★】**进程和线程的区别？**

**进程**：操作系统资源分配的最小单位，拥有独立虚拟地址空间、文件描述符、栈、堆等完整资源，相互隔离。

**线程**：调度执行的最小单位，**共享所属进程的资源**，仅有独立栈、寄存器、局部变量，开销极小。

- **资源归属**

进程资源完全独立；同一进程内线程共享地址空间、全局变量、文件句柄，仅栈私有。

- **开销与创建销毁**

进程创建 / 销毁要申请释放整套资源，开销大；线程仅分配栈与上下文，创建切换成本极低。

- **通信方式**

进程地址隔离，必须使用管道、共享内存、Socket 等 IPC 通信；线程直接读写共享变量，只需锁、条件变量做同步。

- **地址空间**

每个进程有独立虚拟地址空间；线程共用进程的地址空间。

- **生命周期与影响**

一个进程崩溃，不会影响其他进程；**单个线程崩溃，会导致整个进程退出**。

- **调度单位**

OS 以**线程**为单位做 CPU 时间片调度；进程是资源容器，不参与直接调度。

- **执行实体**

一个进程至少包含一个主线程，线程是真正执行代码的载体。



**切换开销**：**进程切换需刷新页表、TLB，开销远大于线程切换；线程切换只切换少量寄存器。**

**适用场景**：**密集 IO 场景多用多线程；需强隔离、高稳定性场景选用多进程。**

**总结：**

进程是资源分配最小单位，线程是 CPU 调度最小单位。进程资源相互隔离，创建切换开销大，通信依赖 IPC；线程共享进程资源，开销小、通信简单。线程崩溃会牵连整个进程，进程之间互不影响。日常开发中，IO 密集型适合多线程，需要强隔离则使用多进程。



【★★★★★】**线程的通信方式？**

先点明前提：**同一进程内线程共享虚拟地址空间、全局变量、堆、文件描述符，天然具备通信条件，核心难点是解决并发安全与执行顺序问题**。

**共享全局变量 / 堆内存**

最直接的方式，多个线程读写同一块共享内存完成数据交互。

**问题**：并发读写会引发数据竞争，必须配合同步机制使用。

**同步互斥手段（保证线程安全、控制执行顺序）**

1. **互斥锁（mutex）**

- 作用：保证**同一时刻仅一个线程访问临界资源**，解决数据竞争。
- 特点：独占式访问，加锁、解锁成对使用；不可重复加锁（普通锁）。

2. **条件变量（condition_variable）**

- 作用：常和互斥锁搭配，实现**线程等待、唤醒**，控制执行时序。
- 典型场景：生产者 - 消费者模型，队列空则消费者等待，队列满则生产者等待。

3. **读写锁（rwlock）**

- 规则：**读共享、写独占**。
- 适用：读多写少场景，相比互斥锁提升并发读性能。

4. **信号量（semaphore）**

- 分为二进制信号量（等价互斥锁）、计数信号量。
- 作用：既可实现互斥，也能控制**并发线程数量**、协调执行顺序。

5. **原子变量（atomic）**

- 作用：底层 CPU 指令实现无锁并发，保护简单数值（计数器、状态标记）。
- 特点：无加锁开销，效率高，仅适用于简单读写场景。

同一进程线程共享内存，主要通过**全局变量、堆内存**完成数据通信。为解决并发安全与时序控制，常用：**互斥锁**保证临界区独占；**条件变量**实现线程等待唤醒，多用于生产消费模型；**读写锁**适配读多写少场景；**信号量**控制并发数；简单计数场景优先使用**原子变量**提升效率。



【★★★★★】**什么是死锁？如何避免死锁？**



**什么是死锁？**

**死锁**：指**两个或多个线程**，在执行过程中，**互相持有对方需要的锁，又都不释放自己的锁**，导致彼此永久阻塞、无法推进的状态。

**这四个条件**必须**同时满足**，才会产生死锁：

1. **互斥条件**：资源（锁）同一时间只能被一个线程占有。
2. **请求并保持**：线程已经持有一把锁，又去请求另一把锁，且不释放已有的锁。
3. **不可剥夺**：线程持有的锁，只能自己释放，别人不能强行抢走。
4. **循环等待**：线程之间形成**环形等待链**，A 等 B，B 等 C，C 等 A。



**破坏 4 个条件中的任意一个即可避免死锁**，工程上最常用、最实用的 4 种方法：

1. **固定加锁顺序（最常用、最有效）**

所有线程**统一按照固定顺序加锁**，比如：先锁 A，再锁 B，绝不反过来。

**破坏：循环等待**

2. **避免嵌套加锁**

一个线程同时只持有一把锁，不要在加锁后再去请求另一把锁。

**破坏：请求并保持**

3. **定时锁 / 尝试锁（try_lock）**

使用带超时的锁申请，拿不到就放弃、释放已持有的锁，不硬等。

**破坏：请求并保持 / 不可剥夺**

4. **加锁层次化 / 锁粗化**

减少锁的数量，用一把大锁代替多把小锁，从根源避免嵌套。



**总结：**

死锁是多个线程互相持有对方需要的锁，形成循环等待，导致永久阻塞。

产生必须满足**互斥、请求保持、不可剥夺、循环等待**四个条件。

避免死锁最常用的方法是**固定加锁顺序**，也可以**避免嵌套锁、使用定时尝试锁**，破坏任意一个必要条件即可。



【★★★✩✩】**如何排查死锁？**

**怎么发现死锁？（现象）**

1. **程序完全卡住，不响应请求**
2. **CPU 占用极低（几乎为 0）**，线程都在阻塞等待
3. **日志停输出，接口超时，服务假死**
4. `top` 查看进程处于休眠（S/D 状态）

只要满足 **CPU 低、卡住、不工作**，90% 是死锁。



**找到进程 PID**

```
ps -ef | grep 你的程序
```

**查看线程状态（看是否大量阻塞）**

```
top -H -p <PID>
```

**现象**：所有线程都在 `lock`、`wait`、`sleep` 状态，没有运行中的线程。

**使用 gdb 查看线程调用栈**

```
gdb attach <PID>
```

**进入 gdb 后输入：**

```
info threads
thread apply all bt
```

你会看到：

- 线程 1 等待锁 A
- 线程 2 等待锁 B
- 互相持有对方需要的锁



**死锁排查主要分为四步：**

第一，通过服务**卡住、CPU 低、无响应**等现象判断可能发生死锁；

第二，使用 `gdb` 查看线程栈，找到阻塞在锁上的线程；

第三，分析线程间的**锁持有与等待关系**，确认循环等待；

第四，定位代码中的**加锁顺序不一致、嵌套锁**等问题，最终通过统一加锁顺序、避免嵌套、使用定时锁等方案解决死锁。



【★★★✩✩】**进程与线程上下文切换**？

**上下文**：CPU 执行任务时的运行环境，包括寄存器、程序计数器、栈、内存映射、状态等数据。

**上下文切换**：CPU 暂停当前任务，保存现场，加载另一个任务的现场并继续执行，是操作系统调度的核心行为。



**进程上下文切换**

**触发场景**

- 进程时间片耗尽
- 进程阻塞（等待 IO、锁、信号）
- 高优先级进程抢占

**切换流程 & 开销**

1. 保存当前进程**寄存器、程序计数器**；
2. 保存**页表、虚拟地址空间、文件描述符、全局资源**等整个进程环境；
3. 更新内存管理单元（MMU），刷新 TLB（地址翻译缓存）；
4. 加载新进程的上下文、页表，恢复寄存器，继续执行。

**特点**

开销**极大**。需要切换完整地址空间、刷新 TLB，伴随大量内存访问与内核操作，频繁切换会严重拉低系统性能。

> ## 一、定义
>
> **TLB (Translation Lookaside Buffer)**，地址转换后备缓冲，是 CPU 内部的**高速缓存**，专门用来缓存**虚拟地址 → 物理地址**的映射关系。
>
> 页表存在内存中，每次地址翻译都访问内存会很慢，TLB 就是为了**加速地址转换**。
>
> ## 二、工作流程
>
> 1. CPU 拿到虚拟地址，先查 **TLB**；
> 2. **命中**：直接取出物理地址，访问内存，速度极快；
> 3. **未命中（TLB Miss）**：再去查内存中的页表，完成地址映射，同时把新映射写入 TLB。
>
> ## 三、和进程 / 线程切换的关联（面试核心）
>
> 1. **每个进程有独立页表**，对应一套独立的地址映射；
> 2. **进程上下文切换**：必须切换页表，原有 TLB 里的映射全部失效，需要**刷新 TLB**，产生大量 TLB Miss，带来显著性能开销；
> 3. **同进程线程切换**：共用同一份页表，TLB 映射不变，**无需刷新**，这也是线程切换远快于进程切换的关键原因之一。
>
> ## 补充要点
>
> - TLB 容量很小，属于 CPU 硬件缓存，访问速度远快于内存；
> - 频繁进程切换 → 频繁刷新 TLB → 大量地址转换失效 → 系统性能下降。
>
> TLB 是 CPU 内置的地址转换缓存，用来缓存虚拟地址到物理地址的映射，加快内存访问。进程切换会更换页表、刷新 TLB，开销大；同进程线程共享页表，TLB 无需刷新，切换开销很小。



**线程上下文切换**

**触发场景**

和进程一致：时间片耗尽、线程阻塞、高优先级线程抢占。

**切换流程 & 开销**

1. 仅保存 / 恢复**线程私有数据**：寄存器、程序计数器、线程栈；
2. **共享进程的虚拟地址空间、页表、全局资源**，无需切换页表、不刷新 TLB；
3. 直接加载新线程私有现场即可运行。

**特点**

开销**极小**。同进程线程共用资源，仅切换少量寄存器与栈，是多线程模型高效的核心原因。

**为什么进程切换开销远大于线程？**

同一进程内线程共享虚拟地址空间、页表等核心资源，线程切换只需保存少量硬件上下文；而进程拥有独立地址空间，切换时必须替换页表、刷新 TLB，产生额外内存开销，因此代价更高。

**自愿 / 非自愿切换区别**

**自愿切换**：任务主动放弃 CPU（如`sleep`、等待互斥锁、读 IO），属于正常业务行为；

**非自愿切换**：时间片用完、被高优先级任务抢占，频繁出现说明 CPU 资源紧张。

**总结**

上下文切换是 CPU 调度时保存、恢复任务运行现场的过程。进程切换需要更换独立地址空间、刷新 TLB，开销很大；同进程线程共享资源，仅切换寄存器和私有栈，开销很小。实际开发中，IO 密集型场景用多线程减少切换开销；需要强资源隔离时选用多进程。同时可通过`vmstat`、`pidstat`监控切换次数，判断系统调度压力。



【★★★✩✩】**进程调度算法知道哪些**？

**基础概念**

1. **调度时机**：时间片耗尽、进程阻塞、高优先级进程就绪、中断唤醒。
2. **抢占式**：高优先级进程可打断正在运行的低优先级进程（现代操作系统主流）。
3. **非抢占式**：进程主动放弃 CPU，才切换任务，简单但响应差。

**经典基础调度算法**

1. **FCFS 先来先服务**

- 规则：按进程到达顺序依次执行，**非抢占式**。
- 特点：实现简单；短作业等待长作业，产生**惊群效应 / 短作业饥饿**。
- 适用：早期批处理系统。

2. **SJF 短作业优先**

- 规则：优先调度**预计运行时间更短**的进程，分抢占 / 非抢占。
- 特点：平均等待时间最短；**长作业饥饿**。

3. **高优先级调度**

- 规则：优先运行优先级高的进程，分静态优先级、动态优先级。
  - 静态：优先级创建后不变；
  - 动态：随等待时间、运行时长动态调整，避免饥饿。
- 抢占式实现：高优先级进程就绪，立刻抢占 CPU。
- 问题：低优先级进程长期得不到执行，引发饥饿。

4. **时间片轮转 RR**

- 规则：就绪队列按 FCFS 排队，每个进程分配固定**时间片**，时间片耗尽则切换下一个进程，**抢占式**。
- 特点：公平、响应快，适合分时系统（桌面、服务器）；时间片大小是关键：
  - 片过长 → 退化为 FCFS，交互卡顿；
  - 片过短 → 频繁上下文切换，开销变大。

5. **多级反馈队列**

- 规则：划分多个优先级队列，优先级从高到低；高优先级队列先调度，同队列内时间片轮转。
  1. 新进程进入最高优先级队列；
  2. 时间片用完未结束，降级到下一级队列；
  3. 阻塞进程唤醒后，回到原队列。
- 特点：兼顾**响应速度、长短作业**，综合性能强，传统 Unix 常用。

**Linux 主流：CFS 完全公平调度**

1. **核心思想**

摒弃传统时间片、静态优先级，**保证所有就绪进程公平占用 CPU**。

- 引入 **虚拟运行时间**：进程实际运行越久，虚拟时间增长越快；
- 调度规则：永远选择**虚拟运行时间最小**的进程执行。

2. **权重与 nice 值**

- `nice` 值：范围 `-20 ~ 19`，默认 0；数值越小，优先级越高、权重越大。
- 权重决定 CPU 占比：高权重进程能分到更多运行时间，但依然遵循公平原则。

3. **特点**

4. **完全抢占**：新就绪进程若虚拟时间更小，会抢占当前进程；
5. 无严格时间片概念，动态分配运行时长；
6. 有效避免饥饿，所有进程都能被调度；
7. 适配服务器、桌面场景，是目前 Linux 默认调度器。



1. **短作业优先为什么会饥饿？**

   持续不断的短作业到达，长作业一直得不到调度，长期等待。动态优先级可缓解该问题。

2. **时间片大小如何取舍？**

   结合硬件性能与业务场景，兼顾交互体验和切换开销；IO 密集型可设小时间片，CPU 密集型适当放大。

3. **CFS 和传统 RR 区别？**

   RR 是固定时间片轮转；CFS 基于虚拟时间实现公平调度，依靠 nice 值区分权重，调度更均衡，适应性更强。

4. **IO 密集型 vs CPU 密集型 调度表现**

- IO 密集型：频繁主动放弃 CPU，切换开销小，系统整体吞吐高；
- CPU 密集型：持续占用 CPU，容易引发非自愿上下文切换，拉高系统负载。

**总结**

常见进程调度算法有先来先服务、短作业优先、高优先级、时间片轮转、多级反馈队列。

现代 Linux 系统默认使用 **CFS 完全公平调度器**，它以虚拟运行时间为调度依据，结合 nice 值设置进程权重，保证公平性且支持抢占，能很好适配各类业务场景。不同算法各有优劣，短作业优先易造成饥饿，时间片轮转需合理设置片长。

## 计算机网络

【★★★★★】**TCP三次握手与四次挥手？**

 **三次握手的完整流程**

三次握手的步骤如下：

**第一次握手（客户端→服务器）：**

SYN = 1， seq = x浏览器（客户端）向服务器发送一个“连接请求报文”，报文中包含两个关键信息：SYN标志位设为1（表示这是一个连接请求），序号seq = x（x是客户端随机生成的一个初始序号，用于标识后续发送的数据）。此时，客户端进入“SYN_SENT”状态，等待服务器回应。

**第二次握手（服务器→客户端）：**

SYN = 1， ACK = x+1， seq = y服务器收到客户端的连接请求后，会先确认：“我收到你的请求了，并且我也能向你发送数据”。所以服务器会返回一个响应报文，包含三个关键信息：SYN标志位设为1（表示服务器也同意建立连接），ACK标志位设为1（表示确认收到客户端的请求），确认号ACK = x+1（表示“我已经收到了你序号为x的报文，下次请发送x+1及以后的报文”），同时服务器也生成一个自己的初始序号seq = y。此时，服务器进入“SYN_RCVD”状态，等待客户端的最终确认。

**第三次握手（客户端→服务器）：**

ACK = y+1， seq = x+1客户端收到服务器的响应后，会确认：“服务器的收发能力正常，我也准备好发送数据了”。所以客户端会再发送一个确认报文，包含两个关键信息：ACK标志位设为1，确认号ACK = y+1（表示“我已经收到了你序号为y的报文，下次请发送y+1及以后的报文”），序号seq = x+1（延续客户端之前的序号，开始发送后续数据）。**此时，客户端进入“ESTABLISHED”状态（连接建立完成）；服务器收到这个确认报文后，也进入“ESTABLISHED”状态，双方可以开始传递数据了。**

**为什么需要三次握手？**

核心是**防止 “失效的连接请求报文” 被服务端接收**，导致资源浪费：

假设客户端发送的 SYN 报文因网络延迟滞留，客户端超时后重发并建立连接、传输数据、关闭连接。

**之后滞留的旧 SYN 报文到达服务端，若只有两次握手，服务端会直接建立新的连接并等待客户端发送数据，但客户端根本不会理会，服务端会一直维护这个无效连接，浪费资源。**



**二次握手就可以建立链接，三次握手是为了防止丢包的链接被服务端等待：**

如果拆成**四次握手**：

1. 客户端 SYN → 服务端
2. 服务端 ACK → 客户端（单独应答）
3. 服务端 SYN → 客户端（再单独发连接请求）
4. 客户端 ACK → 服务端

问题：**第 2、3 步可以合并成一个 `SYN+ACK` 报文**，没必要拆成两个。

四次只是把**可合并的两个报文强行拆分**，多一次报文交互，增加延迟、带宽开销，没有任何收益。

**三次握手时，服务端需要等待客户端的第三次 ACK 确认，旧的 SYN 报文不会收到客户端的 ACK，服务端会超时释放资源。**



**为什么三次握手需要确认号（ack）和序列号（seq）**

TCP 三次握手时发送**序列号（seq）** 和**确认号（ack）**，核心目的是为后续的**可靠数据传输**打下基础 —— 这两个字段是 TCP 实现**有序传输、去重、重传**等核心功能的关键，也是连接双方确认彼此收发能力的核心依据。

**有了seq 和 ack，此时双方才能实时监控信息传输进度，自动过滤重复信息 或 确认丢失数据。**

`seq` 和 `ack` 的这个作用，是**从三次握手同步初始序号后才正式生效**的。三次握手的核心就是让双方确认彼此的初始 `seq`（ISN），只有初始序号同步了，后续数据传输的 `seq` 递增、`ack` 反馈才有统一的基准，上述的进度监控、去重、重传逻辑才能正常运转

三次握手的核心目标之一，是让客户端和服务端都确认 **“对方能正常发，我能正常收；我能正常发，对方能正常收”**。而 seq 和 ack 就是验证这个能力的关键：

- 服务端能正确返回 `ack=x+1`，说明**客户端的发送能力正常**（服务端收到了客户端的 SYN 报文），且**服务端的发送能力正常**（能发出带 ack 的报文）。
- 客户端能正确返回 `ack=y+1`，说明**服务端的发送能力正常**（客户端收到了服务端的 SYN+ACK 报文），且**客户端的确认能力正常**。

如果没有 seq 和 ack，双方只能确认 “报文收到了”，但无法确认 “后续数据该怎么编号”，更无法验证完整的收发链路。

**如果三次握手不携带这两个字段，TCP 连接会存在的致命问题：**

1. **数据传输无序**：后续发送的报文段没有编号，接收方无法判断报文的先后顺序，可能出现 “后发的数据先到” 的混乱。
2. **无法重传和去重**：接收方无法确认哪些数据丢了、哪些数据重复了，TCP 的可靠传输就无从谈起。
3. **旧连接请求泛滥**：大量失效的旧 SYN 报文会建立无效连接，耗尽服务端资源。



**四次挥手**

TCP 是**全双工**通信（双方可同时收发数据），关闭连接时，需要双方分别关闭自己的发送通道，因此需要四次交互。

**核心前提**

- 连接关闭可由**客户端或服务端**主动发起，以下以**客户端主动关闭**为例。
- TCP 报文的 `FIN` 标志位表示 “数据发送完毕，请求关闭发送通道”。

1. **第一次挥手（客户端 → 服务端）**
   - 客户端主动关闭连接，向服务端发送 **FIN 报文**，报文关键字段：
     - `FIN=1`：表示客户端没有数据要发送了，请求关闭发送通道。
     - `seq=u`：客户端当前的序列号（累计之前传输的数据后的序号）。
   - 客户端进入 **FIN_WAIT_1** 状态，等待服务端的确认。
   - 作用：客户端告诉服务端 “我不再向你发送数据了”。
2. **第二次挥手（服务端 → 客户端）**
   - 服务端收到 FIN 报文后，向客户端回复 **ACK 报文**，报文关键字段：
     - `ACK=1`：确认标志位。
     - `ack=u+1`：确认号，等于客户端的 FIN 序列号 + 1。
     - `seq=v`：服务端当前的序列号。
   - 服务端进入 **CLOSE_WAIT** 状态；客户端收到 ACK 后进入 **FIN_WAIT_2** 状态。
   - 作用：服务端告诉客户端 “我已收到你的关闭请求，你可以停止发送数据了，但我可能还有数据要发给你”。
   - 此时**客户端到服务端的发送通道已关闭**，但服务端到客户端的发送通道仍可传输数据。
3. **第三次挥手（服务端 → 客户端）**
   - 服务端将剩余数据发送完毕后，向客户端发送 **FIN 报文**，报文关键字段：
     - `FIN=1`：表示服务端也没有数据要发送了，请求关闭自己的发送通道。
     - `ACK=1`：确认标志位。
     - `ack=u+1`：沿用之前的确认号。
     - `seq=w`：服务端发送完剩余数据后的序列号。
   - 服务端进入 **LAST_ACK** 状态，等待客户端的确认。
   - 作用：服务端告诉客户端 “我也没有数据要发送了，请求关闭我到你的发送通道”。
4. **第四次挥手（客户端 → 服务端）**
   - 客户端收到 FIN 报文后，向服务端回复 **ACK 报文**，报文关键字段：
     - `ACK=1`：确认标志位。
     - `ack=w+1`：确认号，等于服务端的 FIN 序列号 + 1。
     - `seq=u+1`：客户端当前的序列号。
   - 客户端发送 ACK 后进入 **TIME_WAIT** 状态（等待 2MSL 时间后才会进入 CLOSED 状态）；服务端收到 ACK 后直接进入 **CLOSED** 状态。
   - 作用：客户端告诉服务端 “我已收到你的关闭请求，连接即将完全关闭”。

**关键细节：TIME_WAIT 状态与 2MSL**

- **MgSL**（Maximum Segment Lifetime）：报文在网络中的最大生存时间，超过该时间报文会被丢弃。
- 客户端发送第四次 ACK 后，需要等待 **2MSL** 时间：
  1. **确保服务端能收到第四次的 ACK 报文。如果服务端没收到，会重发第三次的 FIN 报文，客户端在 2MSL 内收到后会重新发送 ACK，并刷新等待时间。**
  2. **确保本次连接的所有报文都从网络中消失，避免新连接收到旧连接的残留报文。**

**为什么要四次挥手，三次挥手可以吗？**

TCP 连接是**全双工**的，意味着客户端和服务端可以**同时双向收发数据**，两个方向的数据流是相互独立的。关闭连接不是 “一刀切断”，而是要分两步：

1. 关闭 **A→B** 的发送通道；
2. 关闭 **B→A** 的发送通道。

只有两个方向都关闭，连接才算真正关闭。

| 步骤 | 发起方          | 报文 | 核心目的                                                     |
| ---- | --------------- | ---- | ------------------------------------------------------------ |
| 1    | 客户端 → 服务端 | FIN  | 客户端说：**我不再给你发数据了（关闭自己的发送通道）**，但我还能接收你的数据 |
| 2    | 服务端 → 客户端 | ACK  | 服务端说：**我收到你的关闭请求了**，我还有数据要发，你等我发完 |
| 3    | 服务端 → 客户端 | FIN  | 服务端说：**我也不给你发数据了（关闭自己的发送通道）**       |
| 4    | 客户端 → 服务端 | ACK  | 客户端说：**我收到你的关闭请求了**，连接可以彻底关闭了       |

- 三次挥手：指建立连接时，只需要双方确认 “收发能力正常 + 同步序列号”，一次 SYN+ACK 就能完成双向同步。

**正常来说三次挥手无法满足数据的完整传输，如果第三次挥手时，另一方还有未发送完的数据，此时立即关闭传输会导致数据的传输失败，但是当另一方收到第二次挥手信号时，已经没有多余的数据进行传输，此时可以进一步优化为三次挥手。**



【★★★★★】**什么是https协议？其与传统http协议有什么不同？**

简单来说：**Http是超文本传输协议，明文传输，无加密、无身份验证，安全性差；Https是Http+SSL/TLS协议的组合，基于对称加密+非对称加密实现数据加密传输，有身份验证，安全性高，是目前主流的传输协议**。

Http和Https的核心区别在于安全性和加密机制，具体可以分为6点：

**安全性**：Http明文传输，无加密、无身份验证，易被拦截篡改；Https基于SSL/TLS加密（加密层处于应用层与传输层之间），实现数据加密和身份验证，安全性高。

**协议与端口：**Http基于TCP，**默认80端口**；Https是Http+SSL/TLS，**默认443端口**。

**加密原理：**Http无加密；Https采用**非对称加密（握手）**+**对称加密（传输）**，兼顾安全和效率。

**证书要求：**Http无需证书；Https需配置权威CA颁发的数字证书，验证服务器身份。

**性能开销：**Http无开销，效率略高；Https有握手和加密解密开销，对服务器CPU有要求。

**适用场景：**Http适合非敏感数据（如静态页面）；Https适合敏感数据（如登录、支付），是目前主流选择。

从后端开发角度，我们在部署时，会为Https配置SSL证书，优化握手性能，确保数据传输安全，同时将Http请求跳转至Https，提升用户体验和业务安全性。

【★★✩✩✩】**什么是TLS？TLS握手流程？**

TLS 是用于数据加密传输的安全协议，TLS 是 SSL 的升级版本，核心作用是为 Https 提供加密和身份验证 —— 加密 Http 数据、验证服务器身份，避免数据泄露和伪造，是 Https 实现安全传输的基础。

**TLS 握手四步**

Client Hello（客户端→服务端）

- 发送：TLS 版本、支持加密套件列表、**客户端随机数 Client Random**、会话 ID、扩展字段
- **无加密，明文传输**

Server Hello（服务端→客户端）

- 选定 TLS 版本、选定一套加密套件、**服务端随机数 Server Random**、服务端证书（含公钥）
- 明文下发证书

> 至此：双方拿到两个随机数：ClientRand + ServerRand

密钥交换（**非对称加密环节**，关键分界点）

客户端生成 **Pre-Master Secret (预主密钥，48 字节随机串)**

1. 用**服务端证书里的公钥（非对称 RSA/ECC）加密 PreMasterSecret**，发给服务端
2. 服务端用自己**私钥解密**得到 PreMasterSecret

> ✅ **非对称加密只用在这里：加密预主密钥，防止密钥被窃听**

双方现在三样齐全：

```
ClientRand + ServerRand + PreMasterSecret
```

通过 PRF 伪随机函数推导：

**会话主密钥 Master Secret → 再派生 4 组对称密钥：客户端读写密钥、服务端读写密钥**

ChangeCipherSpec + Finished

1. ChangeCipherSpec：通知后续切换**对称加密**
2. Finished 报文：用派生的**对称密钥 + MAC 算法**加密校验握手完整性

> Finished 校验通过 → **握手结束**

![image-20260604212807713](C:\Users\a2556\AppData\Roaming\Typora\typora-user-images\image-20260604212807713.png)

【★★★★✩】**OSI 七层模型 & TCP/IP 五层模型**

OSI 七层是**理论模型**，TCP/IP 五层是**实际互联网用的简化模型**，结构更清晰，工程上更常用。

**OSI 七层（理论标准，ISO 制定）**

7 应用 →6 表示 →5 会话 →4 传输 →3 网络 →2 数据链路 →1 物理

**TCP/IP 四层（工业事实标准，互联网原生）**

4 应用层 (合并 7/6/5) →3 传输层 →2 网际层 (IP) →1 网络接口层 (合并 2+1)

对应：

- 应用：HTTP/DNS/TLS/FTP
- 传输：TCP/UDP
- 网际：IP、ICMP、ARP
- 接口：以太网、WiFi

OSI 是理想化理论分层，TCP/IP 是互联网实战沉淀的事实标准；OSI 分七层细化会话、表示，TCP/IP 四层合并上层，因为 TCP/IP 提前占领市场，成为现在通用网络架构。

【★★★★✩】**在浏览器中键入一段URL到浏览器返回响应，期间经历了什么？**

**DNS 解析 → TCP 连接 → TLS 握手（HTTPS）→ HTTP 请求 → 服务器处理 → 响应返回 → 浏览器渲染 → 断开连接**

1. 浏览器查 DNS，把域名解析成 IP
2. 与服务器建立 TCP 三次握手
3. HTTPS 进行 TLS 加密握手
4. 发送 HTTP 请求报文
5. 服务器经负载均衡、应用处理、数据库查询后返回响应
6. 浏览器解析 HTML、CSS、JS 并渲染页面
7. 最后 TCP 四次挥手断开连接

【★★✩✩✩】**什么是大端序，小端序，网络字节序？**

**字节序概述：**

- 字节序（Endianness）是数据在计算机内存中存储的顺序方式
- 计算机将数据按字节（8位）进行存储，不同的系统可能会有不同的字节存储顺序

**大端序（Big Endian）：**

- 定义：在大端序中，数据的高位字节存储在内存的低地址处，低位字节存储在高地址处。
- 例：假设我们要存储一个32位整数`0x12345678`，它在内存中的存储顺序是：
  - 地址 0X00：`0x12`
  - 地址 0X01：`0x34`
  - 地址 0X02：`0x56`
  - 地址 0X03：`0x78`

**小端序（Little Endian）:**

- 定义：在小端序中，数据的高位字节存储在内存的高地址处，低位字节存储在低地址处。
- 例：假设我们要存储一个32位整数`0x12345678`，它在内存中的存储顺序是：
  - 地址 0X00：`0x78`
  - 地址 0X01：`0x56`
  - 地址 0X02：`0x34`
  - 地址 0X03：`0x12`

**网络字节序（Network Byte Order）:**

- 定义：网络字节序是为了确保在网络中不同的计算机间传输数据时，能够统一数据的存储顺序，它采用的是**大端序。**

- 说明：即使发送方和接收方的计算机采用不同的字节序，网络字节序确保了数据传输的正确性，通常使用网络协议（如TCP/IP）进行数据传输时，都要求将数据转换为网络字节序。

**为什么需要字节序？**

- **跨平台兼容性：**不同架构的计算机可能使用不同的字节序方式，为了确保数据在不同机器间传输时不会发生错误，需要有统一的字节序标准
- **网络通信：**通过网络传输数据时，不同的计算机可能采用不同的字节序，使用网络字节序可以确保数据正确传输。
- **硬件差异：**一些硬件平台（如X86、ARM）采用小端序，而一些平台（如某些RISC系统）采用大端序，标准化字节序能够提高兼容性。

【★★★★✩】**HTTP状态码？**

当浏览者访问一个网页时，浏览者的浏览器会向网页所在服务器发出请求。当浏览器接收并显示网页前，此网页所在的服务器会返回一个包含 HTTP 状态码的信息头（server header）用以响应浏览器的请求。

HTTP 状态码的英文为 **HTTP Status Code**。

下面是常见的 HTTP 状态码：

- **1xx（信息性状态码）**：表示请求已接收，正在处理，很少用到（比如100 Continue，表示客户端可以继续发送请求）。
  - **101**  **Switching Protocols**  切换协议。服务器根据客户端的请求切换协议。只能切换到更高级的协议，例如，切换到HTTP的新版本协议或切换至WebSocket协议

- **2xx（成功状态码）**：表示请求处理成功，最常用的是**200 OK**（请求成功，返回正常数据）；还有204 No Content（请求成功，但没有响应体，比如DELETE请求成功后）。

- **3xx（重定向状态码）**：表示请求需要进一步操作才能完成，常用的有：
  - **301 Moved Permanently**：永久重定向（比如域名更换后，访问旧域名会重定向到新域名，浏览器会缓存重定向关系）。

  - **302 Found：**临时重定向（比如登录后重定向到首页，浏览器不会缓存）。

  - **304 Not Modified：**协商缓存命中（表示浏览器缓存的资源没有过期，服务器不需要返回新的资源，直接使用浏览器缓存即可，提升效率）。

- **4xx（客户端错误状态码）**：表示请求有错误，责任在客户端，常用的有：

  - **400 Bad Request：**请求参数错误（比如POST请求体格式错误）。

  - **401 Unauthorized：**未授权（比如访问需要登录的接口，没有携带登录凭证）。

  - **403 Forbidden**：禁止访问（比如没有权限访问某个资源）。

  - **404 Not Found**：请求的资源不存在（比如URL路径错误）。

- **5xx（服务器错误状态码）**：表示请求处理失败，责任在服务器，常用的有：
  - **500 Internal Server Error：**服务器内部错误（比如应用服务器崩溃、代码报错）。

  - **502 Bad Gateway：**网关错误（比如负载均衡器无法连接到后端服务器）。

  - **503 Service Unavailable：**服务器暂时不可用（比如服务器过载、维护中）。

  - **504 Gateway Timeout：**网关超时（比如后端服务器处理请求时间过长，超过了网关的超时时间）。

【★★★✩✩】**HTTP协议各版本的区别？**



**HTTP/1.0（短连接）**

1. 默认**短连接**：一次 TCP 连接只发**1 次请求响应**，完事断开；频繁建连开销大。
2. 无持久连接、无管道，不支持断点续传规范。
3. 头域简陋，**缓存只靠`Expires`**。



**HTTP/1.1（最经典，长线连接）**

新增核心

1. **长连接 Keep-Alive**：一条 TCP 链路复用连续多次请求，**默认开启**，减少握手。
2. **管线化 Pipeline**：**客户端连续多发请求不用等上一个响应**，但**服务端必须顺序回包（队头阻塞 HOL）**，实际极少使用。
3. 缓存升级：新增`Cache-Control`，优先级高于 Expires。
4. 断点续传：Range 分块下载；Host 头，一台服务器多域名。

**痛点**

**TCP 层队头阻塞**：一个请求卡住，同连接后面所有请求全部等待。



**HTTP/2（二进制帧、多路复用，TCP 之上）**

1. **二进制帧**：文本报文 → 二进制分片帧，解析更快。
2. **单 TCP 多路复用（关键）**：一条 TCP 连接，**并行收发多个流**，解决 HTTP1.1 队头阻塞；不同请求帧交错传输。
3. **头部压缩 HPACK**：重复 Header 压缩，减少带宽。
4. **服务器推送 Server Push**：服务端主动推送静态资源（css/js）。



**遗留问题** 

底层仍是 TCP，**TCP 丢包会阻塞整条连接所有流（TCP 层队头阻塞无法根治）**。



**HTTP/3 = HTTP over QUIC（UDP 之上）**

1. 传输层改用**UDP**，改作 **QUIC** 协议，彻底干掉 TCP 队头阻塞。
2. 每条数据流独立拥塞控制，单个流丢包只阻塞当前请求，不影响其他。
3. **0-RTT/1-RTT 握手**：TLS 和传输握手合并，比 TCP+TLS 握手更快。
4. 连接迁移：手机切 WiFi / 流量，连接不断（用连接 ID 标识，不用 IP + 端口）。



核心对比一句话

- 1.0：一请求一 TCP；
- 1.1：长连接但串行排队，队头阻塞；
- HTTP2：单 TCP 多路复用，应用层无阻塞、TCP 仍阻塞；
- HTTP3：UDP+QUIC，全链路无队头阻塞。

【★★★✩✩】**TCP的流量控制与拥塞控制？**

**流量控制保接收端不爆仓，拥塞控制保整条网络不堵车，发送窗口取两者最小值。**

**TCP流量控制**

**防止发送方发送速率过快，淹没接收方缓冲区，造成接收溢出丢包**；由**接收方告知剩余接收缓存容量**

TCP 头部 16 位**窗口字段 = 接收窗口 (rwnd)**，由接收方在 ACK 中携带，代表：**接收方当前空闲缓冲区字节数，发送方最多一次性发送 rwnd 字节数据**。

**滑动窗口工作逻辑**

1. 接收方收到数据→存入缓冲区，消耗缓存；空闲缓存变少，下一次 ACK 更新更小的 rwnd。
2. 发送方收到 ACK，更新自身允许发送上限，未确认数据不能超窗口上限。
3. 接收方应用程序从内核缓冲区读取数据→空闲缓存增大，后续 ACK 携带更大 rwnd，发送方可提速。

> 注：TCP滑动窗口与内核缓冲区
>
> TCP 的滑动窗口协议是一种流量控制和数据重传的协议，可以用于确保数据的可靠性和完整性。在 TCP 的滑动窗口协议中，发送方和接收方都维护一个窗口，用于控制数据的发送和接收。**以下是使用 TCP 的滑动窗口协议进行数据重传的一般步骤：**
>
> **发送方发送数据：**发送方将数据分为多个分组，每个分组包含一个序号和一定数量的数据。发送方维护一个发送窗口，用于控制已发送但未确认的数据的数量。发送方依次发送分组，并等待接收方的确认。
>
> **接收方接收数据：**接收方维护一个接收窗口，用于控制已接收但未处理的数据的数量。接收方依次接收分组，并发送确认。
>
> **发送方等待确认：**在发送数据后，发送方等待接收方的确认。如果接收方未及时发送确认，则发送方会重传相应的数据。
>
> **接收方处理数据：**在接收数据后，接收方将数据存储到缓存区中，等待应用程序的处理。如果接收方发现数据有误或者丢失，则发送 NACK（Negative Acknowledgement）请求发送方进行重传。
>
> **发送方重传数据：**当发送方收到接收方的 NACK 请求时，会重新发送相应的数据。发送方依然维护一个发送窗口，用于控制已发送但未确认的数据的数量。重传的数据会使用新的序号，可以和之前的数据进行区分。
>
> 重复上述步骤，直到数据传输完成。
>
> 
>
> **内核 TCP 接收缓冲区 = 内核预留缓存区，收到数据先存在内核缓冲区，应用进程调用`read/recv`把数据从内核拷贝到用户内存 → 内核里的数据被拿走，内核缓冲区空出空间 → 空闲变大，rwnd 变大。**
>
> ## 缓冲区分层
>
> TCP 数据分两段缓存：
>
> 1. **内核接收缓冲区（OS 内核空间）**：网卡收包→内核协议栈→存在这里，**rwnd 统计的就是这块剩余空闲大小**；
> 2. **应用用户缓冲区（用户进程内存）**：应用`recv()`才把数据从**内核缓冲区搬到应用缓冲区**。

举例

- 接收缓冲区总大小 4000B；收到 2000B 未被应用读走 → 空闲 2000B → ACK 中`rwnd=2000`，发送方最多发 2000B。
- 应用读取 1500B 数据，空闲变为 3500B → 下次 ACK`rwnd=3500`，发送窗口放大。

**TCP拥塞控制：四大阶段**

经典的TCP拥塞控制包含四个核心阶段：**慢启动、拥塞避免、快速重传、快速恢复。**这四个阶段协同工作，实现了“探测带宽→稳定传输→应对丢包→快速恢复”的闭环，也是后续所有优化算法的基础。

**慢启动（Slow Start）：谨慎探测，指数增长**
当TCP连接刚建立，或发生超时重传后，发送端对网络的可用带宽一无所知——此时若盲目发送大量数据，极易引发拥塞，因此慢启动的核心目标是“快速、谨慎地探测网络带宽上限”，采用指数增长策略。

具体机制：

初始状态：cwnd初始化为1 MSS（即发送端先发送1个数据包，等待确认）；

增长规则：每收到一个ACK（确认报文），cwnd就增加1 MSS，意味着每经过一个RTT（往返时间），cwnd会翻倍（比如1→2→4→8…）；

**拥塞避免（Congestion Avoidance）：稳定传输，线性增长**
当cwnd达到ssthresh后，网络已接近其承载上限，此时若继续指数增长，极易引发拥塞。因此拥塞避免的核心目标是“稳定传输，缓慢试探带宽上限”，采用线性增长策略，**遵循“AIMD（加法增大，乘性减小）”原则**——无拥塞时缓慢增加带宽占用，检测到拥塞时快速降低带宽占用，保证网络稳定性和公平性。

具体机制：

**增长规则：**每经过一个RTT，cwnd只增加1 MSS（而非翻倍）。具体实现为：每收到1个ACK，cwnd增加 MSS/cwnd，这样一个RTT内收到cwnd个ACK后，cwnd总增长为1 MSS；

**拥塞响应：**若发生超时丢包，将ssthresh重置为当前cwnd的一半，cwnd重置为1 MSS，重新进入慢启动；若收到3个重复ACK（提示轻微丢包），则触发快速重传，进入快速恢复阶段。

**快速重传（Fast Retransmit）：提前检测丢包，避免超时等待**
经典TCP的丢包检测，原本依赖“超时重传”——发送端发送数据包后，若等待超时（通常为200ms~1s）仍未收到ACK，才判断数据包丢失。这种方式的缺点是等待时间过长，会导致传输效率下降，尤其在轻微拥塞场景下，完全可以提前检测丢包并重传。

快速重传的核心机制：当发送端连续收到3个相同的重复ACK，就判定对应序号的数据包丢失（原理：接收端收到乱序数据包时，会重复发送已收到的最后一个有序数据包的ACK，若连续3次收到相同ACK，说明中间有数据包丢失），此时无需等待超时，立即重传丢失的数据包，并触发快速恢复阶段。

**优势：将丢包检测时间从“超时时间”缩短到“3个重复ACK的时间”，大幅减少等待损耗，提升传输效率。**

**快速恢复（Fast Recovery）：丢包后不“归零”，快速恢复传输**
快速恢复是Reno版本在Tahoe版本基础上的核心改进——Tahoe版本在收到3个重复ACK后，会将cwnd重置为1 MSS，重新进入慢启动，导致传输效率骤降；而快速恢复的核心目标是“避免cwnd骤降，在丢包后快速恢复到稳定传输状态”，适用于“轻微拥塞、少量丢包”的场景。

【★★★✩✩】**UDP协议相关问题**

**UPD & TCP 对比**

可靠性：TCP面向连接，提供可靠服务，即传送的数据不丢不重，按序无错。UDP无连接，尽最大努力交付，不保证可靠交付。

首部：TCP首部开销20字节，UDP首部8字节。

TCP提供拥塞控制，UDP无这个功能。

TCP面向字节流传输，UDP面向报文传输。

TCP仅支持点对点传输，UDP支持点对点，点对多，多对多。

**TCP什么时候比UDP快？**

大多数应用是基于http的，而http基于tcp，http是一个长时间的事务，可能会发生丢包的情况，**tcp只会重传丢失的数据包，而udp要将所有数据包重传，此时在数据传输的过程中TCP可能快于UDP**

![img](https://uploadfiles.nowcoder.com/images/20231227/575874874_1703686277320/4C6FA062CEB3407FADEDF634C998D439)



## **并发编程**

## 数据库相关

### MySQL

**【★★★✩✩】MySQL常用的存储引擎有哪些？它们的区别？**

MySQL的存储引擎是**基于表级别的插件式存储机制**——不同的表可以选择不同的存储引擎，作用是：

- 负责数据的存储、读取、更新、删除；
- 实现索引、事务、锁、崩溃恢复等核心功能；
- 适配不同的业务场景（如高并发写、只读查询、临时数据存储）。

常用的存储引擎有：**InnoDB**、**MyISAM**、**Memory**、**Archive**

1）其中InnoDB是为**事务处理、高并发写操作、数据完整性**设计的存储引擎，也是目前MySQL的默认选择，核心特性：

- **支持事务（ACID）**：通过MVCC（多版本并发控制）实现事务，支持COMMIT/ROLLBACK，满足金融、电商等对数据一致性要求高的场景；
- **行级锁**：仅锁定修改的行（而非整张表），高并发写场景下冲突更少、性能更高；
- **外键约束**：支持外键，保证数据的参照完整性；
- **崩溃恢复**：通过redo日志、undo日志实现崩溃后的数据恢复，可靠性高；
- **聚簇索引**：数据和主键索引存储在同一文件，主键查询速度极快；

**核心劣势**

- 读写性能略低于MyISAM（尤其是只读场景）；
- 占用磁盘空间更大（需存储事务日志、索引等）；

90%的业务场景（电商、金融、社交）优先用InnoDB，满足事务、高并发、数据可靠性要求；

2）而MyISAM是为**只读/高并发读、简单查询**设计的轻量级引擎，核心特性：

- **查询速度快**：无事务、锁机制简单，只读场景下性能优于InnoDB；
- **表级锁**：实现简单，开销小（但高并发写时会锁整张表）；
- **占用空间小**：存储结构简单，磁盘利用率高；

**核心劣势**

- **不支持事务**：一旦写入数据崩溃，可能导致数据丢失或损坏；
- **不支持外键**：无法保证数据参照完整性；
- **表级锁**：高并发写场景下（如频繁更新、插入），会出现严重锁等待，性能暴跌；

典型适用场景：仅当表以只读为主、无事务要求时，才考虑MyISAM；

3）Memory引擎将数据**全部存储在内存中**，磁盘仅存储表结构，核心特性有：

- **极致查询速度**：数据在内存中，读写速度远超磁盘引擎；
- **支持哈希索引**：等值查询（如`WHERE id = 1`）性能极高；
- **临时数据存储**：适合存储临时计算结果、会话数据。

**核心劣势**

- **数据易失**：MySQL重启、服务器宕机后，数据全部丢失；
- **不支持事务**：无ACID保证；
- **表级锁**：高并发写性能差；
- **存储限制**：受内存大小限制，无法存储大量数据；

典型适用场景：仅存储临时数据，且能接受数据丢失时使用；

4）Archive引擎聚焦于**高压缩、低访问频率的归档数据**，核心特性：

**核心优势**

- **超高压缩比**：数据压缩后存储，磁盘占用仅为InnoDB的1/10；
- **插入性能高**：适合批量写入归档数据（如日志、历史订单）；
- **支持行级锁**：插入时不阻塞读（但读会阻塞插入）。

**核心劣势**

- **仅支持INSERT和SELECT**：不支持UPDATE/DELETE；
- **无索引**：除主键外无其他索引，查询需全表扫描；
- **不支持事务**：仅保证插入的原子性。

典型适用场景：仅归档、极少查询的历史数据用Archive。

【★★✩✩✩】**MySQL的数据类型有哪些？**

- 整数

  TINYINT、SMALLINT、MEDIUMINT、INT、BIGINT分别占用8、16、24、32、64位存储空间。值得注意的是，INT(10)中的10只是表示显示字符的个数，并无实际意义。一般和UNSIGNED ZEROFILL配合使用才有实际意义，例如，数据类型INT(3)，属性为UNSIGNED ZEROFILL，如果插入的数据为3的话，实际存储的数据为003。

- 浮点数

  FLOAT、DOUBLE及DECIMAL为浮点数类型，DECIMAL是利用字符串进行处理的，能存储精确的小数。相比于FLOAT和DOUBLE，DECIMAL的效率更低些。FLOAT、DOUBLE及DECIMAL都可以指定列宽，例如FLOAT(5,2)表示一共5位，两位存储小数部分，三位存储整数部分。

- 字符串

  字符串常用的主要有CHAR和VARCHAR，VARCHAR主要用于存储可变长字符串，相比于定长的CHAR更节省空间。CHAR是定长的，根据定义的字符串长度分配空间。

  应用场景：对于经常变更的数据使用CHAR更好，CHAR不容易产生碎片。对于非常短的列也是使用CHAR更好些，CHAR相比于VARCHAR在效率上更高些。一般避免使用TEXT/BLOB等类型，因为查询时会使用临时表，造成严重的性能开销。

- 日期

  比较常用的有year、time、date、datetime、timestamp等，datetime保存从1000年到9999年的时间，精度位秒，使用8字节的存储空间，与时区无关。timestamp和UNIX的时间戳相同，保存从1970年1月1日午夜到2038年的时间，精度到秒，使用四个字节的存储空间，并且与时区相关。

  应用场景：尽量使用timestamp，相比于datetime它有着更高的空间效率。



【★★★★✩】**聚簇索引和非聚簇索引的区别？**

**聚簇索引**：**数据和索引存在一起**，叶子节点就是**真实数据行**。

<img src="C:\Users\a2556\AppData\Roaming\Typora\typora-user-images\image-20260603223758531.png" alt="image-20260603223758531" style="zoom:50%;" />

**非聚簇索引**：**索引和数据分开存**，叶子节点存的是**找到数据的地址 / 主键**。

<img src="C:\Users\a2556\AppData\Roaming\Typora\typora-user-images\image-20260603223854607.png" alt="image-20260603223854607" style="zoom:50%;" />

存储方式

- **聚簇索引**：

  索引树的**叶子节点 = 完整的数据行**

  数据物理存储顺序和索引顺序一致

- **非聚簇索引**：

  索引树的**叶子节点 = 主键值 / 数据地址**

  需要**回表查询**才能拿到完整数据

数量限制

- **聚簇索引：一张表只能有一个**
- **非聚簇索引：一张表可以有多个**

查询速度

- 聚簇索引：
  - 范围查询（>、<、between）**极快**
  - 查整行数据**一次到位**
- 非聚簇索引：
  - 精准查询快
  - 查整行需要**回表**，比聚簇索引慢一点

插入 / 更新速度

- **聚簇索引**：慢（因为要维护物理顺序）
- **非聚簇索引**：快

主键关系

- **InnoDB 的主键就是聚簇索引！**

- 如果你没建主键，InnoDB 会自动生成一个隐藏的聚簇索引

**聚簇索引：数据跟索引走，找到索引就是数据，只能一个。**

**非聚簇索引：索引是索引，数据是数据，找到索引还要找数据，能建多个。**



【★★★★✩】**什么是B+树，相比于B树，数据库为什么选择B+树作为底层数据结构？**

MySQL主流存储引擎（InnoDB/MyISAM）的索引底层均基于**B+树**实现，而非数组、链表、哈希表或红黑树。要理解这一点，需先对比常见数据结构的优劣，再拆解B+树的设计精髓，尤其要搞懂：**为什么MySQL选择B+树而非B树？**

B树和B+树同属“多路平衡树”，但B+树针对数据库的“磁盘I/O特性”和“业务查询场景”做了关键优化，二者的核心差异及选择B+树的原因如下：

**数据存储位置：**

- B+树数据仅在叶子节点，B树分散在所有节点

**B树结构：**

- 每个节点（包括非叶子节点、叶子节点）都存储“**索引值+实际数据**”，导致非叶子节点能容纳的索引项数量大幅减少，树高更高；

**B+树结构：**

- 非叶子节点仅存储“索引值+子节点指针”，不存实际数据，**可容纳更多索引项，树高显著降低；**

**核心优势：**

- **树高越低，磁盘I/O次数越少（每次访问节点需一次I/O），查询效率呈指数级提升。**

**范围查询：**

- **B+树叶子节点链表，B树需回溯父节点**

**B树范围查询：**

- 若要查询id BETWEEN 100 AND 200，找到100后需回溯父节点，再遍历子节点，多次I/O且逻辑复杂；

**B+树范围查询：**

- 所有叶子节点按索引值有序串联成双向链表，找到100后，沿链表直接遍历到200即可，无需回溯，范围查询效率提升数倍。

**数据访问一致性：B+树所有查询最终落到叶子节点**

B树：等值查询可能在非叶子节点找到数据并返回，不同查询的终止节点层级不一致，缓存命中率低；

B+树：无论等值查询还是范围查询，最终都要遍历到叶子节点，数据访问路径统一，缓存策略更易优化，且叶子节点存储完整数据（InnoDB）/数据指针（MyISAM），查询结果更完整。

**更新效率：B+树非叶子节点无数据，更新成本更低**

B树：更新数据时，若数据存储在非叶子节点，**需修改该节点，且可能触发节点分裂/合并，影响范围广；**

B+树：数据仅在叶子节点，更新时仅需修改叶子节点，非叶子节点的索引值仅用于导航，无需频繁调整，维护成本更低。

【★★★★✩】**索引失效的场景？**

![image-20260603225309843](C:\Users\a2556\AppData\Roaming\Typora\typora-user-images\image-20260603225309843.png)

【★★★★✩】**SQL慢查询解决方案？**

**定位慢 SQL**

1. **开启 MySQL 慢查询日志，配置`long_query_time`阈值，记录超时 SQL 与未使用索引的 SQL；**
2. 通过`mysqldumpslow、pt-query-digest`工具聚合分析慢日志，筛选高频慢 SQL；
3. 业务侧结合链路监控、数据库性能监控（CPU、IOPS、连接数）锁定问题语句。

**使用 EXPLAIN 分析执行计划，定位根因**

重点关注 explain 关键字段：

1. `type`：避免 ALL 全表扫描，目标做到 ref/range；
2. `key`：**判断是否命中有效索引，NULL 代表无索引**；
3. `rows`：预估扫描行数，行数过大是性能瓶颈；
4. `Extra`：排查 Using filesort（文件排序）、Using temporary（临时表）。

**SQL 与索引优化（常规优化，80% 问题解决）**

（1）索引优化

1. WHERE、JOIN、ORDER BY、GROUP BY 字段合理建立联合索引，遵循**最左前缀原则**；
2. **建立覆盖索引，减少回表开销；**
3. 区分度极低字段不建索引，避免索引失效与索引维护成本。

（2）**改写 SQL 规避索引失效**

规避：隐式类型转换、字段运算、左模糊`%xx`、!=/not in、or 无索引等失效场景；禁止`select *`，只查必要字段。

（3）特殊语句优化

1. 深度分页`limit offset,size`：采用主键筛选延迟关联优化；
2. 多表 JOIN：小表驱动大表，关联字段加索引，减少关联表数量；
3. 优化 GROUP BY、ORDER BY，利用索引避免文件排序。

（4）业务层面优化

**缩短事务时长，杜绝长事务占用锁资源；拆分大 SQL，避免单次大批量查询。**

数据库配置 & 架构层面优化（大数据量兜底）

1. 调参：合理配置`innodb_buffer_pool_size`，缓存热点数据，减少磁盘 IO；优化 redo/undo 相关参数；
2. 读写分离：主库写入，从库分担查询压力；
3. 分库分表：单表超千万数据，做水平 / 垂直拆分，从根源缩减单表数据量。

**优先日志抓 SQL、EXPLAIN 分析，优先索引 + SQL 改写；优化无效再从参数、读写分离、分库分表做架构优化。**

【★★★✩✩】**什么是回表，如何避免回表？**

**什么是回表**

InnoDB 主键是聚簇索引（叶子存整行数据），**普通二级索引叶子只存储主键 ID**。

1. 通过二级索引查到主键；
2. 再拿着主键去聚簇索引树检索完整行数据，这个二次查表操作就是**回表**；

> 举例：idx_name (name)，`select * from t where name='xx'`：先通过 name 索引拿 id → 再用 id 查聚簇拿全字段，发生回表。

```
select * from t where name = 'xx'
```

`select *` 代表：**我要所有字段**（id、name、age、phone、address……）

执行过程：

1. 先去 idx_name(name)找 name='xx'拿到的只有：name + id
2. 但是你要的是 `*` 所有字段索引里没有 age、phone、address 这些字段！
3. 所以必须**拿着 id 再去查主键索引树**，把整行数据取出来。

**结论：** **因为二级索引里不包含你要查询的全部字段，所以必须再查一次主键索引 → 这就叫回表！**

**如何避免回表：建立【覆盖索引】**：

**覆盖索引：索引字段包含 SQL 所有查询字段，查询需要的数据全部在二级索引叶子中，不用再查主键、不用访问聚簇索引。**



【★★★✩✩】**什么是索引下推ICP**

**索引下推（Index Condition Pushdown，ICP）**

就是：**把本来要在 MySQL 服务层做的索引过滤条件，下推到存储引擎层提前过滤，减少回表次数，提升查询速度。**

没有 ICP 之前：

1. 引擎层根据索引查到数据 → **全部回表**
2. 回到服务层 → 再过滤不符合条件的数据
3. 回表次数多，性能差

开启 ICP 之后：

1. 引擎层查索引时，**直接在索引上提前过滤**
2. 不符合条件的直接丢掉，**不回表**
3. 回表次数大大减少

一句话：**ICP = 在索引上就提前过滤，少回表，更快。**

表：user (name, age, city)

索引：idx_name_age (name, age)

SQL：

```sql
select * from user
where name like '张%' and age = 20;
```

**没有 ICP：**

1. 用 `name` 找到所有姓张的人
2. **全部回表**查完整数据
3. 再在服务层过滤 age !=20 的 → 回表次数多

**开启 ICP**

1. 用 `name` 找到姓张的
2. **直接在索引上判断 age 是否 = 20**
3. 不符合的直接丢掉，不回表→ 回表次数大幅减少

索引下推（ICP）是 MySQL 的优化机制，**在遍历二级索引时，将过滤条件下推到存储引擎层提前过滤，不需要先全部回表再过滤，从而减少回表次数和 IO，提升查询效率。**



**【★★★✩✩】什么是MySQL的隔离级别**

MySQL 事务隔离级别是数据库为平衡**并发性能与数据一致性**设计的事务可见性规则，共四级。**其中 InnoDB 默认 RR 可重复读，依靠 MVCC 实现无锁一致性读，解决了脏读、不可重复读，配合间隙锁缓解幻读，是兼顾性能与数据安全的最优选择。**

想要理解隔离级别，首先要明确并发读写会出现的三类数据异常问题：

- **脏读（Dirty Read）**：一个事务读取到了另一个事务**未提交的修改数据**，对方事务回滚后，读取的数据失效，属于严重数据错误。
- **不可重复读（Non-Repeatable Read）**：同一个事务内，**两次查询同一数据结果不一致**。原因是中途其他事务提交了更新/修改操作。
- **幻读（Phantom Read）**：同一个事务内，范围查询的结果数量发生变化，其他事务新增/删除了数据，导致前后查询行数不一致。

SQL 标准定义了四种隔离级别，InnoDB 引擎完全实现，且**后两种依赖 MVCC 实现**，也是面试和工作核心重点：

1. **读未提交（Read Uncommitted，RU）**

最低隔离级别，事务可以读取到其他事务**未提交**的数据。

问题：**存在脏读、不可重复读、幻读**。

场景：几乎不使用，无任何隔离保障，无 MVCC、无锁机制限制。

2. **读已提交（Read Committed，RC）**

事务只能读取其他事务**已经提交**的数据，是大多数数据库的默认级别（非MySQL默认）。

解决：**彻底解决脏读**。

残留问题：存在不可重复读、幻读。

底层实现：基于 MVCC，**每次快照读都会重新生成 ReadView**，可以实时看到其他事务的已提交数据。

3. **可重复读（Repeatable Read，RR）**

**MySQL InnoDB 默认隔离级别**。同一个事务内，多次读取同一数据，结果始终一致。

解决：**解决脏读、不可重复读**。

残留问题：存在轻微幻读（**InnoDB 通过间隙锁极大缓解幻读，但未彻底杜绝**），快照读靠 MVCC 只是**隐藏**新插入数据，物理插入不受限，切当前读立刻暴露幻行。

底层实现：基于 MVCC，**事务内第一次快照读生成 ReadView，后续全程复用**，保证事务视图一致性，实现可重复读。

4. **串行化（Serializable）**

最高隔离级别，完全串行执行，读写全部互斥，事务串行执行，杜绝所有并发问题。

解决：**脏读、不可重复读、幻读全部解决**。

缺点：完全放弃并发性能，读写互相阻塞，性能极差，仅用于金融极致数据安全场景，不用于常规业务。

底层实现：废弃 MVCC，全程加表级锁。

**隔离级别与 MVCC 核心关联**

1、**MVCC 只服务于 RC、RR 两个隔离级别**，这也是 MySQL 并发性能最强的两个级别；

2、RC 和 RR **唯一区别就是 ReadView 的生成时机**，也是两种隔离级别数据表现不同的本质原因；

3、RU 无隔离、串行化用锁，均不使用 MVCC。



**【★★★★✩】什么是MySQL的MVCC（多版本并发控制），讲解一下？**

MVCC是用来解决MySQL在并发读写的过程中，**传统锁机制读写互斥、阻塞严重、并发性能低下**的核心问题。它彻底打破了“读要等写、写要等读”的锁竞争僵局，依托多版本数据快照机制，实现**读不阻塞写、写不阻塞读**的高并发读写能力，是InnoDB引擎支撑高并发业务的核心基石。

InnoDB 存储引擎依靠 **MVCC (Multi-Version Concurrency Control，多版本并发控制)** 实现**不加锁的快照读**，在读写并发场景大幅提升数据库吞吐，是 InnoDB 区别于 MyISAM 的核心特性之一。

- MyISAM：表级共享锁，读写互斥，无 MVCC；
- InnoDB：行锁 + MVCC，**读不加锁、写加行锁**，实现高并发。

MVCC 核心目标：

1. 实现 **RC（读已提交）、RR（可重复读，MySQL 默认隔离）** 两种隔离级别；
2. 避免脏读、不可重复读（RR 下解决）；
3. 减少锁竞争，提升并发读写性能。

> 注意：MVCC 只作用于**快照读 (普通 select)**；`update/delete/select ... for update` 是**当前读**，走行锁，不走多版本。

**前置基础：InnoDB 行隐藏字段**

InnoDB 每条**聚簇索引行**数据固定附带 3 个隐藏列，是 MVCC 版本链的基石：

|    隐藏列     |  长度  |                          作用                           |
| :-----------: | :----: | :-----------------------------------------------------: |
|  `DB_TRX_ID`  | 6 字节 | 修改该行的**最后一个事务 ID**，insert/update 都会赋值； |
| `DB_ROLL_PTR` | 7 字节 |  回滚指针，指向 `undo log` 旧版本数据，串联版本链表；   |
|  `DB_ROW_ID`  | 6 字节 |        无主键时自动生成隐藏主键，和 MVCC 无关。         |

**版本链构成**：同一条数据的所有历史版本，通过 `roll_ptr` 在 undo log 中连成单向链表，新数据在聚簇索引页，旧版本全部存于 undo。

补充：undo log 分类

1. **insert undo**：仅 insert 产生，事务提交后可直接删除，不参与 MVCC；
2. **update undo**：update/delete 产生，**长期保留用于 MVCC 多版本**，老快照还在引用时不能被 purge 线程清理。

**两种读取：快照读 vs 当前读**

1. 快照读（普通 SELECT，MVCC 生效）

```sql
SELECT * FROM user WHERE id=1;
```

不加任何锁，**依靠 MVCC 从版本链选取可见版本**，实现无锁读取，是 MVCC 使用场景。

2. 当前读（加锁读，不走 MVCC）

```sql
-- 所有DML + 锁查询都是当前读
UPDATE user SET name='xxx' WHERE id=1;
DELETE FROM user WHERE id=1;
SELECT * FROM user WHERE id=1 FOR UPDATE; -- 排他锁
SELECT * FROM user WHERE id=1 LOCK IN SHARE MODE; -- 共享锁
```

读取**当前最新已提交数据**，需要加行级锁，访问真实最新行，不回溯 undo 版本链。

> 结论：MVCC = 快照读的实现方案，和当前读无关。

**Read View（读视图）**

Read View 是事务执行快照读瞬间生成的**一致性视图**，用来按事务 ID 规则判定：当前版本对本事务是否可见。

Read View 四个核心字段：

1. `m_ids`：生成视图时，**活跃未提交事务 ID 集合**；
2. `min_trx_id`：`m_ids` 中最小事务 ID；
3. `max_trx_id`：下一个即将分配的事务 ID（所有大于该值的事务视图生成后才启动，不可见）；
4. `creator_trx_id`：**当前生成 ReadView 的事务 ID**。

**可见性判定规则（遍历版本链逐个判断 DB_TRX_ID）**

设待判断数据版本的事务 ID = `trx_id`：

1. `trx_id == creator_trx_id`：自己修改的数据，**可见**；
2. `trx_id < min_trx_id`：事务在视图生成前已提交，**可见**；
3. `trx_id >= max_trx_id`：视图创建后才开启的新事务，**不可见**；
4. `min_trx_id < trx_id < max_trx_id：`
   - `trx_id ∈ m_ids`：事务未提交，**不可见**；
   - trx_id ∉ m_ids：事务已提前提交，可见；不可见就通过 roll_ptr顺着版本链找上一个历史版本，重复判断，直到找到可见版本或链表尾。

**RC 与 RR 隔离下 MVCC 的本质区别：ReadView 生成时机**

> MySQL InnoDB 只有 RC、RR 使用 MVCC；RU（读未提交）无视图，串行化全表锁，废弃 MVCC。

1. RR（可重复读，MySQL 默认）

**同一个事务中，第一次快照读时创建唯一一份 ReadView，后续所有快照读复用该视图**。

- 优势：整个事务视图固定，保证**可重复读**，别的事务中途提交修改，当前事务永远看不到新数据，规避不可重复读；
- 问题：无法读到事务启动后其他事务新提交的数据。

2. RC（读已提交）

**事务中每次执行快照读，都重新生成新 ReadView**。

- 每次查询刷新活跃事务集合，能读到别的事务上一次已经提交的数据；
- 缺点：同一事务多次查询，中间其他事务提交数据会导致结果变化，**不可重复读**。

> 这是 RC/RR 行为差异的根本原因，不是锁的区别，是 ReadView 创建时机不同。

**Delete 在 MVCC 中的逻辑**

InnoDB DELETE 不是物理删除：

1. delete 本质是**update，在行中打上删除标记**，生成新版本写入 undo；
2. 真正物理删除由后台 `purge` 线程异步执行：当没有任何快照视图引用该 undo 版本时，清理废弃 undo 数据，释放空间。

**MVCC 优缺点总结**

优点

1. 快照读无锁，读写互不阻塞，大幅提升并发；
2. RR 隔离下解决脏读、不可重复读；
3. 减少行锁冲突，降低死锁概率。

缺点

1. undo log 占用磁盘，历史版本堆积增加存储开销；
2. 长事务会阻止 purge 清理 undo，导致 undo 膨胀、表空间暴涨；
3. 遍历版本链会带来 CPU 开销，版本链过长查询性能下降。

**即：**

MVCC = **undo log（存历史版本 + 版本链） + ReadView（可见性规则） + 隐藏行字段（串联版本）**，三者协作实现 InnoDB 无锁一致性读。RC/RR 差异仅在于 ReadView 创建时机。

MVCC 由隐藏行字段、undo、ReadView 组成，undo 存历史版本，roll_ptr 串起版本链，ReadView 控制数据可见。RC 每次查询新建视图，RR 全事务共用一份视图。快照读依赖 MVCC 无锁，当前读依赖行锁。



【★★✩✩✩】**讲一下ACID分别代表什么？**

ACID 是**事务四大特性**，保证事务可靠。

1. **原子性 Atomic**

**事务是不可分割最小单元，要么全成功，要么全回滚。**

实现：**undo log**，失败通过 undo 回滚数据。

2. **一致性 Consistency**

事务执行前后，数据整体合法（约束、主键、外键不变）。

原子、隔离、持久性最终共同保障一致性。

3. **隔离性 Isolation**

多个事务并发互不干扰，依靠**事务隔离级别 + MVCC + 行锁**实现，**分 RU/RC/RR/SERIAL。**

4. **持久性 Durability**

事务提交后数据永久落地，宕机不丢失。

实现：**redo log**，刷盘故障后靠 redo 恢复。

一句话总结

**undo 保证原子，redo 保证持久，锁 + MVCC 保证隔离，三者合力实现一致性。**



**【★★★✩✩】什么是数据库设计的三大范式**

三大范式是数据库表设计的一种规范

**第一范式：**数据库每列中都是不可分割的基本数据项； 

**第二范式**：每条记录都可以被独立区分（主键） 

**第三范式**：任何非主属性不依赖于其他非主属性；

第三范式在实际开发中，有些时候会违反，因为虽然会造成数据冗余，但是可以通过建立索引加快数据库查询



**【★★★✩✩】MySQL Innodb是如何避免可重复读隔离级别下幻读问题的？**

**MySQL InnoDB 在 RR 隔离级别下，通过 【MVCC（快照读） + 间隙锁 / Next-Key Lock（当前读）】 两个机制联合避免了幻读。**

**什么是幻读？**

一个事务内，**多次相同的范围查询，结果集条数不一样**（突然多了 / 少了行）。

例如：

第一次查 `where id > 10` → 2 条

第二次查 `where id > 10` → 3 条

这就是**幻读**。

分两种读取情况：

1. **快照读（普通 select）—— 靠 MVCC**

- MVCC 生成**事务一致性视图**
- 事务启动时拍一个 “快照”
- 全程读这个快照，其他事务插入的数据看不见→ 自然不会出现幻读

2. **当前读（select for update /update/delete /insert）—— 靠 Next-Key Lock（临键锁）**

当前读会加锁：

- **Record Lock**：行锁
- **Gap Lock**：间隙锁（锁住两个索引之间的空隙）
- **Record + Gap = Next-Key Lock**

**作用：锁住范围 + 锁住间隙，别的事务无法插入新行 → 从物理上禁止幻行产生。**

**MySQL InnoDB 的 RR 级别：**

- **普通 select 快照读 → MVCC 避免幻读**
- **锁定读 / 更新 / 删除 当前读 → Next-Key Lock（行锁 + 间隙锁）避免幻读**

**所以 InnoDB 在 RR 下，实际上已经解决了幻读。**



什么是 Next-Key Lock？

**行锁 + 间隙锁**，锁住索引记录和索引之间的空隙，防止其他事务插入，从而避免幻读。

RC 为什么不能避免幻读？

- RC 每次 select 都会生成新快照
- 且 RC 没有间隙锁→ 所以会出现幻读

总结

MySQL InnoDB 在可重复读（RR）隔离级别下，通过两种机制避免幻读：

一是**普通查询使用 MVCC 快照读**，事务启动时生成一致性视图，全程读取该版本，其他事务插入的数据不可见；

二是**锁定查询、更新、删除使用当前读**，通过 Next-Key Lock（行锁 + 间隙锁）锁住范围与间隙，禁止其他事务插入新数据。

两者结合，使 RR 级别**完全避免了幻读**。



【★★★★✩】**MySQL 中 redo log、undo log、binlog 的区别与联系**？

要理解 MySQL 中 redo log、undo log、binlog 的区别与联系，首先要明确它们的**设计目标**完全不同——redo log 保障崩溃恢复，undo log 保障事务回滚，binlog 保障数据备份与主从同步。三者协同工作，共同支撑 MySQL 的事务一致性、数据可靠性和高可用性，下面从定义、作用、核心特性、区别及联系五个维度，由浅入深详细讲解。

**一、三者核心概念与区别（核心重点）**

三者的核心差异集中在「所属存储引擎」「设计目标」「数据范围」「写入时机」等方面，先通过分点详细拆解，再用表格总结。

1. **Redo Log（重做日志）**

**定位**：InnoDB 存储引擎独有的日志，属于「物理日志」，记录的是「数据页的物理修改」（比如某数据页的某个偏移量的值修改为多少），与具体的 SQL 语句无关。

**核心作用**：保障 MySQL 崩溃恢复（Crash Recovery），解决「事务已提交但数据未写入磁盘」的问题，避免数据丢失。

举个例子：执行 `update user set age=20 where id=1`，redo log 不会记录这条 SQL，而是记录「id=1 所在的数据页，偏移量 X 的值从 18 改为 20」。即使 MySQL 崩溃，重启后会读取 redo log，将未写入磁盘的物理修改重新执行，确保已提交的事务不会丢失——这就是 InnoDB 「事务持久性」的核心保障。

**核心特性**：

- 循环写入：redo log 有固定大小（如 4 个文件，每个 1GB），写满后会覆盖旧日志（前提是旧日志对应的物理修改已写入磁盘）；
- 写入时机：事务执行过程中，会先将修改写入 redo log buffer（内存），事务提交时，通过 fsync 刷到磁盘（可通过innodb_flush_log_at_trx_commit 配置刷盘策略）；
- 仅 InnoDB 独有：MyISAM 存储引擎没有 redo log，因此不支持崩溃恢复和事务。

**2. Undo Log（回滚日志）**

**定位**：同样是 InnoDB 独有的日志，属于「逻辑日志」，记录的是「事务执行前的原始数据」（或原始操作的逆操作），与 SQL 语句的逻辑对应。

**核心作用**：保障事务的「原子性」，支持事务回滚（ROLLBACK），同时配合 MVCC（多版本并发控制）实现读取已提交（RC）、可重复读（RR）等隔离级别。

举个例子：执行 `update user set age=20 where id=1`（原 age=18），undo log 会记录「id=1 的 age 从 20 改回 18」的逆操作；如果事务执行失败或主动回滚，MySQL 会通过 undo log 执行逆操作，将数据恢复到事务开始前的状态。

**核心特性**：

- 可回滚性：每个事务都会生成对应的 undo log，事务回滚时，依次执行 undo log 中的逆操作；
- 支持 MVCC：undo log 会保留数据的历史版本，当其他事务读取数据时，若数据被当前事务修改，可通过 undo log 读取历史版本，避免脏读、不可重复读；
- 回收机制：undo log 会在事务提交后，由 InnoDB 的 purge 线程异步回收（前提是没有其他事务需要读取该历史版本）。

**3. Binlog（二进制日志）**

**定位**：MySQL 服务器层（不是存储引擎层）的日志，属于「逻辑日志」，记录的是「所有修改数据的 SQL 语句（或其逻辑等价形式）」，与存储引擎无关。

**核心作用**：用于数据备份、主从复制，解决「数据备份恢复」和「集群数据同步」的问题。

举个例子：执行 `update user set age=20 where id=1`，binlog 会记录这条 SQL 语句（或其逻辑编码）；主库将 binlog 发送给从库，从库执行相同的 SQL，实现主从数据一致；备份时，可通过 binlog 恢复指定时间点的数据。

**核心特性**：

- 追加写入：binlog 是按时间顺序追加写入的，不会覆盖旧日志，可通过日志轮转（如按大小、按时间）管理；
- 与存储引擎无关：无论使用 InnoDB、MyISAM 还是其他存储引擎，只要执行修改数据的操作，都会记录 binlog；
- 三种格式：STATEMENT（记录 SQL 语句）、ROW（记录行的修改前后状态，推荐）、MIXED（混合前两种格式）；
- 写入时机：事务提交时，将事务中的所有修改操作一次性写入 binlog（可通过 sync_binlog 配置刷盘策略）。

**4. 三者核心区别总结表**

| 所属层级     | InnoDB 存储引擎层                | InnoDB 存储引擎层           | MySQL 服务器层                   |
| ------------ | -------------------------------- | --------------------------- | -------------------------------- |
| 日志类型     | 物理日志（数据页修改）           | 逻辑日志（逆操作/历史数据） | 逻辑日志（SQL/行修改）           |
| 核心作用     | 崩溃恢复，保障持久性             | 事务回滚，支撑 MVCC         | 备份恢复、主从复制               |
| 写入方式     | 循环写入（固定大小）             | 追加写入，异步回收          | 追加写入（可轮转）               |
| 存储引擎支持 | 仅 InnoDB                        | 仅 InnoDB                   | 所有存储引擎                     |
| 与事务的关系 | 事务提交时刷盘，保障提交后不丢失 | 事务执行时生成，回滚时使用  | 事务提交时写入，记录已提交的修改 |

**二、三者的内在联系（协同工作机制）**

redo log、undo log、binlog 并非独立工作，而是协同支撑 MySQL 的事务和高可用，核心协同场景有 3 个：事务执行与提交、崩溃恢复、主从复制。

**1. 事务执行与提交的协同流程（核心）**

以一个更新事务为例，三者的协同步骤如下（简化版）：

1. 事务开始，执行 update 语句，InnoDB 先读取数据页到内存，修改内存中的数据；
2. 同时，InnoDB 生成 undo log（记录逆操作，用于回滚）和 redo log（记录物理修改，用于崩溃恢复），写入各自的内存缓冲区；同时，MySQL 服务器层会将该修改操作写入 binlog cache（binlog 的内存缓冲区）；
3. 事务提交时，执行「两阶段提交」（保障 redo log 和 binlog 一致性）：第一阶段：将 redo log 标记为「准备状态」，并刷盘（确保 redo log 已持久化）；第二阶段：将 binlog 写入并刷盘，然后将 redo log 标记为「提交状态」，完成事务提交；
4. 事务提交后，undo log 由 purge 线程异步回收，redo log 后续会被覆盖（当对应数据页已写入磁盘）。

关键说明：两阶段提交的目的是避免「redo log 提交但 binlog 未写入」或「binlog 写入但 redo log 未提交」的情况，确保主从复制时数据一致（从库依赖 binlog，主库崩溃恢复依赖 redo log）。

**2. 崩溃恢复时的协同**

当 MySQL 崩溃后，重启时会执行崩溃恢复，此时 redo log 和 undo log 协同工作：

- 第一步：读取 redo log，将所有已标记「准备状态」或「提交状态」的物理修改，重新应用到数据页（恢复已提交的事务，保障持久性）；
- 第二步：读取 undo log，将未提交的事务（redo log 中未标记提交的）执行回滚操作（保障原子性）；
- 此时 binlog 不参与崩溃恢复（binlog 仅用于备份和主从，崩溃恢复依赖 InnoDB 自身的 redo/undo log）。

**3. 主从复制时的协同**

主从复制的核心是 binlog，但 redo log 间接保障了 binlog 的可靠性：

- 主库：事务提交时，通过两阶段提交确保 redo log 和 binlog 同步（binlog 只记录已提交的事务）；
- 从库：通过 IO 线程读取主库的 binlog，写入自身的 relay log（中继日志）；
- 从库 SQL 线程：读取 relay log，执行其中的 SQL 语句，实现数据同步；
- 若主库崩溃，重启后通过 redo log 恢复数据，确保 binlog 中的内容都是已提交的有效数据，避免从库同步错误数据。

**三、总结**

三者的核心定位可概括为：

- undo log：负责事务回滚和 MVCC，保障原子性；
- redo log：负责崩溃恢复，保障持久性；
- binlog：负责数据备份和主从复制，保障高可用。

三者协同，既保障了单实例 MySQL 的事务一致性（ACID），又支撑了集群的高可用（主从复制）和数据可靠性（备份恢复），是 MySQL 核心架构中不可或缺的组成部分。



【★★★✩✩】**MySQL 有了 binlog 来记录SQL操作，那为什么还需要 redo log？**

**因为 binlog 是归档日志，不能做崩溃恢复；redo log 是物理日志，专门保证事务不丢数据 + 提升写入性能。两者定位完全不同，缺一不可。**

 **作用不同**

- redo log：保证事务持久性 + 崩溃恢复数据库崩溃重启后，能把未刷盘的修改重新恢复，保证数据不丢。
- binlog：归档、备份、主从复制不负责崩溃恢复，只记录 “做了什么”，用于数据恢复、主从同步。

**日志类型不同**

- redo log：物理日志记录 “在哪个数据页、偏移量，修改了什么值”。
- binlog：逻辑日志记录 SQL 语句或行变化。

崩溃恢复必须用物理日志，逻辑日志做不到！

**写入机制不同（性能关键）**

- redo log：顺序 I/O、循环写、极快事务提交，只需要写 redo log，不用随机写磁盘。

  > ## 引入 redo log 后的写入流程（WAL 机制）
  >
  > 遵循**先写日志，后写数据**：
  >
  > 1. 修改内存中的缓冲页（Buffer Pool）；
  > 2. 把本次修改记录**顺序追加**到 redo log 文件（顺序 IO，速度快）；
  > 3. 事务提交只需保证 redo log 落盘，即可返回成功；
  > 4. 后台线程（checkpoint）在系统空闲时，批量将内存脏页**批量刷写到磁盘**。
  >
  > 
  >
  > **单次提交只做顺序 IO**
  >
  > redo log 是固定文件、循环顺序写入，全程无频繁寻道，单事务提交耗时大幅降低，支撑高并发。
  >
  > **合并随机 IO，批量刷盘**
  >
  > 后台刷脏页时，会把多个事务的修改合并，一次性写入磁盘。把大量零散随机写，转为**批量顺序写**，进一步放大性能。
  >
  > **日志文件结构天然适配顺序写**
  >
  > redo log 逻辑是环形队列，数据按操作时序依次记录，天生适合顺序读写。

- binlog：追加写，但不能用于实时恢复数据页，它是 Server 层日志，不关心磁盘数据页结构。



MySQL 虽然有 binlog，但 binlog 是逻辑日志，属于 Server 层，只能用于归档和主从复制，**不能做崩溃恢复**。

而 redo log 是 InnoDB 引擎层的物理日志，**记录数据页修改，通过顺序 I/O 提升写入性能，**并且在数据库崩溃时能恢复未刷盘的数据，**保证事务持久性**。

两者定位不同，缺一不可。



**【★★★✩✩】MySQL是如何保证事务的数据一致性的？**

MySQL 保证事务一致性，是通过 **ACID 四大特性** 共同实现的：

- **原子性**靠 undo log 保证事务要么全部成功，要么全部回滚；
- **持久性**靠 redo log 保证提交后数据不丢失；
- **隔离性**靠 MVCC 和锁机制避免并发事务互相干扰；
- **最后通过 redo log + binlog 两阶段提交**保证日志与数据完全一致。这整套机制确保了无论并发、崩溃、异常，事务最终都能保100% 一致。

> ## 1. **原子性（Atomicity）：要么全成功，要么全失败**
>
> - 依靠 **undo log 回滚日志**
> - 执行修改前先记录旧版本
> - 事务出错 → 立即用 undo log 回滚所有操作
> - **保证不会只执行一半，破坏数据一致性**
>
> ## 2. **持久性（Durability）：提交后永久不丢**
>
> - 依靠 **redo log 重做日志**
> - 事务提交先写 redo log（顺序 IO，速度快）
> - 数据库崩溃重启 → 自动根据 redo log 恢复数据
> - **保证已提交数据绝对不丢失**
>
> ## 3. **隔离性（Isolation）：多事务并发不互相干扰**
>
> - 依靠 **MVCC（多版本并发控制）+ 锁机制**
> - 普通查询用 **MVCC 快照读**，互不阻塞
> - 更新 / 删除用 **行锁、间隙锁、Next-Key Lock** 防止并发修改冲突
> - RR 级别下避免**脏读、不可重复读、幻读**
> - **保证并发事务之间不会互相污染数据**
>
> ## 4. **一致性（Consistency）：最终数据一定正确**
>
> - 依靠 **原子性 + 持久性 + 隔离性** 共同保证
> - 再配合 **binlog + redo log 两阶段提交**
> - 确保日志与数据绝对一致，不会出现中间状态
> - **无论崩溃、并发、死锁，数据永远合法、正确**



【★★★✩✩】**MySQL的BufferPool是什么？作用是？**

**Buffer Pool 是 InnoDB 存储引擎在内存中开辟的一块巨大缓存区域，用来缓存磁盘上的数据页和索引页。**

作用：**把磁盘随机读写 → 变成内存读写，极大提升 MySQL 性能。**

1. **缓存热点数据**

   把频繁访问的数据页、索引页放到内存，查询直接读内存，不用走磁盘。

2. **缓存修改操作（写缓冲）**

   数据修改先改内存页，不立即写磁盘，大幅提升写入速度。

3. **配合 Redo Log 实现 WAL 机制**

   写缓冲池 + 写 redo log = 高性能事务。

Buffer Pool 以 **页（Page，默认 16KB）** 为单位管理数据。

**使用LRU 链表实现冷热数据淘汰，优先保留热点页。**

内部包含三大区域：

1. **数据页缓存**：缓存表数据、索引
2. **Free 链表**：管理空闲页
3. **Flush 链表**：管理被修改过的**脏页（Dirty Page）**

**MySQL数据读取流程**：

1. 执行查询 → 先去 **Buffer Pool** 找页
2. **命中（内存有）**：直接返回，速度极快
3. 未命中（内存没有）：
   - 从**磁盘加载数据页**到 Buffer Pool
   - 再返回数据

这就是**缓存机制**。

**数据修改流程（超级重点）**

1. 先修改 **Buffer Pool 中的内存页** → 变成**脏页**（脏页是内存中已完成修改、数据状态正确，只是尚未同步到磁盘的页面。内存与磁盘数据不一致，因此称作脏页。）
2. 记录 **redo log**（保证崩溃不丢）
3. 事务提交成功
4. 后台线程**异步、批量把脏页刷入磁盘**

✅ **优点**：不阻塞业务，写入极快。

**总结**

Buffer Pool 是 InnoDB 的**内存缓存池**，用于缓存磁盘上的数据页和索引页。

读取数据时先查缓存，命中直接返回；修改数据时先改内存脏页，并记录 redo log，后台再批量刷盘。

它把磁盘随机 IO 转为内存操作，是 MySQL 高性能的核心。



### Redis

**【★★★★★】Redis底层的常用数据结构？**

**简单动态字符串 SDS（Simple Dynamic String）**

**所有 String 类型的底层实现**

- 比 C 字符串好：
  - 能**快速获取长度**（O(1)）
  - **二进制安全**（能存图片、视频）
  - 自动扩容，不溢出
- 所有 key、字符串值、协议解析都用 SDS

**双向链表 LinkedList**

**List 的底层之一**

- 前后指针，增删快
- 头尾操作 O (1)
- 元素多的时候用

**压缩列表 ZipList**

**List / Hash / ZSet 的底层之一（小数据时候用）**

- 内存连续、紧凑、省空间
- 元素少、值小的时候自动用
- 缺点：查找慢，修改会连锁更新

**哈希表 Dict（最重要）**

**Redis 整个 keystore 就是一张大哈希表**

- Hash 类型的底层之一
- 拉链法解决哈希冲突
- 渐进式 rehash（不阻塞主线程）

**跳表 SkipList**

**ZSet（有序集合）的底层之一**

- 多层链表，二分查找思想
- 查找 / 插入 / 删除都是 O (logN)
- 支持范围查询，比红黑树好实现

整数集合 IntSet

**Set 集合底层之一**

- 元素全是整数，且数量少时使用
- 内存紧凑，排序好

**小数据用压缩表，大数据用哈希 / 跳表，省内存又高性能**



**【★★★✩✩】跳表的实现**

```cpp
using std::vector;

class SkipList {
private:
    // 跳表最大层的限制
    int n, MAXLVL;
    int cnt;
    // 节点的key以及key的计数和节点拥有多少层指针
    vector<int> key,key_cnt,level;
    // 节点每一层指针指向哪个节点，以及节点每一层指针的长度(底层跨过多少数，左开右闭)
    vector<vector<int>> next_node,len;
public:
    SkipList() = default;
    explicit SkipList(int MAXN) : n(MAXN + 1), key(MAXN + 1), key_cnt(MAXN + 1), level(MAXN + 1) {
        MAXLVL = std::__lg(MAXN) + 1;
    
        next_node.resize(MAXN+1, vector<int>(MAXLVL + 1));
        len.resize(MAXN+1, vector<int>(MAXLVL + 1));
        
        init();
    }   

    void init() {      
        cnt = 1;
        key[cnt] = INT_MIN;
        level[cnt] = MAXLVL; // 初始负无穷节点拥有最大层
    }

    int randLevel() {
        int ans = 1; // 最低都要有一层
        while(rand() / double(RAND_MAX) < 0.5) {
            ans++;
        }
        return std::min(ans, MAXLVL);
    }

    // 当前在i号节点的h层，返回key为num的节点，空间编号是多少
    int find(int i, int h, int num) {
        while(next_node[i][h] != 0 and key[next_node[i][h]] < num) {
            i = next_node[i][h];
        }
        if(h == 1) {
            if(next_node[i][h] != 0 and key[next_node[i][h]] == num) {
                return next_node[i][h];
            } else {
                return 0; // 0 表示未找到
            }
        }
        return find(i, h - 1, num);
    }

    // 当前在i号节点的h层，num增加一个词频
    void addCount(int i, int h, int num) {
        while(next_node[i][h] != 0 and key[next_node[i][h]] < num) {
            i = next_node[i][h];
        }
        if(h == 1) {
            key_cnt[next_node[i][h]]++; // 节点词频统一计入叶子节点
        } else {
            addCount(i, h - 1, num);
        }
        len[i][h]++;
    }

    
	// 当前在i号节点的h层，插入空间编号为j的节点
	// 返回值：从i号节点出发，直到把空间编号为j的节点插入，底层总共有多少数字比key[j]小
	// 返回值很重要，因为上游需要这个信息来改动指针的长度信息
    int addNode(int i, int h, int j) {
        int rightCnt = 0; // 向右经过了多少节点
        while(next_node[i][h] != 0 and key[next_node[i][h]] < key[j]) {
            rightCnt += len[i][h];
            i = next_node[i][h];
        }
        if(h == 1) {
            next_node[j][h] = next_node[i][h];
            next_node[i][h] = j;
            len[j][h] = key_cnt[next_node[j][h]]; // 第一层指针的长度就等对于下一个节点的词频
            len[i][h] = key_cnt[next_node[i][h]];
            return rightCnt;
        } else {
            int downCnt = addNode(i, h - 1, j); // downCnn代表上一层下降到这一层的当前节点的过程中前面略过了多少节点
            if(h > level[j]) {
                // 如果当前层高大于j节点的指针数，那么j节点不会落入当前层高，所以当前层的节点肯定会增加一个略过的节点数
                // 即len[i][h]++;
                len[i][h]++;
            } else {
                next_node[j][h] = next_node[i][h];
                next_node[i][h] = j;
                len[j][h] = len[i][h] + 1 - downCnt - key_cnt[j];
				len[i][h] = downCnt + key_cnt[j];
            }
            return rightCnt + downCnt;
        }
    }

    void add(int num) {
        // 如果已存在则加词频，否则分配空间并插入节点
        if(find(1, MAXLVL, num) != 0) {
            addCount(1, MAXLVL, num);
        } else {
            key[++cnt] = num;
            key_cnt[cnt] = 1;
            level[cnt] = randLevel(); // 摇色子
            addNode(1, MAXLVL, cnt);
        }
    }
    
    // 删除x，如果有多个，只删掉一个
    void removeCount(int i, int h, int num) {
        while(next_node[i][h] != 0 and key[next_node[i][h]] < num) {
            i = next_node[i][h];
        }
        if(h == 1) {
            key_cnt[next_node[i][h]]--; // 节点词频统一计入叶子节点
        } else {
            removeCount(i, h - 1, num);
        }
        len[i][h]--;
    }

    // 在h层从i号节点出发，删除节点编号为j的节点
    void removeNode(int i, int h, int j) {
        if(h < 1) return;        
        while(next_node[i][h] != 0 and key[next_node[i][h]] < key[j]) {
            i = next_node[i][h];
        }
        if(h > level[j]) {
            len[i][h]--;
        } else {
            next_node[i][h] = next_node[j][h];
            len[i][h] += len[j][h] - 1; 
        }
        removeNode(i, h - 1, j);
    }

    // 删除一个数值为num的节点
    void remove(int num) {
       int j = find(1, MAXLVL, num);
       if(j == 0) return; // 如果本来就不存在

        if(key_cnt[j] > 1) {
            removeCount(1, MAXLVL, num);
        } else {
            removeNode(1, MAXLVL, j);
        }
    }

    // 当前在i号节点的h层，查询有多少个数字比num小
    int small(int i, int h, int num) {
        int cnt = 0;
        while(next_node[i][h] != 0 and key[next_node[i][h]] < num) {
            cnt += len[i][h];
            i = next_node[i][h];
        }
        if(h == 1) {
            return cnt;
        } else {
            return cnt + small(i, h - 1, num);
        }
    } 

    // 获取num排名
    int getRank(int num) {
        return small(1, MAXLVL, num) + 1;
    }

    // 当前在i号节点的h层，查询排名第x的key是什么
    int index(int i, int h, int x) {
        int curRank = 0;
        while(next_node[i][h] != 0 and len[i][h] + curRank < x) {
            curRank += len[i][h];
            i = next_node[i][h];
        }
        if(h == 1) {
            return key[next_node[i][h]]; // 注意是i的下一个节点，因为前面找的是 < x的排名的节点
        } else {
            return index(i, h - 1, x - curRank);
        }
    }

    // 查询排名为X的数
    int index(int x) {
        return index(1, MAXLVL, x);
    }

    int pre(int num) {
        return pre(1, MAXLVL, num);
    }

    int pre(int i, int h, int num) {
        while(next_node[i][h] != 0 and key[next_node[i][h]] < num) {
            i = next_node[i][h];
        }
        if(h == 1) {
            // 不存在前驱会返回，key[1] = INT_MIN;
            return key[i];
        } else {
            return pre(i, h - 1, num);
        }
    }

    int post(int num) {
        return post(1, MAXLVL, num);
    }

    int post(int i, int h, int num) {
        while(next_node[i][h] != 0 and key[next_node[i][h]] < num) {
            i = next_node[i][h];
        }
        if(h == 1) {
            if(!next_node[i][h]) return INT_MAX; // 表示没有后继
            if(key[next_node[i][h]] > num) return key[next_node[i][h]];
            // 说明key[next_node[i][h]] == num
            i = next_node[i][h];
            if(!next_node[i][h]) return INT_MAX;
            return key[next_node[i][h]];
        } else {
            return post(i, h - 1, num);
        }
    }

    void clear() {
        std::fill(key.begin(), key.begin() + cnt + 1, 0);
        std::fill(key_cnt.begin(), key_cnt.begin() + cnt + 1, 0);
        std::fill(level.begin(), level.begin() + cnt + 1, 0);
        
        for(int i=1;i<=cnt;i++) {
            std::fill(next_node[i].begin(), next_node[i].begin() + MAXLVL + 1, 0);
            std::fill(len[i].begin(), len[i].begin() + MAXLVL + 1, 0);
        }
        
        init();
    }
};
```



【★★★★✩】**redis的持久化机制？**



**RDB（快照持久化，二进制文件 dump.rdb）**

**原理：全量快照，把某一刻内存所有数据落地二进制文件**

**触发方式**

- 手动：`save`（同步阻塞）、`bgsave`（fork 子进程，后台持久）
- 自动：配置 `save m n`：m 秒内 n 次修改自动 bgsave
- 关机 /flushall 也会触发

**优点**

- 二进制紧凑，体积小，适合冷备份、全量迁移；
- 恢复速度远快 AOF；
- fork 子进程，主线程不阻塞（bgsave）。

**缺点**

- 间隔备份，**两次快照间宕机会丢中间数据**；
- 大数据量 fork 耗时，内存超大时卡顿。



**AOF（日志追加 appendonly.aof，记录写命令）**

**原理：每次写命令追加写入日志，重启回放命令恢复数据**

**刷盘策略 appendfsync**

- always：每条命令落盘，安全、性能差；
- everysec（默认）：每秒刷一次，最多丢 1s 数据，均衡；
- no：交给 OS，不可控，丢数据多。

**AOF 重写 bgrewriteaof**

日志持续膨胀，fork 子进程，根据当前内存数据生成精简新 AOF，删掉冗余指令（多次 set 同一个 key 只留最后一条），减少文件体积。

**优点**

- 数据安全性高，everysec 最多丢 1 秒数据；
- 文本格式，可手动修改修复。

**缺点**

- 文件体积比 RDB 大，恢复慢；
- 频繁刷盘带来 IO 开销。

**混合持久化（Redis4.0+ 默认推荐）**

AOF 重写时，**先把全量数据以 RDB 格式写入 AOF 头部，后续增量命令用 AOF 格式**

恢复：先加载头部 RDB 快照，再回放后面增量 AOF 命令

兼顾：RDB 恢复快 + AOF 丢数据少，生产标配。



## 消息队列



**Kafka 基础介绍**

**Kafka 是什么：** **分布式消息队列，本质是一个持久化的、高吞吐的发布-订阅系统。**

**核心模型：**

```
Producer ──写入──▶ Topic（逻辑队列）
                      │
                   Partition 0  ◀── Consumer Group A
                   Partition 1  ◀── Consumer Group A
                   Partition 2  ◀── Consumer Group B
```

- **Topic**：消息分类，比如 `logs`、`task-events`
- **Partition**：Topic 的物理分片，保证同一 partition 内消息有序
- **Producer**：写消息的一方（各服务器）
- **Consumer Group**：消费消息的一方，组内成员分摊 partition，一条消息只被组内一个消费者处理
- **Offset**：每个消费者在每个 partition 上的消费位置，Kafka 自动管理
- **Broker**：Kafka 的服务器节点，负责接收、存储、复制消息，是 Kafka 集群的核心工作进程。一个集群由多个 Broker 组成，保证高可用。

**关键特性：**

- 消息持久化到磁盘，可按时间或大小配置保留策略
- 吞吐量极高（百万级/秒）
- Consumer Group 天然支持多实例负载均衡
- 消息可回放（重置 offset 重新消费）

**核心作用**

1. **解耦**

   业务服务无需同步处理日志、埋点等附属逻辑，生产、消费端独立迭代，互不影响。

2. **异步处理**

   把非核心操作转为异步，避免 IO / 计算阻塞主流程，提升接口响应速度。

3. **削峰填谷**

   突发流量下暂存消息，平滑流量压力，保护下游消费端不被打垮。

4. **数据缓冲 & 重试**

   消息持久化留存，消费失败可重试，保障数据不丢失。

5. **多分区 / 多副本**

   支持高吞吐、水平扩展，适配分布式集群场景。



**消息队列选型：**

![image-20260608162108779](C:\Users\a2556\AppData\Roaming\Typora\typora-user-images\image-20260608162108779.png)

### Git

【★★★★✩】**Git rebase 和 Git merge的区别？**

- **merge**：**合并分支，保留所有历史**，会生成一个**新的合并提交**，历史是**网状**。
- **rebase**：**变基，重写历史**，把你的提交**移植**到目标分支顶端，历史是**一条直线**。

**详细区别**

1. **历史结构不同**

- **merge**：保留真实提交轨迹，分支分叉 → 合并，**历史清晰但杂乱**。
- **rebase**：把你的提交线性接在目标分支顶端，**历史干净、线性、美观**。

2. **是否生成新提交**

- **merge**：**一定会生成一个 merge commit**（如 Merge branch 'dev'）。
- **rebase**：**不会生成合并提交**，只是移动、重写你的提交历史。

3. **底层原理**

- **merge**：把两个分支的末端**合并**。
- **rebase**：找到两个分支的公共祖先，把你分支上的新提交**临时保存**，先同步目标分支最新代码，再把你的提交**一个个重放上去**。

**merge 优点**

- **安全**，不修改历史
- 适合**公共分支、多人协作分支**（如 develop、master）

**rebase 优点**

- **历史干净线性**
- 无多余合并节点
- 适合**自己的私有分支、功能分支**

**merge 缺点**

- 历史杂乱，出现大量菱形合并结构
- 查看日志不方便

**rebase 缺点**

- **会重写历史**，绝对不能用在 **公共分支 / 别人也在用的分支**
- 一旦强推会覆盖别人代码

**使用规则**

**merge 用于：公共分支（master、develop）合并**

**rebase 用于：自己的私有功能分支同步最新代码**

**总结：**

1. **merge 是合并，保留完整历史，生成合并提交，历史呈网状。**
2. **rebase 是变基，将当前分支的提交移植到目标分支顶端，重写历史，历史呈线性。**
3. **merge 安全适合公共分支，rebase 整洁但只能用于私有分支。**
4. **严禁在公共分支上执行 rebase。**

# 手撕

## 算法相关

### 排序

**归并排序，稳定 O(NlogN)**

**快速排序，不稳定，完全有序取端点为pivot时最坏O(n^2)，理想状态下时间复杂度O(NlogN)**

```cpp
void quicksort(std::vector<int>& arr, int l, int r) {
    if(l >= r) return;
    int pivot = arr[l];
    int i = l, j = r;
    while(i < j) {
        while(i < j and arr[j] >= pivot) j--;
        while(i < j && arr[i] <= pivot) i++;
        std::swap(arr[i],arr[j]);
    }
    // 此时i == j, arr[i]的位置就是pivot的位置
    std::swap(arr[i],arr[l]);
    quicksort(arr, l, i - 1);
    quicksort(arr, i + 1, r);
}

void merge(std::vector<int>& arr, std::vector<int>& temp, int l, int r) {
    if(l >= r) return;
    int mid = (l + r) >> 1;
    int i = l, j = mid + 1;
    int ptr = l;
    while(i <= mid and j <= r) {
        if(arr[i] <= arr[j]) {
            temp[ptr++] = arr[i];
            i++;
        } else {
            temp[ptr++] = arr[j];
            j++;
        }
    }
    while(i <= mid) {
        temp[ptr++] = arr[i];
        i++;
    }
    while(j <= r) {
        temp[ptr++] = arr[j];
        j++;
    }
    for(int _ = l;_ <= r; _++) arr[_] = temp[_];
}

void mergesort(std::vector<int>& arr, std::vector<int>& temp, int l, int r) {
    if(l < r) {
        int mid = (l + r) >> 1;
        mergesort(arr, temp, l, mid);
        mergesort(arr, temp, mid + 1, r);
        merge(arr, temp, l, r);
    } 
}

int main() {
    std::ios::sync_with_stdio(false), std::cin.tie(0), std::cout.tie(0);
    int n;
    std::cin >> n;
    std::vector<int> arr(n+1);
    for(int i=1;i<=n;i++) {
        std::cin >> arr[i];
    }
    auto temp = arr;
    mergesort(arr,temp,1,n);
    // quicksort(arr, 1, n);
    for(int i=1;i<=n;i++) {
        std::cout << arr[i] << ' ';
    }
    std::cout << std::endl;
}
```

### 链表

**链表题目，一定要判断初始值不为nullptr**



**合并有序链表**

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
public:
    ListNode* mergeTwoLists(ListNode* list1, ListNode* list2) {

        if(list1 == nullptr) return list2;
        if(list2 == nullptr) return list1;

        ListNode* merged = (list1->val > list2->val ? list2 : list1);
        ListNode* head = merged;
        if(merged == list1) list1 = list1->next;
        else list2 = list2->next;
        while(list1 != nullptr or list2 != nullptr) {
            if(list1 == nullptr) {
                merged->next = list2;
                break;
            } else if(list2 == nullptr) {
                merged->next = list1;
                break;
            }
            ListNode* lo = (list1->val > list2->val ? list2 : list1);
            if(lo == list1) merged->next = list1, list1 = list1->next;
            else merged->next = list2, list2 = list2->next;
            merged = merged -> next;
        }   

        return head;
    }
};
```

**LRU Cache**

```cpp
class LRUCache {
private:
    struct Node {
        int key, val;
        Node* prev;
        Node* next;
        Node(int key_ = 0, int val_ = 0) : key(key_), val(val_) {}
    };

    Node* dummy;
    int capacity_;
    unordered_map<int, Node*> key2Node;

    void del(Node* node) {
        node -> prev -> next = node -> next;
        node -> next -> prev = node -> prev;
    }

    void push_front(Node* node) {
        node -> next = dummy -> next;
        node -> next -> prev = node;
        node -> prev = dummy;
        dummy -> next = node;
    }

    Node* get_node(int key) {
        auto it = key2Node.find(key);
        if(it == key2Node.end()) return nullptr;
        Node* node = it->second; // 无需解引用，Node*本身就是指针
        del(node);
        push_front(node);
        return node;
    }

public:
    LRUCache(int capacity) : capacity_(capacity), dummy(new Node()) {
        dummy -> next = dummy;
        dummy -> prev = dummy;
    }

    
    int get(int key) {
        auto node = get_node(key);
        return node ? node -> val : -1;
    }
    
    void put(int key, int value) {
        Node* node = get_node(key);
        if(node) {
            node -> val = value;
            // push_front(node); 此处已经在get_node中执行
            return;
        }
        node = new Node(key, value);
        key2Node[key] = node;
        push_front(node);
        if(key2Node.size() > capacity_) {
            Node* last = dummy -> prev;
            del(last);
            key2Node.erase(last->key);
            delete last;
        }
    }
};

/**
 * Your LRUCache object will be instantiated and called as such:
 * LRUCache* obj = new LRUCache(capacity);
 * int param_1 = obj->get(key);
 * obj->put(key,value);
 */
```

环形链表II

判断是否有环，如有返回入环第一节点

空间复杂度O（N）的做法是打vis标签

以下是O（1）空间的做法

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode(int x) : val(x), next(NULL) {}
 * };
 */
class Solution {
public:
    ListNode *detectCycle(ListNode *head) {
        if(head == nullptr or head->next == nullptr) return nullptr;
        ListNode* slow = head;
        ListNode* fast = head;
        while(fast != nullptr) {
            if(fast->next == nullptr) return nullptr;
            // 快慢指针判有无环，若相遇则有环
            fast = fast->next->next;
            slow = slow->next;
            // 有环的情况下
            if(fast == slow) {
                ListNode* ptr = head;
                while(ptr != slow) {
                    ptr = ptr->next;
                    slow = slow->next;
                }
                return ptr;
            }
        }
        return nullptr;
    }
};
```



### 滑动窗口

滑动窗口与双指针不同，一般是固定右边界一直移动（for循环），随后在循环体内，判断窗口的大小等性质是否符合窗口概念，符合则计算贡献，不符合则收缩左边界，同时减去旧贡献加上新贡献。

**[找到字符串中所有字母异位词](https://leetcode.cn/problems/find-all-anagrams-in-a-string/)** 【中等】

给定两个字符串 `s` 和 `p`，找到 `s` 中所有 `p` 的 **异位词** 的子串，返回这些子串的起始索引。不考虑答案输出的顺序。

```cpp
class Solution {
public:
    vector<int> findAnagrams(string s, string p) {
        if(p.size() > s.size()) return {};
        std::unordered_map<char,int> lack; // 当前字符在窗口内还缺少几个
        int totallack = p.size(); // 初始窗口总缺少字符数
        int totalextra = 0; // 初始窗口内多余的字符数
        int n = s.size() - 1; 
        for(auto &ch : p) lack[ch]++; // 初始缺目标串的所有字符
        std::vector<int> ans; // 存储答案
        for(int r=0,l=0;r<=n;r++)
        {   
            lack[s[r]]--; // 右边界扩张
            if(lack[s[r]] < 0) totalextra++;
            else totallack--;
            // 如果窗口长度过大则收缩左边界，同时修改贡献
            while(r - l + 1 > p.size()) {
                lack[s[l]]++; 
                if(lack[s[l]] <= 0) totalextra--;
                else totallack++;
                l++;
            }
            // 最后如果该窗口符合答案，记录贡献
            if(!totallack and !totalextra) ans.emplace_back(l); 
        }
        return ans;
    }
};
```

[无重复字符的最长子串](https://leetcode.cn/problems/longest-substring-without-repeating-characters/)

给定一个字符串 `s` ，请你找出其中不含有重复字符的 **最长 子串** 的长度。

```cpp
class Solution {
public:
    int lengthOfLongestSubstring(string s) {
        std::unordered_map<char,int> cnt;
        int ans = 0;
        int n = s.size() - 1;
        for(int r=0,l=0;r<=n;r++) {
            cnt[s[r]]++;
            while(cnt[s[r]] > 1) {
                cnt[s[l++]]--;
            }
            ans = std::max(ans, r - l + 1);
        }
        return ans;
    }
};
```

[滑动窗口最大值](https://leetcode.cn/problems/sliding-window-maximum/)

大根堆模拟

```cpp
class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        vector<int> ans;
        int n = nums.size();
        std::multiset<int,greater<int>> st; 
        // 注意不能对st.end()解引用，所以我们要降序排st，从而可以解引用st.begin();
        for(int r=0,l=0;r<n;r++)
        {
            st.insert(nums[r]);
            while(r - l + 1 > k) {
                st.erase(st.find(nums[l++]));
            }
            if(r - l + 1 == k) ans.emplace_back(*st.begin());
        }
        return ans;
    }
};
```

**三数之和**

```cpp
class Solution {
public:
    vector<vector<int>> threeSum(vector<int>& nums) {
        sort(nums.begin(), nums.end());
        vector<vector<int>> ans;
        int n = nums.size();
        for(int i = 0; i < n; ++i) {
            // i++后如果和前面元素相同，需要去重
            if(i > 0 and nums[i] == nums[i-1]) continue;
            int l = i + 1, r = n - 1;
            while(l < r) {
                int sum = nums[i] + nums[l] + nums[r];
                if(sum == 0) {
                    // 先去重，移动到当前相同元素块的边缘
                    while(l < r and nums[l] == nums[l+1]) l++;
                    while(l < r and nums[r] == nums[r-1]) r--;
                    ans.push_back({nums[i], nums[l], nums[r]});
                    l++, r--; // 统计答案后，收缩指针
                } 
                else if (sum < 0) l++; 
                else r--;
            }
        }
        return ans;
    }
};
```

单调队列

双端队列，从左至右单调递减，存储下标

```c++
class Solution {
public:
    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
        int n = nums.size();
        deque<int> q;
        vector<int> ans;
        for(int r=0;r<k;r++) 
        {
            while(!q.empty() and nums[r] >= nums[q.back()]) q.pop_back();
            q.push_back(r);
        }
        ans.push_back(nums[q.front()]);
        for(int r=k;r<n;r++)
        {
            while(!q.empty() and nums[r] >= nums[q.back()]) q.pop_back();
            q.push_back(r);
            while(q.front() <= r - k) q.pop_front(); // 如果当前最大值下标不在窗口内则弹出
            ans.push_back(nums[q.front()]);
        }
        return ans;
    }
};
```

### 数组

和为k的子数组个数

```cpp
class Solution {
public:
    int subarraySum(vector<int>& nums, int k) {
        int n = nums.size();
        int pre = 0;
        std::unordered_map<int,int> mp;
        mp[pre]++; // 注意前缀和为0的次数为1（空集）
        int ans = 0;
        // pre[r] - pre[l-1] == k -> pre[l-1] == pre[r] - k
        for(int r=0;r<n;r++)
        {
            pre += nums[r];
            ans += mp[pre - k];
            // 此时的pre为r，所以要等贡献算完之后再++，否则会影响pre[l-1]个数的计算，因为k有可能为0 
            mp[pre]++;
        }
        return ans;
    }
};
```

螺旋矩阵

```cpp
class Solution {
public:
    vector<int> spiralOrder(vector<vector<int>>& matrix) {
        vector<int> ans;
        int n = matrix.size();
        int m = matrix[0].size();
        vector<vector<bool>> vis(n,vector<bool>(m));
        // 1 R 2 D 3 L 4 U

        auto check = [&](int x,int y) -> bool {
            return x >=0 and x <= n-1 and y >= 0 and y <= m-1;
        };

        auto dfs = [&](auto &&self, int x, int y, int dir) -> void {
            
            if(!vis[x][y])
                ans.push_back(matrix[x][y]);

            vis[x][y] = true;

            if(ans.size() == n * m) return;

            if(dir == 1) {
                if(check(x,y+1) and !vis[x][y+1]) self(self,x,y+1,1);
                else if(!check(x,y+1) or vis[x][y+1]) self(self,x,y,2);
            }
            else if(dir == 2) {
                if(check(x+1,y) and !vis[x+1][y]) self(self,x+1,y,2);
                else if(!check(x+1,y) or vis[x+1][y]) self(self,x,y,3); 
            }
            else if(dir == 3) {
                if(check(x,y-1) and !vis[x][y-1]) self(self,x,y-1,3);
                else if(!check(x,y-1) or vis[x][y-1]) self(self,x,y,4); 
            }
            else if(dir == 4) {
                if(check(x-1,y) and !vis[x-1][y]) self(self,x-1,y,4);
                else if(!check(x-1,y) or vis[x-1][y]) self(self,x,y,1); 
            }

        };
        dfs(dfs,0,0,1);
        return ans;
    }
};
```

旋转矩阵

```cpp
class Solution {
public:
    void rotate(vector<vector<int>>& matrix) {
        int n = matrix.size();
        int m = matrix[0].size();
        // 先按对角线反转
        for(int i=0;i<n;i++) {
            for(int j=i;j<m;j++) {
                swap(matrix[i][j], matrix[j][i]);
            }
        }
        // 最后将每行反转
        for(int i=0;i<n;i++) reverse(matrix[i].begin(),matrix[i].end());
        // 顺时针旋转90°
        
        //若为逆时针旋转90°，则先进行每行反转，再进行按对角线反转即可
    }
};
```

最大矩形面积，单调栈

```cpp
class Solution {
    struct Node {
        int id,next,pre;
    };
public:
    int largestRectangleArea(vector<int>& heights) {
        stack<Node> stk;
        int n = heights.size();
        vector<int> pre(n,-1), next(n,n);
        for(int i=0;i<heights.size();i++) {
            next[i] = n;
            pre[i] = -1;
        }
        
        for(int i=0;i<heights.size();i++) {
            while(!stk.empty() and heights[stk.top().id] > heights[i]) {
                next[stk.top().id] = i;
                stk.pop();
            } 
            stk.push({i,-1});
        }   

        while(!stk.empty()) stk.pop();
		// 注意还要求左边第一小
        for(int i=heights.size()-1;i>=0;i--) {
            while(!stk.empty() and heights[stk.top().id] > heights[i]) {
                pre[stk.top().id] = i;
                stk.pop();
            } 
            stk.push({i,-1});
        }   

        int ans = 0;

        for(int i=0;i<heights.size();i++) {
            int nxt = next[i];
            int pr = pre[i];
            ans = max(ans,(nxt-pr-1) * heights[i]);
        }
        return ans;
    }
};
```



### List Collection

**相交链表**

法一：vis标记，略

法二：双指针

```cpp
class Solution {
public:
    ListNode *getIntersectionNode(ListNode *headA, ListNode *headB) {
        if(headA == nullptr or headB == nullptr) return nullptr;
        ListNode* ptrA = headA;
        ListNode* ptrB = headB;
        while(ptrA != ptrB) {
            ptrA = ptrA == nullptr ? headB : ptrA->next;
            ptrB = ptrB == nullptr ? headA : ptrB->next;
        }
        return ptrB;
    }
};
```

**链表反转**

维护三个指针遍历（previous, current, next）

注意链表起点的next也要反转成nullptr

```cpp
class Solution {
public:
    ListNode* reverseList(ListNode* head) {
        if(head == nullptr) return head;
        ListNode* prev = nullptr;
        ListNode* next = head->next;
        ListNode* curnode = head;
        while(curnode != nullptr) {
            if(next == nullptr)
                std::tie(prev,next,curnode,curnode->next) = std::tuple(curnode,nullptr,nullptr,prev);
            else  
                std::tie(prev,next,curnode,curnode->next) = std::tuple(curnode,next->next,next,prev);
        }
        return prev;
    }
};
```

**环形链表**

仅需判断是否有环

```cpp
class Solution {
public:
    bool hasCycle(ListNode *head) {
        if(head == nullptr or head->next == nullptr) return false;
        ListNode* slow = head;
        ListNode* fast = head->next;
        while(slow != fast) {
            if(fast == nullptr or fast->next == nullptr) return false;
            slow = slow->next;
            fast = fast->next->next;
        }
        return true;
    }
};
```

**环形链表 II**

返回环的起始节点

```cpp
class Solution {
public:
    ListNode *detectCycle(ListNode *head) {
        if(head == nullptr or head->next == nullptr) return nullptr;
        ListNode* slow = head;
        ListNode* fast = head;
        while(fast != nullptr) {
            if(fast->next == nullptr) return nullptr;
            fast = fast->next->next;
            slow = slow->next;
            if(fast == slow) {
                ListNode* ptr = head;
                while(ptr != slow) {
                    ptr = ptr->next;
                    slow = slow->next;
                }
                return ptr;
            }
        }
        return nullptr;
    }
};
```

**循环链表**

使用std::tie + std::tuple可以实现打包赋值

```cpp
class Solution {
public:
    ListNode* rotateRight(ListNode* head, int k) {
        if(!head) return nullptr;

        int len = 0;
        ListNode* tail;
        ListNode* h = head;
        while(h != nullptr) {
            len++;
            tail = h;
            h = h->next;
        }
        k %= len;
        if(k == 0) return head;
        int temp = 0;
        h = head;
        while(h!=nullptr) {
            temp++;
            if(temp == len - k) {
                ListNode* newhead = h->next;
                std::tie(tail->next,h->next) = std::tuple(head, nullptr);
                return newhead;
            }
            h = h->next;
        }
        return nullptr;
    }
};
```

### Tree

**字典树**

```cpp
class Trie {
public:
    struct Node {
        std::unordered_map<char,int> leaf; // 注意leaf是映射下标
        bool have;
        Node() : have(false) {}
    };

    std::vector<Node> trie;
    int idx;

    Trie() {
        trie.emplace_back(Node());
        idx = 0;
    }
    
    void insert(string word) {
        int u = 0;
        for(auto &ch : word) {
            if(trie[u].leaf.find(ch) == trie[u].leaf.end()) {
                trie.emplace_back(Node());
                trie[u].leaf[ch] = ++idx;
            }
            u = trie[u].leaf[ch];
        }
        trie[u].have = true;
    }
    
    bool search(string word) {
        int u = 0;
        for(auto &ch : word) {
            if(trie[u].leaf.find(ch )== trie[u].leaf.end()) return false;
            u = trie[u].leaf[ch];
        }
        return trie[u].have;
    }
    
    bool startsWith(string prefix) {
        int u = 0;
        for(auto &ch : prefix) {
            if(trie[u].leaf.find(ch )== trie[u].leaf.end()) return false;
            u = trie[u].leaf[ch];
        }
        return true;
    }
};

```

**镜像二叉树判断**

```cpp
class Solution {
public:
    bool isSymmetric(TreeNode* root) {
        if (root == nullptr) return true;
        bool isSym = true;
        auto dfs = [&](auto&& self, TreeNode* a, TreeNode* b) -> bool {
            // 终止条件1：两个节点都为空，是镜像
            if (a == nullptr && b == nullptr) return true;
            // 终止条件2：其中一个为空，另一个不为空，不是镜像
            if (a == nullptr || b == nullptr) return false;
            // 终止条件3：值不相等，不是镜像
            if (a->val != b->val) return false;
            // 递归判断：a的左和b的右镜像，且a的右和b的左镜像
            return self(self, a->left, b->right) && self(self, a->right, b->left);
        };
        isSym = dfs(dfs, root->left, root->right);
        return isSym;
    }
};
```

**递归查询LCA**

```cpp
class Solution {
public:
    TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) {
        auto lca = [&](auto&& self, TreeNode* u) -> TreeNode* {
            if(u == nullptr) return nullptr;
            if(u == p or u == q) return u;

            auto left = self(self,u->left);
            auto right = self(self,u->right);

            if(left and right) return u;
            else if(!left) return right;
            else return left; 
        };
        return lca(lca,root);
    }   
};
```

**层序遍历**

每一层在队列中的都是同一层的节点，依次处理

```cpp
class Solution {
public:
    vector<vector<int>> levelOrder(TreeNode* root) {
        vector<vector<int>> ans;
        queue<TreeNode*> q;
        if(root) q.push(root);
        while(!q.empty()) {
            int sz = q.size();
            ans.push_back({});
            for(int i=1;i<=sz;i++) {
                ans.back().push_back(q.front()->val);
                if(q.front()->left) q.push(q.front()->left);
                if(q.front()->right) q.push(q.front()->right);
                q.pop();
            }
        }
        return ans;
    }
};
```

**有序数组转BST**

```cpp
class Solution {
public:
    TreeNode* sortedArrayToBST(vector<int>& nums) {
        return build(nums, 0, nums.size() - 1);
    }
    TreeNode* build(vector<int>& nums, int l, int r) {
        if(l > r) return nullptr;
        int mid = (l + r) / 2;
        TreeNode* root = new TreeNode(nums[mid]);
        root->left = build(nums, l, mid - 1); 
        root->right = build(nums, mid + 1, r); 
        return root;
    }
};
```

**二叉树的直径**

```cpp
class Solution {
public:
    int ans = 0;

    int dfs(TreeNode* root) {
        if (!root) return 0;
        int left = dfs(root->left);   // 左子树深度
        int right = dfs(root->right); // 右子树深度
        ans = max(ans, left + right); // 更新直径（经过当前点的最长路径）
        return max(left, right) + 1;  // 返回当前子树高度（给父亲用，所以还要+1）
    }

    int diameterOfBinaryTree(TreeNode* root) {
        dfs(root);
        return ans;
    }
};
```

### Search

获取所有子集，有重复元素

```cpp
class Solution {
public:
    vector<vector<int>> subsets(vector<int>& nums) {
        vector<vector<int>> ans;
        int n = nums.size();
        // 先排序，让重复元素相邻
        sort(nums.begin(), nums.end());
        // 跳过重复元素
        auto dfs = [&](auto&& self, vector<int> temp, int idx) -> void {
            ans.push_back(temp); // 每进入一层递归，就把当前子集加入结果
            for(int i=idx;i<n;i++) {
                // 跳过重复元素（i > idx 保证是同一层的重复，不是上一层的）
                if(i > idx and nums[i] == nums[i-1]) continue;
                temp.push_back(nums[i]);
                self(self, temp, i + 1); // 下一层从 i+1 开始，避免重复选同一元素
                temp.pop_back(); // 回溯
            }
        };
        dfs(dfs, {}, 0);
        return ans;
    }
};

```

### DP

打家劫舍II

环形房屋，相邻的房屋不能偷，问最大收益

```cpp
class Solution {
public:
    int rob(vector<int>& nums) {
        int n = nums.size();
        if(nums.empty()) return 0;
        if(nums.size() == 1) return nums[0];
        vector<int> dpsteal(n), dpnotsteal(n);
        // steal nums[0], then cannot steal nums[n-1];
        dpsteal[0] = nums[0];
        dpsteal[1] = nums[0]; // 注意此时是偷到i能获得的最大收益，所以dpsteal[1]同样继承dpsteal[0]的答案
        for(int i=2;i<n-1;i++) dpsteal[i] = max(dpsteal[i-1], nums[i] + dpsteal[i-2]);
        // do not steal nums[0], then can steal nums[1] -> nums[n];
        dpnotsteal[0] = 0;
        dpnotsteal[1] = nums[1];
        for(int i=2;i<n;i++) dpnotsteal[i] = max(dpnotsteal[i-1], nums[i] + dpnotsteal[i-2]);
        return max(*max_element(dpsteal.begin(),dpsteal.end()),*max_element(dpnotsteal.begin(),dpnotsteal.end()));
    }
};
```

跳跃游戏I & II

最多往后跳nums[i]步，问能够到达最后一个格子

```cpp
class Solution {
public:
    bool canJump(vector<int>& nums) {
        if(nums.size() == 1) return true;
        int n = nums.size();
        for(int i=0;i<n;i++) {
            if(nums[i] == 0) {
                bool flag = false;
                for(int j=0;j<i;j++) {
                    if(nums[j] + j > i or nums[j] + j == n - 1) {
                        flag = true;
                    }                    
                }
                if(!flag) return false;
            }
        }
        return true;
    }
};
```

问到达最后一个格子的最小步数

```cpp
class Solution {
public:
    int jump(vector<int>& nums) {
        int n = nums.size();
        vector<int> dp(n, INT_MAX); 
        dp[0] = 0;
        for(int i=1;i<n;i++) {
            for(int j=0;j<i;j++) {
                if(nums[j] + j >= i) dp[i] = min(dp[j] + 1, dp[i]);
            }
        }
        return dp[n-1];
    }
};
```

完全平方数

```cpp
class Solution {
public:
    int numSquares(int n) {
        vector<int> dp(n+1);
        for(int i=1;i<=n;i++) {
            int lo = INT_MAX;
            for(int j=1;j*j<=i;j++) {
                lo = min(lo, dp[i - j * j] + 1);
            }
            dp[i] = lo;
        }
        return dp[n];
    }
};
```

最大乘积和

```cpp
class Solution {
public:
    int maxProduct(vector<int>& nums) {
        vector<int> dphi(nums.begin(),nums.end()), dplo(nums.begin(),nums.end());
        for(int i=1;i<nums.size();i++) {
            dphi[i] = max(dphi[i-1] * nums[i], max(nums[i], dplo[i - 1] * nums[i]));
            dplo[i] = min(dplo[i-1] * nums[i], min(nums[i], dphi[i - 1] * nums[i]));
        }
        return *max_element(dphi.begin(),dphi.end());
    }
};
```

01背包，能否凑成特定和

```cpp
class Solution {
public:
    bool canPartition(vector<int>& nums) {
        int sum = accumulate(nums.begin(), nums.end(), 0);
        if (sum & 1) return false;
        int target = sum / 2;  
        vector<bool> dp(target + 1, false);
        dp[0] = true; 
        for (int num : nums) {
            for (int j = target; j >= num; j--) {
                dp[j] = dp[j] or dp[j - num];
            }
        }
        return dp[target];
    }
};
```

俄罗斯套娃信封问题

```cpp
class Solution {
public:
    int maxEnvelopes(vector<vector<int>>& envelopes) {
        int n = envelopes.size();
        vector<pii> arr;
        arr.reserve(n);
        for(auto& e : envelopes) {
            // if (e[0] > e[1]) swap(e[0], e[1]);
            arr.emplace_back(e[0], e[1]);
        }
        // 第一维相等时，第二维降序（避免同宽度信封互相嵌套）
        sort(arr.begin(), arr.end(), [](const pii& a, const pii& b) {
            if (a.first != b.first) return a.first < b.first;
            return a.second > b.second;
        });
        vector<int> heights;
        heights.reserve(n);
        for (auto& p : arr) heights.push_back(p.second);
        return lengthOfLIS(heights);
    }
    
    int lengthOfLIS(const vector<int>& nums) 
    {
        if(nums.empty()) return 0;
        vector<int> tails;
        tails.reserve(nums.size());
        for(int num : nums) 
        {
            auto it = lower_bound(tails.begin(), tails.end(), num);
            if(it == tails.end()) tails.push_back(num);
            else *it = num;
        } 
        return (int)tails.size();
    }
};
```



## 工程相关

### My_Vector

```cpp
template<class T>
class Vector {
private:

    T* start_;
    T* next_;
    T* end_;

public:

    Vector() : start_(nullptr), next_(nullptr), end_(nullptr) {};
    ~Vector() {
        if(start_) delete []start_;
        start_ = next_ = end_ = nullptr;
    }

    T& operator[](size_t pos) {
        return *(start_ + pos);
    }

    size_t size() {
        return next_ - start_;
    }

    size_t cap() {
        return end_ - start_;
    }

    bool empty() {
        return next_ == start_;
    }

    void reserve(size_t new_cap) {
        size_t cursize = size();

        T* next_start = new T[new_cap];

        memcpy(next_start, start_, cursize * sizeof(T));

        delete []start_;

        start_ = next_start;
        next_ = start_ + cursize;
        end_ = start_ + new_cap;
    };

    void push_back(T val) {
        if(end_ == next_) {
            size_t new_cap = cap() == 0 ? 1 : cap() * 2;
            reserve(new_cap);
        }
        *next_ = val;
        next_++;
    }

    void pop_back() {
        if(empty()) {
            return;
        }
        next_--;
        // 不能delete，需要交给析构函数统一调用 delete[]
    }
};

```

### 单例模式

单例模式是一种**创建型设计模式**，核心目标是：**保证一个类在程序生命周期内只有一个实例，并提供一个全局访问点**。

常见的使用场景：配置管理器、日志管理器、数据库连接池等（这类对象只需要一个实例，多实例会导致资源浪费或逻辑冲突）。

**实现要点**

- 私有化构造函数 / 拷贝构造 / 赋值运算符，防止外部创建

- 提供静态方法获取唯一实例

- 线程安全（C++11 后静态局部变量天生线程安全）

  - 函数内的静态局部变量初始化时，若多个线程同时进入初始化语句，只有一个线程完成初始化，其他线程阻塞等待初始化完成。

    因此 `static Singleton UniqueInstance;` 是天然线程安全的，无需手动加锁（如 `std::mutex`）

```cpp
#include <iostream>
#include <mutex>

// 单例基类
// 后续使用需将Singleton置为友元类
template<typename T>
class Singleton {
protected:
    Singleton() = default;
    ~Singleton() = default;
    Singleton(const Singleton&) = delete;
    Singleton& operator=(const Singleton&) = delete;
public:
	static T& getInstance() {
		static T instance;
        return instance;
    }
};


class Singleton {
private:
    // 私有化构造函数，禁止外部创建
    Singleton() { 
        std::cout << "Singleton 实例创建" << std::endl; 
    }
    // 传统懒汉式（new出的单例，因为 new 强制要求生命周期由程序员管理
    // 要手动delete）退出时不会调用虚构函数，需要手动释放资源，否则会造成内存泄漏
    // ~Singleton() {
    //   std::cout << "Release Resources!" << std::endl;
    // };
    // 禁用拷贝和赋值
    Singleton(const Singleton&) = delete;
    Singleton& operator=(const Singleton&) = delete;


public:
    // 全局访问点
    static Singleton& getInstance() {
        // C++11起，静态局部变量初始化是线程安全的
        static Singleton instance;
        return instance;
    }
    // 示例方法
    void doSomething() {
        std::cout << "单例对象执行操作" << std::endl;
    }
    
};
// 测试
int main() {
    // 获取唯一实例
    Singleton& s1 = Singleton::getInstance();
    Singleton& s2 = Singleton::getInstance();
    // 验证是同一个实例
    std::cout << "&s1: " << &s1 << std::endl;
    std::cout << "&s2: " << &s2 << std::endl;
    s1.doSomething();
    return 0;
}
```

## 场景题

### Top-K 问题



## 项目问题

### OxyTeamTasks分布式任务分配协同系统

【★★★✩✩】**项目中的Token是如何参与身份验证的？**

**登录时：服务器生成 token → 返回给客户端**

[GateServer/LogicSystem.cpp:126](vscode-webview://1vpsetqqd1khfd5fti5svpfol81j3kbfdvpd021abdgp62mliej6/GateServer/LogicSystem.cpp#L126) 的 `/user_login` 处理器中：

```
客户端 POST /user_login {email, password}
  → GateServer 调 UMSServer.Login() 验证账号密码
  → GateServer 调 StatusServer.AllocatePushServer(uid)
      → StatusServer 生成 UUID token，存入 Redis (utoken_<uid> → token)
      → 返回 {host, port, token}
  → GateServer 把 token 放进 JSON 响应返回给客户端
```

[GateServer/LogicSystem.cpp:143](vscode-webview://1vpsetqqd1khfd5fti5svpfol81j3kbfdvpd021abdgp62mliej6/GateServer/LogicSystem.cpp#L143) `result["token"] = pushRsp.token();`

**客户端接收 → 持久化存储**

[Client/src/stores/user.js:68-83](vscode-webview://1vpsetqqd1khfd5fti5svpfol81j3kbfdvpd021abdgp62mliej6/Client/src/stores/user.js#L68-L83) 的 `login()` 方法收到响应后：

```js
token.value = res.token        // 存 Pinia store
persist('token', res.token)    // 存 localStorage
uid.value = res.uid
persist('uid', res.uid)        // uid 也存了
```

**后续每次请求 → 自动带 token**

[Client/src/api/request.js:14-23](vscode-webview://1vpsetqqd1khfd5fti5svpfol81j3kbfdvpd021abdgp62mliej6/Client/src/api/request.js#L14-L23) 的 axios 请求拦截器在**每次请求前**自动执行：

```js
const token = localStorage.getItem('token')  // 从 localStorage 读出
const uid = localStorage.getItem('uid')
config.headers.Authorization = `Bearer ${token}`  // 加到请求头
config.headers['X-User-Id'] = uid
```

这是 axios 拦截器机制 — 每个发出的 HTTP 请求都会经过这个函数，无需每个 API 调用单独写。所以前端调用任何 `user.js` 里的函数（如 `listAllUsers()`、`updateTeamInfo()` 等）时，token 和 uid 已经自动带在请求头里了。

**服务端验证**

[GateServer/HttpConnection.cpp:126-182](vscode-webview://1vpsetqqd1khfd5fti5svpfol81j3kbfdvpd021abdgp62mliej6/GateServer/HttpConnection.cpp#L126-L182) 的 **`authenticateRequest_()` 从请求头提取 token 和 uid → gRPC 调 `StatusServer.ReportLogin()` → StatusServer 查 Redis 比对 → 匹配放行 / 不匹配返回 401。**

------

简单说：**登录时写入 localStorage → axios 拦截器每次自动读取并附加到请求头 → GateServer 中间件校验**。用户登录后，浏览器 localStorage 持久化存储 token，所有后续请求自动携带，无需手动处理。

【★★★✩✩】**你基于IOContextPool实现的Reactor模型，是只用于处理IO吗，有没有将业务和IO解耦？**

有的，除了IOContextPool，我还使用了异步任务池AsyncTaskPool：

**IOContextPool**：N 个 `io_context`，每个跑在独立线程上。`getIOContext()` 轮询分配，新连接的 socket 注册到不同的 io_context。这是主从 Reactor 的**从 Reactor 池**，处理的是**网络 I/O 事件**（WebSocket async_read/async_write）。

**AsyncTaskPool**：1 个 `io_context`，N 个线程跑同一个 io_context。（多线程跑同一个iocontext是线程安全的）通过 `post(task)` 提交任意任务。这是个**通用线程池**，用于**非 I/O 的后台任务**（比如数据库查询、业务逻辑计算）。

简单说：

- IOContextPool → 网络连接的 I/O 多路复用，一个连接绑定一个 io_context
- AsyncTaskPool → 后台任务队列，任务之间无关联，执行完就结束



## 个人问题 & 反问

### 自我介绍

