但经测试现在有如下的问题：
1. 虽然现在可以实现多指派人下的任务状态独立，但是在队长的队伍信息模块，队员任务的显示模块，其中的任务状态还是全局的任务状态，而不是各自独立的任务状态，也就是说任何一人完成了任务，在这个面板上还是会更新其他未完成任务的人的任务状态为已完成，请修复这个漏洞
2. 在任务管理模块，如果是多指派人的情况，有一个人完成任务，这个任务状态就显示为已完成，现在请在任务状态下新加一个完成人数，即显示为“已完成[0/X]”"进行中[0/Z]""未开始[0/Y]"，其中 X + Y + Z = 总指派人，然后再此模块下可以点击进去该任务的一个统计视图，分别显示具体的”已完成“，”进行中“，”未开始“状态下的用户是谁。

请在完成后作简单的code review，仔细审查是否都完成了上述要求，并作简单测试，并在docs目录中追加本次debug的修复文档，修复文档格式参考已有的debug md文档格式
