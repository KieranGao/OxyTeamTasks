### 自动登录逻辑

目前来看，如果用户token未过期的话，打开页面不会触发到登录页面而是直接进入到dashboard，这个检验token如果未过期就自动跳转的逻辑是在客户端实现的吗

对，完全是客户端实现的。分两层：

第一层：路由守卫（只检查 localStorage 有无 token）
router/index.js 里的 beforeEach 守卫：


const token = localStorage.getItem('token')
if (token) {
    next()  // 有 token → 直接进 dashboard
} else {
    next('/login')  // 没 token → 跳登录页
}
不验证 token 是否有效，只看 localStorage 里有没有这个字符串。

第二层：WebSocket 登录时服务端验证
页面加载后，MainLayout onMounted 重连 WebSocket，发送 login 消息：


客户端 → WebSocket → { type: "login", uid, token }
                          ↓
PushServer → StatusServer ReportLogin
                          ↓
                    Redis GET utoken:<uid>
                          ↓
              token 有效 → 返回 login_rsp（成功）
              token 过期 → 返回 login_rsp（error: 1007）
                          ↓
客户端 login_rsp handler:
    error ≠ 0 →
        disconnect()
        清除 localStorage
        "登录已过期，请重新登录"
        跳转 #/login
两层配合

用户打开页面
  │
  ├── localStorage 有 token？
  │   ├── 否 → 跳登录页（第一层）
  │   └── 是 → 进 dashboard
  │            → WebSocket 重连 → 发 login
  │            → 服务端验证 token
  │                ├── 有效 → 正常使用
  │                └── 过期 → 清 localStorage → 跳登录页（第二层）
所以用户看到的是：有 token 就直接进页面，但如果 token 真的过期了，WebSocket 连接建立后会立刻被踢回登录页。中间有一个短暂的"进了页面但还没验证"的窗口期。

### 登录握手的消息的发送时机在哪？是在登录成功，客户端拿到pushserver 地址的时候吗
对。完整时序：


① 客户端 POST /user_login（HTTP）
   → GateServer → UMSServer 验证密码 → StatusServer 分配 PushServer
   → 返回 { host: "192.168.x.x", port: "8890", token: "uuid-xxx", uid: 1, ... }

② 客户端收到响应，拿到 PushServer 地址和 token

③ 客户端建立 WebSocket 连接
   new WebSocket("ws://192.168.x.x:8890")

④ ws.onopen 触发 → 发送登录握手消息
   ws.send({ type: "login", uid: 1, token: "uuid-xxx" })

⑤ PushServer 收到 → loginHandler
   → StatusServer ReportLogin 验证 token
   → 连接数 +1
   → 返回 login_rsp（未读数 + 离线消息）
对应前端代码 pushClient.js：


ws.onopen = () => {
    sendMessage({
        type: 'login',
        uid,     // 从 HTTP 登录响应拿到的
        token,   // 从 HTTP 登录响应拿到的
    })
}
所以 WebSocket 的 login 消息依赖 HTTP 登录先成功——没有 HTTP 登录就拿不到 PushServer 地址和 token，WebSocket 连接都建不起来。HTTP 登录负责"验身份 + 分配节点"，WebSocket 登录负责"绑定会话 + 推送离线消息"。

### 再讲一下，用户会话中的写队列，是怎么设计的，为什么这么设计，为了解决什么问题
发送队列设计
核心结构

class Session {
    std::queue<std::string> send_que_;   // 消息队列
    std::mutex send_mtx_;                // 保护队列的互斥锁
};
发送流程

void Session::send(const std::string& msg) {
    std::lock_guard<std::mutex> lock(send_mtx_);
    if (send_que_.size() > MAX_SENDQUE) return;  // 队列满，丢弃
    bool empty = send_que_.empty();
    send_que_.push(msg);
    if (empty) doWriteLocked();  // 队列从空变非空，发起写
}

void Session::doWriteLocked() {        // 调用前必须持有 send_mtx_
    const std::string& msg = send_que_.front();
    ws_.async_write(net::buffer(msg), [self](beast::error_code ec, size_t) {
        self->onWrite(ec, ...);
    });
}

void Session::onWrite(beast::error_code ec, size_t) {
    std::lock_guard<std::mutex> lock(send_mtx_);
    send_que_.pop();                    // 写完，弹出队头
    if (!send_que_.empty()) doWriteLocked();  // 队列非空，继续写下一条
}
解决的问题
Boost.Beast 的 WebSocket 不支持并发 async_write。

如果两个线程同时对同一个 socket 调用 async_write，会导致未定义行为（数据交错、崩溃）。但项目中多个地方可能同时给同一个用户发消息：


线程1: pushMessageToUser(uid=1, "新任务")     → session->send(...)
线程2: pushMessageToUser(uid=1, "打卡提醒")   → session->send(...)
线程3: pushMessageToUser(uid=1, "任务完成")   → session->send(...)
没有队列的话，三个 async_write 同时发起，数据会乱。

队列如何解决

线程1: lock → push("新任务")    → 队列空，发起 async_write → unlock
线程2: lock → push("打卡提醒")  → 队列非空，只入队不写    → unlock
线程3: lock → push("任务完成")  → 队列非空，只入队不写    → unlock

async_write 完成:
  onWrite → lock → pop("新任务") → 队列非空 → doWrite("打卡提醒") → unlock

async_write 完成:
  onWrite → lock → pop("打卡提醒") → 队列非空 → doWrite("任务完成") → unlock

async_write 完成:
  onWrite → lock → pop("任务完成") → 队列空 → 不写 → unlock
同一时刻只有一个 async_write 在执行，其他消息排队等待。FIFO 保证消息顺序。

为什么是队列而不是锁
如果用锁直接保护 async_write：


void send(const std::string& msg) {
    std::lock_guard<std::mutex> lock(mtx_);
    ws_.write(net::buffer(msg));  // 同步写，阻塞直到完成
}
问题：

同步阻塞：write() 会阻塞当前线程直到数据发完，如果网络慢或对端收得慢，线程被卡住
无法并发接收：如果发送和接收在同一线程，阻塞发送期间无法处理接收
队列 + async_write 的好处：

非阻塞：send() 只做入队，立刻返回
异步写：async_write 不阻塞线程，写完回调通知
背压控制：队列满（>1000）时丢弃消息，防止内存无限增长
丢弃策略

if (send_que_.size() > MAX_SENDQUE) return;  // 静默丢弃
队列上限 1000 条。用户长时间不在线或网络极慢时，队列会满。丢弃的是实时推送消息，不是持久化消息——消息已经在 MySQL 和 Redis 里了，用户下次登录还能看到。

这是合理的取舍：宁可丢实时推送，也不能让内存无限增长。

### 现在项目中，跨节点转发的逻辑是如何实现的？

核心代码在 PushGrpcServiceImpl::pushMessageToUser，四步判断：


① 写 MySQL（持久化）
② 写 Redis（热缓存 + 未读计数）
③ 查本地 uid_sessions_，用户在本节点？→ WebSocket 直推
④ 不在本节点 → 查 Redis pushnode:<uid> → gRPC 转发到目标节点
第③④步的具体逻辑：


// 3. 尝试本地推送
if (g_main_server) {
    auto session = g_main_server->getSessionByUid(uid);
    if (session) {
        session->send(ws_msg);  // 用户在本节点，直接推
        return true;
    }
}

// 4. 用户不在本节点，跨节点转发
std::string node_val;
if (RedisManager::getInstance().get("pushnode:" + uid_str, node_val)) {
    // 解析 "host:ws_port:grpc_port"
    parseNodeValue(node_val, host, ws_port, target_grpc);

    // 目标节点不是自己才转发（防环）
    if (target_grpc != g_grpc_port) {
        auto stub = PushNodeManager::getInstance().getStub(host, target_grpc);
        stub->PushToUser(&ctx, fwd_req, &fwd_rsp);  // gRPC 调用目标节点
    }
}
目标 PushServer 收到 PushToUser RPC 后，再走一遍本地推送（第③步），此时一定命中，WebSocket 推给用户。

最多一跳：A → B，不会 A → B → C，因为 pushnode:<uid> 精确记录了用户所在节点。



### 如果用户离线，登录时离线消息是怎么拉取的？

离线消息不是客户端主动拉取的，而是登录响应里直接带回来的。

loginHandler 里的逻辑：


// 1. 未读计数
long unread_count = MySQLManager::getInstance().getUnreadCount(uid);
rtvalue["unread_count"] = unread_count;

// 2. 离线消息（Redis 最近 20 条）
std::vector<std::string> cached_msgs;
RedisManager::getInstance().lrange("msgs:" + uid_str, 0, 19, cached_msgs);

Json::Value offline(Json::arrayValue);
for (const auto& msg_json : cached_msgs) {
    // 解析每条消息 JSON
    offline.append(msg);
}
rtvalue["offline_messages"] = offline;

// 3. 返回给客户端
session->send(rtvalue.toStyledString());
客户端收到的 login_rsp：


{
    "type": "login_rsp",
    "error": 0,
    "uid": 1,
    "name": "OxyTheCrack",
    "unread_count": 5,
    "offline_messages": [
        { "msg_type": "task_new", "title": "新任务: xxx", "is_read": 0, "created_at": "..." },
        { "msg_type": "checkin_remind", "title": "打卡提醒", "is_read": 0, "created_at": "..." }
        // ... 最多 20 条
    ]
}
客户端处理：


// pushClient.js 内置 handler
onMessage('login_rsp', (data) => {
    if (data.unread_count > 0) unreadCount = data.unread_count
    if (data.offline_messages) {
        for (const msg of data.offline_messages) {
            // 加入消息列表
        }
    }
})
完整时序

用户 B 离线期间：
  A 发消息给 B → pushMessageToUser(B)
    → MySQL INSERT
    → Redis LPUSH msgs:<uid=B>（缓存最近 50 条）
    → WebSocket 推送 → B 不在线 → 跳过

用户 B 登录：
  WebSocket 连接 → 发 login 消息
    → PushServer loginHandler
    → Redis LRANGE msgs:<uid=B> 0 19（取最近 20 条）
    → login_rsp 带着离线消息返回
    → 客户端展示
所以离线消息走的是 Redis 热缓存（最近 50 条存 Redis，登录时取前 20 条返回）。如果用户离线超过 50 条消息的量，更早的消息在 MySQL 里，需要用户翻页（GetMessages page>1）才会查 MySQL。