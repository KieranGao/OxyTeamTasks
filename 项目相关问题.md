# OxyTeamTasks 项目深挖面试题

**基于简历技术要点，面试官视角逐条追问**

---

## 一、微服务架构 & gRPC 通信

### Q1. 你说后端拆分了 6 个微服务，为什么要拆成这么多服务？拆分的依据是什么？

**追问方向：** 面试官想看你是否理解微服务的取舍，而不是为了拆而拆。

**标准答案：**

拆分依据是**业务边界和部署独立性**：

1. **用户管理（UMSServer）**：注册、登录、角色权限，是独立的业务域
2. **任务管理（TaskServer）**：任务 CRUD、待办、打卡，数据模型独立
3. **邮箱验证（MailerServer）**：第三方 SMTP 依赖，用 Node.js 实现更合适
4. **状态调度（StatusServer）**：心跳、负载均衡、日志聚合，是基础设施层
5. **推送集群（PushServer × 2）**：WebSocket 长连接，需要水平扩展
6. **网关（GateServer）**：HTTP→gRPC 协议翻译，统一鉴权

好处：各服务独立部署、独立扩缩容（PushServer 可以多实例），故障隔离（MailerServer 挂了不影响任务管理）。

代价：服务间通信开销（gRPC 调用延迟）、分布式事务复杂度、部署运维成本。

**如果面试官追问"什么时候不该拆"：** 项目早期、团队小、业务边界不清晰时，单体更合适。微服务是为了应对团队规模和业务复杂度，不是技术炫技。

---

### Q2. gRPC 连接池是怎么实现的？为什么用连接池而不是每次新建连接？

**追问方向：** 面试官想看你对连接池原理和 gRPC 底层的理解。

**标准答案：**

连接池实现：
- 预创建 N 个 `grpc::Channel` + `Stub`，存入 `std::queue`
- 获取时 `cond_.wait_for(3s)` 阻塞等待，超时返回 nullptr
- 归还时 `notify_one` 唤醒等待线程
- 用 `Defer`（RAII）保证异常时也能归还 stub

为什么用连接池：
1. **gRPC Channel 创建开销大**：涉及 TCP 连接建立、HTTP/2 握手，每次新建会严重影响性能
2. **Stub 复用**：Stub 不是线程安全的，多线程不能共享同一个 Stub，所以需要池化
3. **限流保护**：池大小限制了并发 RPC 数量，保护下游服务不被打爆

**如果面试官追问"Stub 为什么不是线程安全的"：** gRPC 的 Stub 内部维护了请求状态（context、序列化缓冲区），多线程共享会导致数据竞争。但 Channel 是线程安全的，可以多个 Stub 共享同一个 Channel。

---

### Q3. Defer 机制是什么？为什么不用智能指针？

**追问方向：** 面试官想看你对 RAII 的理解深度。

**标准答案：**

`Defer` 是一个轻量级 RAII 类，构造时接受一个 lambda，析构时执行：

```cpp
class Defer {
public:
    Defer(std::function<void()> func) : func_(func) {}
    ~Defer() { func_(); }
private:
    std::function<void()> func_;
};
```

用法：`Defer defer([&stub, this](){ rpc_pool_->returnStub(std::move(stub)); });`

为什么不用 `unique_ptr` 自定义删除器：
- `unique_ptr` 的删除器类型是模板参数，无法捕获外部变量（lambda 有状态）
- `Defer` 更灵活，可以在任意作用域注册清理逻辑
- 本质上和 Go 的 `defer`、Java 的 `try-with-resources` 是同一思想

**如果面试官追问"Defer 有什么缺点"：** 依赖程序员手动在每个调用点写 Defer，容易遗漏。更安全的做法是封装一个 `ScopedStub` 类，构造时获取，析构时归还，但需要模板支持不同 Service 类型。

---

## 二、WebSocket 推送 & 网络模型

### Q4. WebSocket 的发送队列是怎么设计的？为什么需要发送队列？

**追问方向：** 面试官想看你对异步网络编程的理解。

**标准答案：**

发送队列设计：
- `std::deque<std::string> send_queue_` + `std::mutex send_mtx_`
- `send()` 时加锁，队列满（>1000）则丢弃消息
- 只有队列从空变为非空时才发起 `async_write`（避免并发写）
- `onWrite` 回调中 pop_front，如果队列非空则继续 `doWriteLocked`

为什么需要发送队列：
1. **Boost.Beast 的 WebSocket 不支持并发 async_write**：如果两个线程同时调用 async_write，会导致未定义行为
2. **保证消息顺序**：队列 FIFO 保证消息按发送顺序到达客户端
3. **背压控制**：队列满时丢弃消息，防止内存无限增长

**如果面试官追问"丢弃消息合适吗"：** 对于实时推送场景，丢弃旧消息比阻塞发送更合理。用户看到的是"最新状态"，中间状态丢了影响不大。但如果是聊天消息，就不应该丢弃，应该缓存到 Redis 等用户上线后补推。

---

### Q5. 主从 Reactor 模型是怎么实现的？为什么这样设计？

**追问方向：** 面试官想看你对高性能网络模型的理解。

**标准答案：**

实现：
- **主 Reactor**：`acceptor_` 绑定在主 `io_context` 上，只负责 `async_accept`
- **从 Reactor**：`IOContextPool` 创建 N 个 `io_context`，每个跑在独立线程上
- **连接分发**：`acceptor_` 的回调中，从池里 round-robin 取一个 `io_context`，把新连接的 socket 注册上去

```
主 Reactor (acceptor_)
    │  async_accept
    ▼
从 Reactor 1 (io_context_1) ── 处理连接 A, B, C 的读写
从 Reactor 2 (io_context_2) ── 处理连接 D, E, F 的读写
从 Reactor 3 (io_context_3) ── 处理连接 G, H, I 的读写
```

为什么这样设计：
1. **accept 和 I/O 分离**：accept 是高频操作，不应该阻塞 I/O 处理
2. **多核利用**：每个 Reactor 跑在独立线程上，充分利用多核 CPU
3. **避免锁竞争**：同一个连接的所有 I/O 操作都在同一个 io_context 上，不需要加锁

**如果面试官追问"work_guard 是什么"：** `executor_work_guard` 防止 `io_context.run()` 在没有待处理任务时返回。如果没有 work_guard，io_context 空闲时线程会退出，连接就无法处理了。

---

### Q6. 跨节点消息推送的完整流程是怎样的？最多经过几次跳转？

**追问方向：** 面试官想看你对分布式消息路由的理解。

**标准答案：**

完整流程（用户 A 在 PushServer1，给用户 B 发消息，用户 B 在 PushServer2）：

1. **TaskServer** 调用 `pushMessageToUser(B, msg)`
2. **写 MySQL**：`INSERT INTO messages`（持久化，保证不丢）
3. **写 Redis**：`pushMessageAtomic()` 原子执行 LPUSH + LTRIM + EXPIRE + INCR（分布式锁保护）
4. **本地查找**：`getSessionByUid(B)` → 不在本节点
5. **查 Redis**：`GET pushnode:{B}` → 拿到 `PushServer2 的 host:grpc_port`
6. **gRPC 转发**：通过 `PushNodeManager` 获取 PushServer2 的 Stub，调用 `PushToUser`
7. **PushServer2 收到**：本地查找 B 的 session，WebSocket 推送

**最多一次跳转**：TaskServer → PushServer1 → PushServer2。不会出现 PushServer2 再转发到 PushServer3 的情况，因为 `pushnode:{uid}` 精确记录了用户所在的节点。

**如果面试官追问"如果 B 不在线怎么办"：** 步骤 4 和 6 都找不到 session，消息已经在步骤 2-3 落盘到 MySQL 和 Redis。用户 B 上线时，loginHandler 会从 Redis 拉取离线消息补推。

---

## 三、负载均衡 & 调度

### Q7. 线段树负载均衡的具体实现是怎样的？push_up 过程中怎么记录最小值对应的服务器？

**追问方向：** 面试官想确认你真的写过这个算法，不是背的。

**标准答案：**

线段树节点结构：
```cpp
struct Node {
    int l, r;      // 区间范围
    int min;       // 区间最小值
    int minidx;    // 最小值对应的服务器索引
};
```

push_up 过程：
```cpp
void push_up(int p) {
    if (tree[lc].min <= tree[rc].min) {
        tree[p].min = tree[lc].min;
        tree[p].minidx = tree[lc].minidx;  // 左子树更小，取左子树的索引
    } else {
        tree[p].min = tree[rc].min;
        tree[p].minidx = tree[rc].minidx;  // 右子树更小，取右子树的索引
    }
}
```

分配流程：
1. `queryMinidx(1, server_cnt_)` → 找到连接数最小的服务器索引
2. `getVal(minIdx)` → 获取当前连接数
3. `updateVal(minIdx, minCon + 1)` → 连接数 +1

断开连接时：
- `updateVal(cs.id, getVal(cs.id) - 1)` → 连接数 -1

**如果面试官追问"为什么不用最小堆"：** 最小堆也能 O(1) 取最小值、O(log N) 更新。但线段树支持区间查询，如果以后要加"按区域分配"（比如只分配给华东区的节点），线段树更灵活。另外线段树的实现更直观，竞赛选手更熟悉。

---

### Q8. 心跳超时后怎么处理离线节点？INT_MAX 的设计有什么好处？

**追问方向：** 面试官想看你对容错处理的思考。

**标准答案：**

心跳超时处理：
```cpp
if (now - last_heartbeat > 30) {
    info.status = "offline";
    SegTree_->updateVal(idx, INT_MAX);  // 连接数设为最大值
}
```

节点恢复时：
```cpp
if (info.status == "offline" && now_reconnected) {
    info.status = "online";
    SegTree_->updateVal(idx, 0);  // 连接数重置为 0
}
```

INT_MAX 的好处：
1. **自然排除**：`queryMinidx` 永远不会选中 INT_MAX 的节点，不需要特殊判断逻辑
2. **不影响其他节点**：只是把这个节点的权重设为最大，其他节点的分配不受影响
3. **恢复简单**：节点回来后直接设为 0，它会立刻开始接收新连接（连接数最少）

**如果面试官追问"30 秒超时会不会误判"：** 会。如果网络抖动导致心跳延迟，节点会被短暂标记为 offline。但恢复后会立刻重置为 0，影响很小。如果要更精确，可以用"连续 N 次未收到心跳才标记 offline"的策略。

---

## 四、Redis 应用

### Q9. 消息推送时为什么先写 MySQL 再写 Redis？反过来行不行？

**追问方向：** 面试官想看你对数据一致性的理解。

**标准答案：**

**MySQL 先写的原因：**
- MySQL 是持久化存储，是数据的**唯一真相来源（Single Source of Truth）**
- 如果先写 Redis 再写 MySQL，Redis 写成功但 MySQL 写失败，消息就丢了
- 先写 MySQL 保证：即使 Redis 和 WebSocket 都失败，消息也不会丢

**不能反过来的原因：**
- Redis 是缓存层，可能丢数据（内存不足淘汰、重启未持久化）
- 如果先 Redis 后 MySQL，MySQL 失败时 Redis 里有脏数据，而且 Redis 的 LPUSH + INCR 不支持回滚
- 先 MySQL 后 Redis：MySQL 失败直接返回错误，Redis 不会有脏数据

**如果面试官追问"MySQL 写成功但 Redis 写失败怎么办"：** 消息已经持久化在 MySQL 了，Redis 缓存失败不影响消息不丢失。用户上线时会从 MySQL 拉取完整消息列表（GateServer 的 `/msg_list` 直接查 MySQL）。Redis 只是热缓存，不是唯一存储。

---

### Q10. pushMessageAtomic 的 Lua 脚本做了什么？为什么用 Lua 而不是多次 Redis 调用？

**追问方向：** 面试官想看你对 Redis 原子操作的理解。

**标准答案：**

Lua 脚本内容：
```lua
local msg_key = KEYS[1]       -- msgs:{uid}
local counter_key = KEYS[2]   -- unread:{uid}
local msg = ARGV[1]           -- 消息 JSON
local max_msgs = tonumber(ARGV[2])  -- 最大消息数（50）
local ttl = tonumber(ARGV[3])       -- TTL（7天）

redis.call('LPUSH', msg_key, msg)
redis.call('LTRIM', msg_key, 0, max_msgs - 1)
redis.call('EXPIRE', msg_key, ttl)
redis.call('INCR', counter_key)
return 1
```

为什么用 Lua：
1. **原子性**：Redis 单线程执行 Lua 脚本期间，不会被其他命令打断
2. **减少网络往返**：4 条命令合并为 1 次 EVAL，减少 3 次网络往返
3. **避免中间状态**：如果用多次调用，LPUSH 和 LTRIM 之间可能有其他线程读到不一致的数据（比如消息数暂时超过 50）

**如果面试官追问"LTRIM 为什么是 0 到 max_msgs-1"：** LPUSH 是左推入，新消息在列表头部。LTRIM 0 到 49 保留前 50 条（最新的 50 条），丢弃更早的消息。

---

### Q11. 踢人机制的完整流程是怎样的？getAndDeleteKick 为什么要用 Lua 脚本？

**追问方向：** 面试官想看你对跨服务协调和竞态条件的处理。

**标准答案：**

完整流程：
1. 用户 B 在新设备登录 → GateServer 调用 `AllocatePushServer(B)`
2. StatusServer 加锁 `lock:login:{B}` → 读旧 token → 生成新 token → 写入 Redis
3. 如果旧 token 存在且不同 → 写 `kick:{B} = old_token`（TTL 60 秒）
4. 释放锁
5. PushServer 收到 B 的 WebSocket 连接 → 调用 `getAndDeleteKick(B)`
6. 如果有 kick 标记 → 找到 B 的旧 session → 发送 `kicked` 消息 → 关闭旧连接
7. 注册新 session

为什么用 Lua：
```lua
local val = redis.call('GET', KEYS[1])
if val then redis.call('DEL', KEYS[1]) end
return val
```
- 如果用 GET + DEL 两步，GET 之后锁可能过期，另一个 PushServer 也 GET 到了同一个 kick 标记
- 两个 PushServer 都执行 DEL，第二个 DEL 失效但不影响逻辑
- 但更严重的是两个 PushServer 都发送了 kicked 消息给同一个用户
- Lua 脚本保证 GET + DEL 原子执行，kick 标记只被消费一次

**如果面试官追问"kick 标记 60 秒 TTL 够吗"：** 足够。用户登录到 WebSocket 连接建立通常在几秒内完成。60 秒是安全余量。如果超过 60 秒才连接，kick 标记已过期，旧用户不会被踢——但旧用户的 token 已经失效，下次请求会鉴权失败。

---

## 五、Kafka 日志系统

### Q12. 为什么用 Kafka 做日志上报？直接用 gRPC 不行吗？

**追问方向：** 面试官想看你对消息队列价值的理解。

**标准答案：**

原来用 gRPC `ReportLog` 同步上报，问题：
1. **StatusServer 挂了日志丢失**：gRPC 调用失败，日志就没了
2. **耦合**：各服务器直接依赖 StatusServer 的可用性
3. **扩展性差**：想接 ELK 要改 StatusServer 代码

用 Kafka 后：
1. **解耦**：各服务器只依赖 Kafka broker，不关心谁消费
2. **缓冲**：StatusServer 短暂不可用时，Kafka 缓冲日志不丢
3. **多消费者**：ELK、监控告警各自加 consumer group，互不影响
4. **异步**：Logger 回调是 fire-and-forget，不阻塞业务线程

**如果面试官追问"Kafka 挂了怎么办"：** gRPC `ReportLog` handler 保留作为兜底通道。Kafka 不可用时可以降级到 gRPC 同步上报。另外 Logger 本身会写本地文件，最极端情况下日志在磁盘上。

---

### Q13. KafkaProducer 的 produce 方法为什么用 RD_KAFKA_MSG_F_COPY 标志？

**追问方向：** 面试官想看你对 Kafka C 客户端 API 的理解。

**标准答案：**

`RD_KAFKA_MSG_F_COPY` 让 librdkafka 内部复制消息内容。

如果没有这个标志：
- librdkafka 只保存消息 buffer 的指针
- 调用方必须保证 buffer 在消息发送完成前不被释放
- 但 produce 是异步的，消息可能在回调完成后才真正发送
- 如果 buffer 是栈上变量或临时字符串，函数返回后内存就失效了

用 COPY 标志：
- librdkafka 在 produce 时就复制消息到内部 buffer
- 调用方不需要管 buffer 生命周期
- 代价是一次内存拷贝，但对日志场景影响很小

**如果面试官追问"性能影响大吗"：** 日志消息通常几百字节，拷贝开销可以忽略。如果是大消息（几 MB），应该用 zero-copy 方式，但日志场景不需要。

---

## 六、HTTP 网关

### Q14. GateServer 的鉴权是怎么做的？为什么用 Redis 存 Token 而不是 JWT？

**追问方向：** 面试官想看你对认证方案选型的思考。

**标准答案：**

鉴权流程：
1. 从请求头取 `Authorization: Bearer <token>` 和 `X-User-Id`
2. 查 Redis `utoken:{uid}`，比对 token 是否一致
3. 公开接口（登录、注册、验证码）跳过鉴权

为什么用 Redis 存 Token 而不是 JWT：
1. **需要服务端踢人**：JWT 是无状态的，签发后无法撤销。Redis Token 可以随时删除，实现强制下线
2. **跨服务共享**：GateServer、PushServer 都需要验证 Token，Redis 是天然的共享存储
3. **TTL 管理**：Redis 的 `SETEX` 天然支持 Token 过期，不需要额外的过期逻辑

JWT 适合的场景：
- 无状态、不需要踢人
- Token 需要携带用户信息（减少查库）
- 微服务间不需要共享会话状态

**如果面试官追问"Redis Token 的缺点"：** 每次请求都要查 Redis，增加一次网络往返。可以用本地缓存（短 TTL）减少 Redis 查询，但会增加踢人延迟。

---

### Q15. 登录接口为什么用 promise/future + AsyncTaskPool？直接在 IO 线程里调 gRPC 不行吗？

**追问方向：** 面试官想看你对异步编程模型的理解。

**标准答案：**

直接在 IO 线程调 gRPC 的问题：
- gRPC 调用是**同步阻塞**的（`stub->Login()` 会等待响应）
- 如果下游服务慢（比如 MySQL 查询耗时 2 秒），IO 线程被阻塞 2 秒
- 期间这个 IO 线程无法处理其他 HTTP 请求
- 如果并发 10 个登录请求，10 个 IO 线程全被阻塞，GateServer 就卡死了

解决方案：
```cpp
auto future = AsyncTaskPool::getInstance().post([request]() {
    return UserGrpcClient::getInstance().Login(request);
});
auto status = future.wait_for(std::chrono::seconds(5));
if (status == std::future_status::timeout) {
    // 超时处理
} else {
    auto response = future.get();
    // 返回响应
}
```

- IO 线程只负责 accept 和 read/write，不阻塞
- 阻塞的 gRPC 调用扔到 AsyncTaskPool 的线程池执行
- 5 秒超时防止无限等待

**如果面试官追问"为什么不全用异步 gRPC"：** gRPC 支持异步 API（`AsyncUnaryRpc`），但编程复杂度高很多，需要管理 CompletionQueue。对实习项目来说，promise/future + 线程池是更务实的选择。

---

## 七、综合追问

### Q16. 如果 PushServer 突然宕机了，连接在上面的用户会怎样？系统怎么恢复？

**标准答案：**

1. **用户感知**：WebSocket 连接断开，客户端检测到 close 事件
2. **客户端重连**：前端 `pushClient.js` 有自动重连逻辑，指数退避重试
3. **重连时重新分配**：客户端重新调用 `/user_login`（或专门的重连接口），GateServer 调用 `AllocatePushServer`，StatusServer 重新分配一个存活的 PushServer
4. **旧连接清理**：StatusServer 心跳超时（30 秒）后，把宕机节点的连接数设为 INT_MAX，不再分配新连接
5. **离线消息补推**：用户重新连接后，loginHandler 从 Redis 拉取离线消息

**如果面试官追问"30 秒内用户重连到同一个宕机节点怎么办"：** 不会。客户端重连时会调用 `AllocatePushServer`，StatusServer 会查心跳状态，如果节点已标记 offline（心跳超时），不会分配它。如果还在 30 秒窗口内，可能会分配到——但此时节点已宕机，TCP 连接会失败，客户端会再次重连。

---

### Q17. 你说"百级集群规模下节点分配效率提升约 93%"，这个数字怎么算的？

**标准答案：**

暴力遍历：每次分配需要遍历所有节点找最小值，O(N)
线段树：每次分配 O(log N)

假设 N = 100 个节点：
- 暴力：100 次比较
- 线段树：log₂(100) ≈ 7 次比较

提升比例：(100 - 7) / 100 = 93%

**如果面试官追问"实际性能差距有这么大吗"：** 理论上是的。但实际瓶颈可能在网络（gRPC 调用、Redis 查询）而不是 CPU 计算。线段树的优势在节点数更多时更明显（N=1000 时，暴力 1000 vs 线段树 10，提升 99%）。

---

### Q18. 项目中有哪些地方用了分布式锁？为什么这些地方需要锁？

**标准答案：**

| 锁 key | 用途 | 为什么需要锁 |
|--------|------|------------|
| `lock:login:{uid}` | 登录时 Token 读写 + 踢人标记 | 两个设备同时登录，不加锁会互相覆盖 Token，踢人消息发两次 |
| `lock:unread:{uid}` | 消息推送时未读计数 INCR + 缓存写入 | 并发推送多条消息时，INCR 和 LPUSH 之间可能插入其他操作，导致未读数和消息数不一致 |
| `lock:online:{uid}` | （已移除）在线状态设置 | 原本用于保护 `online:{uid}` 的 SET/DEL，后发现是死设计已移除 |

**如果面试官追问"为什么不用数据库行锁"：** 分布式锁要保护的是 Redis 操作，不是 MySQL 操作。用数据库行锁的话，每次都要先查 MySQL 再操作 Redis，多一次网络往返，而且 MySQL 锁的粒度和 Redis 操作不匹配。

---

## 八、数据一致性 & 缓存设计（补充）

### Q19. 数据库和缓存的一致性是如何保证的？强一致性还是最终一致？

**最终一致性**，通过"双写 + 登录校准"实现。

项目采用 MySQL 为主存储、Redis 为热缓存的分层架构。写入时双写 MySQL 和 Redis，读取时优先走 Redis。一致性靠以下机制保证：

- **写入双写**：发消息时同时 INSERT MySQL 和 LPUSH Redis，标记已读时同时 UPDATE MySQL 和 DECRBY Redis
- **登录校准**：用户 WebSocket 登录时，用 `SELECT COUNT(*) FROM messages WHERE uid=? AND is_read=0` 查询 MySQL 真实未读数，覆盖写入 Redis `unread:<uid>`，修复 Redis 累积误差
- **删除刷新**：删除消息时先删 MySQL，再从 MySQL 重新加载最近 50 条覆盖 Redis 缓存
- **兜底恢复**：Redis 重启后所有热缓存丢失，用户下次登录时 MySQL 会重新校准未读计数和消息缓存

不追求强一致的原因：强一致需要分布式事务（2PC/XA），对于训练场景的消息系统来说代价过高。当前方案保证"读到的数据最多延迟一个登录周期"即可修复。

---

### Q20. MQ 怎么用的？

项目使用 **Kafka** 作为日志收集的消息队列。

**架构**：
```
各服务器 Logger → KafkaProducer → Kafka(logs topic) → StatusServer KafkaConsumer → Redis
```

**生产者**：每个 C++ 服务器都有一个 KafkaProducer 单例。Logger 的 flush 回调触发时，将日志批次以 JSON 格式发送到 Kafka 的 `logs` topic，key 为服务名（如 `PushServer(127.0.0.1:8890)`），保证同一服务的日志进同一分区。

**消费者**：StatusServer 有一个单线程 KafkaConsumer，订阅 `logs` topic 的所有分区，consumer group 为 `status-server-log-group`。收到消息后解析 JSON，写入 Redis `logs:<service>` LIST。

**用途**：分布式日志聚合。各服务器的日志通过 Kafka 汇总到 StatusServer 的 Redis，教练在前端统一查询。

---

### Q21. 怎么保证 Kafka 一致性，消息不丢失？MQ 消息怎么保证可靠投递？

当前配置下，消息可靠性是**尽力而为**级别，不是强保证。

**生产端**：
- `acks = "1"`：只需要分区 Leader 确认，不需要 ISR 副本确认。Leader 宕机且 Follower 未同步时可能丢消息
- 使用 `RD_KAFKA_MSG_F_COPY`：消息缓冲区由 librdkafka 管理，不依赖调用方生命周期
- 析构时 `rd_kafka_flush(5000)`：等待最多 5 秒发送缓冲区中的剩余消息

**消费端**：
- `enable.auto.commit = true`，每 5 秒自动提交 offset
- `auto.offset.reset = earliest`：新 consumer group 从头开始消费
- 风险：自动提交意味着消息处理成功前 offset 就可能被提交，处理中途宕机会丢消息

**可改进方向**：生产端 `acks = "all"` + 开启重试 + 幂等 producer；消费端改为手动提交 offset。对于日志收集场景，当前配置可以接受。

---

### Q22. 消息队列如何保证最终一致性？

项目中 Kafka 的最终一致性靠以下机制：

1. **持久化**：Kafka 消息写入磁盘（WAL），不会因 Broker 重启丢失
2. **Consumer Group + Offset 管理**：`auto.offset.reset = earliest`，StatusServer 重启后从上次提交的 offset 继续消费
3. **多分区并行**：同一服务的日志通过相同 key 路由到同一分区保证顺序，不同服务并行消费
4. **最终一致的体现**：StatusServer 宕机重启后，Kafka 会把积压的日志重新投递，Redis 中的 `logs:<service>` 会补上缺失条目

---

### Q23. 如果 MQ 消息重复消费怎么办？

当前实现**不做去重**，靠业务层幂等性容忍重复。

**为什么会有重复**：自动提交 offset 场景下，消费成功但提交前宕机，重启后从上次 offset 重新消费。

**当前处理**：日志消费的业务逻辑是 `LPUSH + LTRIM + EXPIRE` 写入 Redis。重复消费 = 同一条日志被 LPUSH 多次 = Redis 列表中出现重复条目。影响：教练查日志时可能看到重复的日志行，不影响业务数据。

**如果需要去重**：消费端用消息的 `(topic, partition, offset)` 三元组做去重 key；或生产端开启 Kafka 幂等 producer（`enable.idempotence = true`）。

---

### Q24. 项目中实现分布式事务了吗？

**没有实现跨服务的分布式事务**（如 2PC、Saga、TCC）。

项目中的"事务"分两个层面：

**单服务内的 MySQL 事务**（已实现）：
- `createTask`：INSERT task + INSERT task_assignments × N，事务包裹
- `deleteTask`：DELETE task_assignments + DELETE task，事务内执行
- `updateTask`：UPDATE task + INSERT ON DUPLICATE KEY UPDATE task_assignments
- `approveUser`/`setUserRole`：SELECT captain + UPDATE user，事务内执行

**跨服务一致性**（靠最终一致）：消息系统 MySQL + Redis 双写靠登录校准；认证 Token 仅存 Redis 靠 TTL 过期。

---

### Q25. 数据库的索引怎么设计的？为什么这么设计？

| 表 | 索引 | 设计原因 |
|---|---|---|
| `user` | `UNIQUE(email)` | 登录查 `WHERE email=? AND password=?`，走索引 O(log n) |
| `user` | `UNIQUE(username)` | 注册判重 |
| `task` | `INDEX(uid)` | 查"我创建的任务" |
| `task` | `INDEX(assigned_to)` | `FIND_IN_SET` 查"分配给我的任务" |
| `task` | `INDEX(status)` | 按状态筛选 |
| `task_assignments` | `PRIMARY(task_id, assignee_uid)` | 联合主键，查询 `WHERE task_id=?` 走前缀索引 |
| `todo_list` | `INDEX(uid, priority, deadline)` | 复合索引覆盖 `WHERE uid=? ORDER BY priority, deadline`，避免 filesort |
| `checkins` | `UNIQUE(uid, checkin_date)` | 每人每天只能打卡一次，INSERT 判重走索引 |
| `messages` | `INDEX(uid, is_read)` | `COUNT(*) WHERE uid=? AND is_read=0` 走索引覆盖扫描 |
| `messages` | `INDEX(uid, created_at DESC)` | 分页查询 `ORDER BY created_at DESC` 避免全表排序 |

---

### Q26. Redis 分布式锁怎么设计的？粒度是多少？

**实现方式**：
- 加锁：`SET lock_key owner_id NX EX ttl`（NX 互斥，EX 防死锁）
- 释放：Lua 脚本原子 `GET + DEL`（校验 owner 后才删除，防误删）
- 重试：指数退避，最多 3 次，间隔 50ms → 100ms → 200ms

**锁粒度**：用户级

| 锁 Key | 粒度 | 保护的资源 | TTL |
|--------|------|-----------|-----|
| `lock:login:<uid>` | 单用户 | 登录分配：token 读写 + 踢人标记 | 10s |
| `lock:unread:<uid>` | 单用户 | 消息缓存：LPUSH + LTRIM + INCR | 10s |

每个用户的操作互不影响，锁的粒度刚好保护需要原子性的临界区。

---

### Q27. 总结：项目如何保证分布式环境下的数据一致性？

**三个层面**：

**第一层：单服务内 — MySQL 事务 ACID**
- 任务 CRUD、用户审批等多表操作用 `setAutoCommit(false)` + `commit/rollback`
- 打卡用 `UNIQUE(uid, checkin_date)` 约束保证幂等

**第二层：服务间 — Redis 分布式锁**
- 登录分配用 `lock:login:<uid>` 保护 token 读写的原子性
- 消息缓存用 `lock:unread:<uid>` 保护 LPUSH + INCR 的原子性
- 锁释放用 Lua 脚本保证 owner 校验的原子性

**第三层：MySQL + Redis — 最终一致性**
- 写入双写，读取走 Redis
- 登录时 MySQL 校准 Redis（未读计数 + 消息缓存）
- 删除时 MySQL 先删、Redis 后刷新
- Redis 故障恢复靠登录校准自动修复

没有做到的：跨服务分布式事务、Kafka exactly-once。这是基于项目场景（ACM 训练队管理）的合理取舍。

