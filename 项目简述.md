# 分布式训练任务协同系统 项目详细介绍

# 一、项目概述

OxyTeamTask 是一款面向 ACM 集训队的分布式训练任务协同系统，基于 C\+\+ 微服务架构开发，核心定位是解决集训队训练管理混乱、任务下发不及时、队员进度难追踪、赛况同步滞后等实际痛点，为集训队队员、队长、教练提供高效、便捷的训练协同工具。系统采用“后端微服务 \+ 前端高颜值桌面客户端”的架构模式，后端复用成熟的分布式技术栈，客户端采用 Electron \+ Vue 实现现代美观的交互界面，整体具备高并发、高可用、易部署、可扩展的特点，可直接部署于校园服务器，供集训队内部日常训练使用。

本项目核心价值：打通集训队训练全流程，实现任务下发、进度追踪、赛况同步、提醒推送的一体化管理，减少队长/教练的管理成本，提升队员的训练效率，同时将 ACM 竞赛算法（权值线段树）与分布式技术落地到真实场景，兼具实用性与技术展示价值。

# 二、业务背景

ACM 集训队作为培养算法竞赛人才的核心组织，日常训练存在诸多痛点，传统的训练管理方式（群聊下发任务、手动统计进度、线下汇报）已无法满足高效训练的需求，具体痛点如下：

- 任务管理混乱：队长/教练下发的每日/每周训练任务、刷题计划，仅能通过群聊、文档等方式传播，队员无法快速获取任务详情、截止时间，且任务完成情况无法实时追踪，易出现漏做、拖延的情况。

- 进度同步滞后：队员的刷题打卡、补题状态，需手动上报给队长，队长再汇总统计，过程繁琐且实时性差，教练无法及时掌握全队训练进度，难以针对性调整训练计划。

- 赛况同步不及时：队内训练赛期间，队员的解题数、罚时、实时排名无法同步展示，队员难以了解自身与他人的差距，教练无法实时监控赛况、分析队员表现。

- 通知触达不到位：任务截止提醒、训练赛通知、补题提醒等，仅能通过群聊发送，队员离线后无法接收，易错过重要训练节点。

- 权限管理缺失：队员、队长、教练的权限未做区分，普通队员可随意修改任务、查看他人隐私数据，管理混乱，存在数据安全隐患。

基于以上真实痛点，设计开发 TeamTask 系统，通过分布式架构实现任务协同、实时推送、权限管控，解决集训队训练管理的核心问题，提升训练效率与管理便捷性。

# 三、业务实现

本项目采用“后端 5 微服务 \+ 前端桌面客户端”的分布式架构，后端基于 C\+\+ 开发，采用 gRPC \+ Protobuf 实现服务间通信，MySQL 实现数据持久化，Redis 实现缓存与分布式锁，PushServer 集群实现实时消息推送，StatusServer 实现在线状态管理与集群负载均衡；前端采用 Electron \+ Vue 开发，实现高颜值、易用的桌面客户端，与后端通过 HTTP/WebSocket 通信，完成所有业务交互。

系统整体业务流程遵循“请求入口 → 鉴权调度 → 业务处理 → 实时推送”的逻辑，所有业务操作均经过网关统一入口，由对应微服务处理，最终通过 PushServer 将结果、提醒等消息实时推送给客户端，确保业务的实时性与可靠性。具体核心业务流程如下：

1. 用户登录：队员/队长/教练通过客户端输入账号密码，请求经 GateServer 转发至 UmsServer，完成身份校验与 Token 颁发，登录成功后由 StatusServer 分配 PushServer 节点，建立 TCP 长连接，用于接收实时消息。

2. 任务下发：队长/教练通过客户端发布训练任务，请求经 GateServer 转发至 TaskServer，完成任务存储，同时 TaskServer 通知 PushServer，将任务下发消息实时推送给所有队员客户端。

3. 每日打卡：队员到达机房训练后，通过客户端提交打卡，请求经 GateServer 转发至 UmsServer 鉴权，再由 TaskServer 记录打卡数据，最后 PushServer 推送打卡反馈消息给队员与队长。

4. 提醒推送：任务临近截止、训练赛开始/结束、队员离线后有未读消息时，PushServer 自动推送提醒，离线队员上线后，系统补发所有未读消息。

系统通过 Redis 分布式锁解决并发冲突（如多人同时打卡、队长同时下发任务），通过权值线段树实现 PushServer 集群负载均衡，确保高并发场景下的系统稳定性；通过 MySQL 持久化所有核心数据，确保数据不丢失；通过长连接实现消息实时推送，提升用户体验。

# 四、各微服务主要负责的业务逻辑

系统后端拆分为 5 个微服务，各服务职责单一、高内聚低耦合，独立部署，通过 gRPC \+ Protobuf 实现跨服务通信，具体业务逻辑如下：

## 1\. GateServer（API 网关服务，单机部署）

核心定位：系统统一请求入口，负责请求转发、限流、跨域、日志记录，隔离外部请求与内部微服务，提升系统安全性与可维护性。

主要业务逻辑：

- 接收前端客户端的所有 HTTP 请求（登录、任务查看、打卡提交等）；

- 对请求进行合法性校验（如请求格式、Token 初步校验），拦截非法请求；

- 根据请求类型，将请求转发至对应微服务（如登录请求转发至 UmsServer，任务请求转发至 TaskServer）；

- 记录所有请求的操作日志，用于系统排查与审计；

- 处理跨域请求，确保前端客户端与后端微服务正常通信。

技术实现：基于 Boost\.Beast 开发，复用原有 HTTP 网关代码，无需额外开发核心逻辑。

## 2\. UMSServer（用户权限服务，单机部署）

核心定位：系统用户管理与权限管控中心，负责用户登录、注册、角色权限分配、Token 管理，确保系统数据安全与操作合法性。

主要业务逻辑：

- 用户管理：处理用户注册、登录、密码修改、个人信息维护，将用户信息持久化至MySQL数据库；

- 权限管控：基于三级角色（队员/队长/教练）实现权限区分，控制不同角色的操作范围（如队员仅能查看自己的任务与打卡记录，队长/教练可下发任务、查看全队进度）；

- Token 管理：用户登录成功后，颁发 JWT Token，用于后续请求的身份校验；同时负责 Token 的过期管理、刷新，通过 Redis 缓存 Token，提升校验效率；

- 角色管理：使用MySQL数据库维护角色信息，支持角色权限的灵活配置，适配集训队的管理需求；

- 提供用户相关的 RPC 接口，供其他微服务调用（如 GateServer 调用其进行 Token 校验，TaskServer 调用其获取用户角色信息）。

技术实现：由原有 StatusServer 复用，核心逻辑不变，新增角色权限相关接口。

## 3\. StatusServer（状态\&amp;调度服务，单机部署）

核心定位：系统在线状态管理与 PushServer 集群调度中心，负责队员在线状态汇总、PushServer 节点管理、负载均衡调度，是系统高并发通信的核心支撑。

主要业务逻辑：

- 在线状态管理：接收 PushServer 上报的队员在线/离线状态，汇总至全局状态，维护队员会话信息，支持在线人数统计；

- 负载均衡调度：基于权值线段树算法，根据各 PushServer 节点的当前连接数，为新登录的队员分配最优的 PushServer 节点，确保集群负载均衡，提升系统并发处理能力；

- 节点健康检查：定期检测 PushServer 节点状态，若节点离线，自动将该节点上的队员连接迁移至其他在线节点，确保消息推送不中断；

- 提供状态查询、节点调度相关的 RPC 接口，供 GateServer、PushServer 调用。

技术实现：服用原 StatusServer 的调度能力，将权值线段树算法集成其中，复用原有在线状态管理代码，新增集群调度相关逻辑。

## 4\. TaskServer（训练任务核心服务，单机部署）

核心定位：系统训练任务与赛况管理中心，负责训练任务的发布、打卡记录的管理，是系统的核心业务服务。

主要业务逻辑：

- 任务管理：处理队长/教练下发的训练任务，包括任务发布、编辑、删除、查询，将任务信息持久化至数据库表；支持按任务状态（进行中/已截止）、难度、发布人筛选任务；

- 打卡管理：接收队员的打卡提交请求，记录打卡信息，确保一人每天只能打卡一次；

- 任务进度统计：统计全队、单个队员的任务完成情况（已完成/未完成/逾期），为队长/教练提供训练进度可视化数据；

- 提供任务、打卡的 RPC 接口，供其他微服务调用（如 PushServer 调用其获取任务详情，用于消息推送）。

## 5\. PushServer（实时推送服务，集群部署）

核心定位：系统实时消息推送中心，负责通过 TCP 长连接，将任务通知、打卡反馈、提醒等消息实时推送给客户端，是系统实时性的核心保障。

主要业务逻辑：

- 长连接管理：与客户端建立 TCP 长连接，维护连接会话；通过心跳机制检测连接状态，若连接中断，自动尝试重连；

- 实时消息推送：接收其他微服务（TaskServer、UmsServer）的消息推送请求，将任务下发、打卡反馈、截止提醒等消息，实时推送给对应队员客户端；

- 离线消息缓存：若队员客户端离线，将消息缓存至本地，待队员上线后，自动补发所有未读消息；

- 消息广播：支持批量消息推送（如任务下发给所有队员）；

- 节点上报：定期向 StatusServer 上报自身节点状态（当前连接数、健康状态），供 StatusServer 进行负载均衡调度；

- 处理客户端的消息确认请求，确保消息推送可靠，避免消息丢失。

技术实现：基于 Boost\.Asio 开发，复用原有 TCP 长连接代码，支持多节点集群部署，由 StatusServer 统一调度。

# 五、客户端功能实现（Electron \+ Vue）

客户端采用 Electron \+ Vue 开发，打造现代、美观、易用的桌面端界面，支持 Windows 系统部署，供集训队队员、队长、教练使用，核心功能按角色划分，覆盖所有业务场景，具体功能如下：

## 1\. 通用功能（所有角色均可用）

- 登录/注册：输入账号密码登录系统，支持密码加密存储；新用户可通过邮箱注册账号，通过MailerServer发送验证消息，随后收录注册记录，由管理者同意后方可注册成功；

- 个人中心：查看个人信息（学号、姓名、角色），修改密码、绑定手机/邮箱；查看个人训练统计（完成任务数、打卡次数）；

- 消息中心：接收实时消息（任务通知、打卡反馈、提醒），查看未读消息、消息历史；支持消息标记已读、删除；

- 离线消息补发：客户端离线后，上线自动接收所有未读消息，确保不遗漏重要通知；

- 界面设置：支持深色/浅色模式切换，调整界面字体大小，适配不同用户习惯。

- 任务看板：查看所有训练任务（进行中/已截止），按难度、发布时间筛选任务；查看任务详情（标题、描述、开始/截止时间、发布人）；

- TODO LIST：队员可以为自身设置任务，设置包括任务优先级、截止时间等指标，同样截止期前有截止提醒，类似于TODO LIST

- 打卡：每日打卡，提交打卡信息，提交后可查看打卡状态；

- 提醒接收：接收任务截止提醒、打卡反馈提醒，避免错过重要任务；

## 2\. 队长专属功能

- 任务管理：发布训练任务（设置标题、描述、开始/截止时间）；编辑、删除已发布的任务；查看所有任务的完成情况（全队完成率、未完成队员名单）；

- 全队进度查看：查看全队队员的训练进度、打卡记录，针对性下发任务；

- 消息推送：向指定队员或全体队员推送通知（如训练调整、补题提醒）。

## 3\. 教练专属功能

- 任务管理：发布训练任务（设置标题、描述、开始/截止时间）；编辑、删除已发布的任务；查看所有任务的完成情况（全队完成率、未完成队员名单）；

- 全队进度查看：查看全队队员的训练进度、打卡记录，针对性下发任务；

- 消息推送：向指定队员或全体队员推送通知（如训练调整、补题提醒）。

- 权限管理：管理所有用户账号，审核新用户注册请求；修改用户角色（队员/队长/教练）；

- 全局监控：查看所有队员的在线状态、训练进度，查看 PushServer 集群节点状态，监控系统运行情况；

- 系统日志查看：查看系统所有操作日志，排查异常操作、追溯问题。

## 5\. 界面设计亮点

- 现代扁平化设计：采用简洁、专业的界面风格，搭配清晰的色彩区分（如任务状态：进行中\-绿色、已截止\-灰色、逾期\-红色）；

- 实时可视化：训练赛排名、任务进度采用图表展示（如柱状图、折线图），直观呈现数据；

- 交互便捷：支持任务拖拽、打卡快速提交、消息快捷回复，提升用户操作效率；

- 响应式布局：适配不同屏幕尺寸，在笔记本、台式机上均能正常显示。

# 六、项目核心亮点

1. 场景真实：完全贴合 ACM 集训队日常训练需求，解决真实痛点，可直接部署使用，而非虚拟项目；

2. 架构标准：采用企业级微服务架构，5 个微服务职责单一、高内聚低耦合，符合后端开发规范，面试官认可度高；

3. 技术落地：将 ACM 竞赛算法（权值线段树）与分布式技术（微服务、分布式锁、集群负载均衡）结合，体现技术深度；

4. 颜值与实用性兼具：前端采用 Electron \+ Vue 实现现代美观界面，后端复用成熟技术栈，兼顾用户体验与技术展示；

5. 易部署、易扩展：后端服务独立部署，PushServer 支持集群扩容，可根据集训队规模灵活调整；前端打包为 Windows 安装包，队员可直接安装使用。

# 七、部署说明

- 后端部署：GateServer、UmsServer、StatusServer、TaskServer 单机部署于校园 Linux 主机；PushServer 集群部署（2\-3 个节点），由 StatusServer 统一调度；

- 数据库部署：MySQL、Redis 单机部署，与后端服务部署在同一主机，确保数据访问高效；

- 客户端部署：打包为 Windows 安装包，队员、队长、教练可直接下载安装，输入服务器 IP 即可连接系统。
